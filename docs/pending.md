# 等你拍板的事（跨会话的待办清单）

这份清单只放一类事：**已经做出来了、但最终取值/取舍该由你定**的东西。每条都写清
「现状 / 可选的几个值 / 代价」——数值大多在 `RANK_3D`（`assets/js/card-3d.js`）或 CSS 令牌里，
改一个数就能变，**不需要重出任何图**（要重出图的会单独注明）。

为什么要有这份文件：这些事以前只活在会话记忆与提交说明里，换个会话就找不到。
**动过的条目标上日期或在提交里收掉，别让它烂在这里。**

---

## 3D 卡（`/collection/` 的弹层里那张）

| 事 | 现状 | 可选的 | 代价 |
|---|---|---|---|
| **浮雕高度** | 传世 2.40 / 奇迹 2.90（基准 3.2% 卡宽 → 7.7% / 9.3%） | 1.9/2.3（最像真卡）· 2.4/2.9（现用）· 2.9/3.4（最夸张） | 改 `RANK_3D.relief`；对比图 `../lab/结果/deck7/sheetA.png` |
| **透明盖的强度** | `lid` .75 / 1.0，光锥 `cone` .70 / 1.0 | 薄盖 .35 · 厚盖 .75（现用）· 只奇迹才有 | 改 `lid`/`cone`；对比图 `deck7/sheetLid.png` |
| **「脱离感」四条** | `wall` .70 / `cast` .65 / `drift` .60（奇迹全 1） | 逐条可关（对比图 `deck7/sheetDetach.png`） | 改对应字段；`cast` 最强、`drift` 静图上几乎看不出 |
| **彩虹流光的力度** | 权重 0.20，且只在掠射角明显 | 调高会更「紫雾」，调低接近看不见 | 我只在静图上定不了它，要转起来看 |
| **星屑密度** | 280×280 格子里约 0.6% 亮（≈470 颗） | 再稀一半（含蓄）· 更密（参考站量级，浅色画上会读成「沙」） | 改片元里 `pow(n,110)*2.5` 两个常数 |
| **真 die-cut（人物切出卡框）** | **没做**（样张结论：不做） | 引分割模型 + 63 张蒙版 + 卡体透明 | 样张与四个数在 `deck7/diecut.png`；要推翻这个结论就说一声 |
| **卡面贴图分辨率** | 查看器用 760px（xl 档） | 1000px 档会更锐（2x 屏上现在仍需放大 1.36×） | 63 张 ×约 60 KB ≈ +3.8 MB，预算要再抬 |
| **移动端真机** | **从未量过** | 现在只有「4× CPU 限速 + 小视口」的近似读数 | 有真机就能把调速器的档位标定一次 |

## 卡片本身（CSS 令牌，改完要重新量对比度）

| 事 | 现状 | 可选的 |
|---|---|---|
| **灰度明度阶梯** | 六档明度 184/180/178/170/169/189（**不单调**，靠形状与内缩扛） | 把收藏/珍稀压暗一档，让灰度下也成阶梯 —— 纯视觉取舍 |
| **「金边」这个名字** | 框的材料已归等级，低档看到的是灰框 + 一层金质感 | 改名（如「拉丝」）或不改 |
| **雕花线粗细** | 比参考图细 | 生成器里一个常量（`tools/cards/make-ornaments.py`） |
| **收藏库列数** | 宽屏 3 列（每张 205px） | 4 列更密（155px 下十几种工艺糊成一片，实测过） |
| **导航第 9 项的角色** | 「收藏库」在首页与数学库之间 | 位置/权重 |

## 收藏库第三轮（2026-09-21 晚做的，下面这几条的取值等你拍板）

| 事 | 现状 | 可选的 / 备注 |
|---|---|---|
| **分组视图的默认值** | 默认**平铺**（来源：这一页的主职是「一次看全」，分组是给「找某一批」用的另一种读法） | 默认分组：改 `deck-wall.js` 里 `grouped` 的初值即可（`?group=series` 那条 URL 参数照旧能用） |
| **卡号写法** | `#07`（与首页名牌、弹层里的 `07 / 63` 同口径、同数字，只是写法不同） | 改成 `07 / 63` 会更一致但更占宽 —— 一格只有 205px |
| **等级比例条的粗细** | 5px（改前 3px，实测「容易被忽略」） | 6~8px 更醒目（会开始像一条分隔线）；也可以加数字刻度 |
| **抽卡按钮的位置** | 在结果行最右端（张数那一行的右侧） | 想让它像看板娘那样浮在卡片墙角落，需要另做定位与窄屏避让 —— 说一声就做 |
| **hover 扫光** | 0.7s、只在悬停那一格跑一次；秘藏以上才**看得见**（低档的雕花层 opacity 0） | 想给低档也看见，得先给它们一层可显形的框（那是等级体系的事，不是这一条） |
| **显形的持久化** | **不持久化**：关掉弹层/刷新之后，那一格又显示「收藏」（与卡面观感一致） | 想「显形过就记下来」要存 localStorage，代价是首帧与 JS 状态不一致的那一段要处理 |

## 卡片第四轮：数学曲线（2026-09-21 深夜做的，取值等你拍板）

| 事 | 现状 | 可选的 / 备注 |
|---|---|---|
| **框环宽度** | `--fret-w` = 0~11px（= `cart-inset − rank-frame`） | 角花在这个宽度上**只能换「轮廓家族」**（凹口 / 圆盘 / 齿环），画不出真的花瓣。想放真曲线就得把框环加宽到 ~16px —— 代价是画面每边少 5px |
| **徽记尺寸** | 15 / 17 / 19 / 21 / 23 / 26px（同时是「越大越稀有」的第二个信号） | 再拉大一档（17/19/22/25/28/32）：205px 的格子上 32px = 15.6%，开始抢画面 |
| **蝴蝶曲线的点数** | 240 点（≈2.2 KB raw，**全局样式表每页都吃**，gzip 约 +0.9 KB） | 降到 160 点省约 1/3 字节；我没压是因为 26px 下翅缘正是曲率最大的地方。压之前建议先出一张 96px 对照图 |
| **卡片内层圆角** | 根是超椭圆（`--cshape: superellipse(4)`），内层 8 处 `--cart-r` 仍是圆弧 —— 差 ≤1.4px | 有意留着（画框内缘更圆，更像实物）；要统一就在那 8 处加 `corner-shape: var(--cshape)` |
| **收藏档徽记的填充** | 由近白改成中间调 `#a9aeb9`（浅色主题下原来的近乎看不见） | 想退回「银白」观感就改两处 `--rank-mark-fill`（收藏与奇迹显形前共用同一个值） |
| **`coneC` 在秘藏/史诗是空转** | `RANK_3D.arcane.coneC = .45`、`epic = .20`，但这两档的 `cone` 是 0 —— 曲线光斑**乘在一个 0 上**，等于没开（2026-09-23 复核：原样未动） | 要么给这两档开一点 `cone`（如 .25/.35），要么把 `coneC` 归 0 免得表里出现「看着有、实际没有」的值。我倾向后者：那张表是被人读的 |
| **超椭圆的指数** | `CARD_N = 4`（CSS 的 `--cshape` 与 3D 的几何/SDF 三处同一个数） | 3.5 更接近「圆润的方」，5~6 更接近「切角」；改的话三处一起改，改完看 `lab/结果/deck9/corner.png` 那张角对照图 |

## 和谐性诊断（第二次取证，2026-09-23）

问的是「卡片不够好看和谐」。这一轮把 63 个格子按访客真实像素逐格裁下来（`colltiles.py` → `tiles/`），
量各格的明度/彩度，并做了一组「现状 vs 明度归一化」的对拍（`collharmony2.py`）。

量出来的散度（格子实拍，含 CSS 框环；只算卡面那一块）：

| 口径 | 现状 | 明度归一化后（提案近似） |
|---|---|---|
| 卡面平均明度 min / 中位 / max | **76 / 203 / 227** | 147 / 217 / 236 |
| p10 / p90 | 102 / 221 | — |
| 标准差 | **44.0** | 18.1 |
| 彩度 > 40 的卡 | 3 / 63 | — |

- 人物在卡面里的高度占比 0.86~0.98（标准差 0.03）—— **取景是统一的**，问题不在取景比例；
  但**景别**仍混杂（源图本身是特写的仍旧是特写，全身的被裁成胸像），这一条是源图属性，算法归一不了。
- 框环是固定配方 `rgba(255,255,255,.34)`：在 L76 的暗卡上是硬白线，在 L227 的亮卡上几乎看不见 ——
  **同一套框在不同卡面上读数不一致**，这是「不像一套」的第二个来源，且与「低三档分不出」是同一个根。
- 相邻格子明度差 > 70 的有 7 处，最大 186（#59 冷香 223 → #60 初始之音 37，按原画口径）。
- 独立判读（三次、不同取样）结论一致：整墙最大的问题是**明度断层**（暗卡像黑洞、亮卡融进白底，缩略下呈「补丁感」）；
  全局拉齐会明显变整齐，但会把高调卡（#33 银霜、#03 花信）洗成白雾 —— 所以**只能收极端值，不能全局拉平**。

对拍图：`lum12-nat.png` / `lum12-norm.png`（12 张跨明度取样，真实像素）、`wall-ba.jpg`（整墙 63 张左右对照）。

## 收藏库观感复核（第一次取证，2026-09-23；数据在 `lab/结果/collreview/`）

| 事 | 现状 | 可选的 / 代价 |
|---|---|---|
| **首屏 0 张完整卡** | 1440×1000：标题顶 199、筛选条顶 833 + 高 163、**第一排卡顶 1063**（视口 1000）；390×844：筛选条 1210 + 349、**卡顶 1625**。页高 8527 / 10583（21 排） | ① 说明那三段折叠成一行 + 展开；② 筛选条默认收起、点「筛选」才展开；③ 认。要在 1440 视口里露出第一排，得省约 200px，改文案最快、动筛选条最贵（它有 5 排 pill） |
| **墙上低三档分不出来** | 框宽 2 / 2 / 2px（收藏/珍稀/史诗）、框环一律 8px，框色相近；秘藏 2.5/9、传世 3/9.5。独立判读两次都判「收藏·珍稀·史诗 分不出」，实际靠格子下方那行文字 | ① 只拉低三档之间的框环/框色差（中高两档不动）；② 认「等级靠标签」；③ 顺手把灰阶明度阶梯（184/180/178/170/169/189 不单调）一次修好 —— ①③ 同一轮做最省 |
| **弹层「脱离感」的硬切** | 发饰/头发越过卡框处被卡框硬切；独立判读两次给出「悬浮 / 重复」；`RANK_3D.legend.relief = 2.40`、`miracle = 2.90` | 先做一次**同尺寸对拍**（平面卡面 vs 画布，素材已在 `lab/结果/collreview/`）再决定是否降 `relief` —— 判读文字不足以定罪，**没对拍之前别改数** |
| **卡面明度散度 44（整墙「补丁感」的根）** | 63 张卡面平均明度 76 → 227（中位 203），标准差 44；相邻格最大差 186；缩略扫视时暗卡像黑洞、亮卡融进白底 | ① **只收极端值**：暗卡（L<110 那 5 张）抬中间调、过亮卡（L>222）压到 ~215 并加极轻中性底；② 全局拉进 [0.72,0.84]（`lum12-norm.png` 那版）——明显更整齐，但把 #33 银霜 / #03 花信 洗成白雾，**我不建议**；③ 认。落在 `tools/cards/make-cards.py`，重出 63 张 + 复核体积预算 |
| **框环 recipe 固定（低三档分不出、明暗两端读数不一）** | `--rank-ring: rgba(255,255,255,.34)`，六档共用；暗卡上是硬白线、亮卡上几乎看不见 | ① **按卡面明度自适应**（亮卡走深中性、暗卡走白；清单里已有 `tint` 可当输入，纯 CSS/JS，不动产物，可当天回滚）；② 给框环垫一层极浅的中性描边；③ 认。① 同时能把 2 / 2.5 / 3px 的档位差读出来 |

## 站点级

| 事 | 现状 | 备注 |
|---|---|---|
| **动态背景要不要留在循环里** | **2026-09-22 晚已改**：拆成两根轴 —— `#bg-switch` 只管静态壁纸、`#bg-dyn` 管动态背景（关闭 → 极光 → 夜樱 → 关闭），两份偏好互不污染，但**同一时刻只显示一套**（互斥：点壁纸会把动态关掉，动态开着时这一下不换套、按钮上写着哪张就显示哪张；开动态则壁纸退场） | 还可调：① 动态套的名字（现在是「极光」「夜樱」）；② `dynamicDefault` 留空 = 首屏回到静态壁纸；③ 加第三套只需在 `[[params.appearance.shaders]]` 里加一条（`kind` 得在 `bg-shader.js` 的 `KINDS` 里有对应画法）；④ 「关掉动态那一刻零等待」与「动态开着时不下壁纸」是一笔交易的两头 —— **第四版选的是后者**（跨轴空闲预取下线、改意图预热，`prefetchIdle()` 里一行 `if (dynId) return;` 就是那个开关）；代价落在触屏 + 慢网（没有悬停，实测 875 ms）。同一轮还让引擎**惰性挂载**（静态壁纸的访客不建 canvas / 不取 GL 上下文） |
| **搜索索引余量** | `public/index.json` 49,970 B / 预算 **56 KB**（2026-09-21 从 52 抬到 56，余量 14%；gzip 15.4 KB） | 下一次加内容页就会顶到；要么治内容要么改口径。2026-09-23 复核：仍是 48 KB / 56 KB |
| **公式页的渲染后端**（KaTeX 那份隐藏的 MathML 副本要不要留） | 公式重页里 MathML 占 raw 的 25–55%（无障碍用）。2026-09-23 实测 `output: "html"`（Hugo 0.165 认）：gzip 问题一 71.3 → 58.0 KB、notes-02 69.2 → 52.1、作业 59.5 → 45.1、notes 62.1 → 47.0（**−19~−25%**）；保留 a11y 就换成给外层加 `aria-label="<TeX>"`，仍 **−13~−17%**（读屏拿到 TeX 源码、不是排版语义） | 另一条方向**已经做过试点**：`output: "mathml"` 让浏览器原生排版（DCL −58~68%、DOM −67~75%，代价是 `\underbrace` / 求和上下限 / `\bigg` 的排版退化清单），在独立分支 `exp/mathml`（worktree `lab/工作树/mathml`，报告 `docs/exp-mathml.md`）。**三选一要你拍**：这是「读屏可用性 vs 每页 10~17 KB gzip」的交换，不是纯性能题。改法是 `layouts/_markup/render-passthrough.html` 一行 `output`，`throwOnError` 保持 true |
| **收藏库里在弹层里翻卡还是慢**（约 2 s，512 kbit/s） | 2026-09-22 晚加的那套（低清先上 / 预取 / 意图预取）在这一档上**没有收益**：它要「先上」的低清是相邻格子的图，而慢网下那一张浏览器还没下到，低清自己也在等网络。三种排队顺序都试过，1.6～3.3 s 之间抖 | 要真解决得换思路：① 让墙上的格子**提前下相邻格**的图（带宽账要重算）；② 弹层翻页时先复用上一张的贴图、xl 到了再换（等于放弃「先上低清」那一步）；③ 认了 —— 本地服务是 HTTP/1.0 + `Connection: close`，线上没量过，先量线上再决定（`lab/结果/deck-perf/check-cardflow.py --only wall-next`） |
| **背景壁纸** | 单张 137 / 127 KB（此前是全站最重的图），1600×900，窄屏只露出约 26% 宽度 | **2026-09-22 起动态背景是首屏默认**（`dynamicDefault`），这些图不进首屏（`--bg-image-*` = `none`）；**2026-09-22 晚第四版起动态开着时空闲期也不再预取**（跨轴两张：浅色 164 KB / 深色 271 KB，改由「悬停/聚焦/按下任一个背景按钮」的意图预热接手）—— 首页总传输 744,974 → 582,605 B。代价：触屏没有悬停，关掉动态那一下在慢网上要等（实测 512 kbit/s 875 ms vs 有意图预热 270 ms；`check-prefetch.py` 4 跑）。母图尺寸已量（2026-09-21：全 1600×900）。按视口出 1280/1920 两档 + q 降一档估 −35%；观感要你拍 |
| **收藏库一页的图片重量** | 滚完整页 860 KB（DPR2 时 1.7 MB） | **2026-09-22 已落地 412 档**（`docs/features.md` 追加九）：DPR2 卡片下载 1710.4 → 1286.1 KB（−24.8%），DPR1 974.1 KB；代价是产物 +1.29 MB、整站 gzip 预算抬到 11264。**还剩一笔**：`--art` 改用 CSS `image-set(url(272) 1x, url(412) 2x)` 可在 DPR1 下再省约 100 KB（5 张玻璃卡各一次的 412），代价是 Chromium < 113 上那一层底图整条声明失效 —— 玻璃厚度层会退化成没有折射，所以先不冒 |
| ~~**主样式按页拆包**~~ · **2026-09-23 已落地** | 阅读页 gz **36,934 → 13,415 B（−64%）**；`/collection/` −2.5 KB；首页 +747 B（多两个条件外链）。逐节点计算样式对拍：首页 341 / 收藏库 1267 / 列表页 166 / M1 notes 26,871 个节点，**真差异 0** | 做法、全部数字与核对方法见 `docs/features.md` 第 6 节**追加十**；接线由 `scripts/check-css-split.mjs`（阻断）守 |
| **壁纸的发起时机** | `bg-daylight-girl` 96.8 KB 由 CSS 解析后才发起（start 0.64~0.99 s，三页一致） | 在 `<head>` 按已定主题 preload 可与主样式并行；**本地 HTTP/1.1 测不出干净数字**，要线上前后对照（口径同 KaTeX 字形那条） |
| **M1 三页拆页** | 作业 29701 / notes-02 28160 / notes 26904 元素；作业 4G 下 DCL 3177 ms，仍是全站最重 | 手术同问题三（23566→16222 元素，最长长任务 97→64 ms）；拆哪几节由你定 |
| ~~**仓库根的 `d/`**~~ · **2026-09-23 收掉** | 目录已不存在（`test -d d` 为假，git 里也没有），这条是过期的 | — |

## 卡片全面升级（S1–S3）：进度与撤回记录（2026-09-23）

### S1+S2 已落地并推送（`d416525`）

| 改动 | 做法 | 落点 |
|---|---|---|
| 全档物理卡边 | box-shadow 加左上 1px 受光 + 右下 1px 背光，**刻意不表达等级** | `21-card-deck.css` 1 处 |
| 内衬线自适应 | 新增令牌 `--ring-ink`，模板按主色亮度分支；63 张里 51 张拿到暗墨、12 张保持原白线 | 令牌 + `deck-wall.html` / `home-cards.html` 各一行（要 `safe.CSS`，否则 Hugo 把值转义成 `ZgotmplZ`） |
| 六档材质色阶 | 亮度逐档递减 + 彩度逐档递增（改前秘藏彩度 19.3 反而低于史诗 28.7，阶梯是断的） | 六条 `--cframe` |

令牌调色板（工艺无关）：亮度 204.8 / 187.2 / 165.2 / 149.9 / 137.8，彩度 10.4 / 40.6 / 46.8 / 71.8 / 83.6。
63 格真实像素复测（`lab/工具/collcompare.py`）：框带亮度 194/189/187/177/148 → 197.6/189.8/179.9/173.3/146.3，单调有阶。

### S3（工艺标尺）：调了两遍又撤回

按 `craftaudit.py` 的「装饰强度」算系数 `--apply` 了两遍（改 `data/card-styles.yaml` 的 opacity / 印纹 alpha），
第三遍做单旋钮对照时发现**指标不可用**，全部撤回（`git checkout data/card-styles.yaml` + 重出产物，工作树已干净）：

- filigree 印纹不透明度从 .78 砍到 .30，逐像素平均差动了 19，指标只从 59.8 动到 41.5 —— 它由重采样基线与等级覆膜主导。
- 换高频能量比（`lab/工具/craftread.py`）v2/v3/v4 三版同值，对渐变型印纹完全盲。
- 尺子本身也有问题：墙上卡面只有 205 CSS px，渐变型印纹在那儿只剩低频色偏 —— **判工艺要在弹层的 1050px 物理像素上量**。

S3 的前提是换一把有响应的尺子（弹层尺度 + 单旋钮对照），不是接着调 opacity。教训已写入 `blog-audit` §19。

### 3D 出框层：跟着 relief 档走（已取证）

按档对拍弹层卡面上缘（`lab/工具/relieftest.py` → `relief-3rank.jpg`）：收藏档（relief 1.00）卡框上缘干净；
传世（2.40）与奇迹（2.90）才出现「发饰漂在框上 / 第二份」。现象由 `RANK_3D.*.relief` 驱动，是 3D 侧产物而非原画重复。
收不收要在会动的卡上判 —— 静态裁图只作候选。

### 3D 轴的两条否定结论（2026-09-23，都有数字，别再重做）

**① 弹层纹理升 1024：买不到细节，不做。** 63 张卡面的**原始素材**可用宽度 min 319 / 中位 **498** / max 4637
（`src` 的尺寸乘 crop 宽比；`figure` 口径按 cover 算），能真撑到 1024 的只有 **3 张**。现在的 xl 档是 760×1064，
对中位数那张已经是放大；再出 1024 档 63 张三档合计 **3.11 MB**（q72），而整站 gzip 余量只剩 666 KB。
要真提弹层锐度只能换高分辨率源图重出卡面，或做超分后处理（伪细节）——两条都比换档位贵。

**② `sharp` 这个旋钮已经在正确位置上，不加。** 用 `card3d.setFx({sharp:v})` 做单旋钮对照（同卡、同角度、只动这一个数），
三张素材原生宽度不同的卡（1684 / 369 / 319）测 acutance 与高通肩部：

| 卡 | sharp 0 → 1.0 的 acutance | 亮侧 p99 / 暗侧 p1 |
|---|---|---|
| 木刀（原生 1684） | 2.706 → 3.725（+37.7%） | 24→36 / −29→−45 |
| 玄袖（原生 369） | 4.575 → 5.769（+26.1%） | 28→36 / −34→−48 |
| 银绢（原生 319） | 3.010 → 3.690（+22.6%） | 24→31 / −33→−47 |

旋钮**有响应**（这是与 S3 那把坏尺子的区别），但 1.0 在低清素材上明显过锐出白边/锯齿，产品表的上限 0.30 合理。
另外 acutance 的基线**与素材分辨率不相关**（玄袖 369 反而最高 4.575，木刀 1684 只有 2.706）——
「按素材分辨率补锐度」这条提案**不成立**，不做。台账读数在 `lab/工具/sharpmeasure.py`，对照图 `sharp-sheet.jpg`。

### 立体效果单旋钮对照：传世/奇迹两档已经过头（2026-09-23 已改）

用 `card3d.setFx({relief:v})` 在同一张卡（#33 夜翎）、同一角度（yaw 0.30 / pitch 0.10）上只动浮雕：

| relief | 生效值 | 亮度 std | p95−p5 | 边缘对比 | 与上一档的逐像素差 |
|---|---|---|---|---|---|
| 0.6 | 0.026 | 30.59 | 86 | 1.144 | — |
| 1.2 | 0.053 | 32.35 | 87 | 1.469 | 4.08 |
| 2.0 | 0.088 | 33.48 | 90 | 1.872 | 5.17 |
| 2.9 | 0.128 | 34.43 | 94 | **2.292** | 6.52 |

三个指标全部单调动 —— 旋钮有响应（对照 S3 那把坏尺子）。判读结论：0.6 太平（像印刷）、2.9 明显「剪纸贴上去／边缘像崖壁」、
**1.2–2.0 之间最像真卡压出来的**（1.2 最自然、2.0 是上限附近）。
与「出框层只在 relief ≥2.4 出现」那条取证一致 → **传世 2.40 → 1.90、奇迹 2.90 → 2.20**（六档仍单调，`check-deck` 守卫过）。

### 「工艺能不能融进 3D 卡」：现状与一次失败的尝试

现状：3D 卡上的工艺只有六个参数（`foil / foilScale / spec / tint / sparkle / edge`），**没有纹样** ——
纹样（和纸、冰裂、星屑、珐琅格、雕花金…）只活在 DOM/CSS 那一层，弹层的 canvas 里看不到。

尝试（`lab/工具/patternproto.py`）：把珐琅格画进**浮雕高度图**（格线抬升 0.055，格 46px），直接改 public 里那张指纹化的
深度图，前后各拍一张对照。结果：**平均绝对差 0.03/255、改动 >3 的像素 0.1%** —— 等于没动。
再加一个夸张对照（把整张高度图抬到 0.85）也只有 0.11/255、0.7% —— 仍然几乎无变化。
但同一次会话里 `stats().relief` 为真（深度纹理已绑）、`relief` 旋钮的对照又明显有响应（上表）。

**所以「工艺进高度图」这条路目前不成立，且原因还没查清**：位移幅度的旋钮有响应、而高度图内容改了却没反应，
两者矛盾。下一步要查的是「uDepth 到底在哪一步被采样、弹层这个角度走的是哪条分支」，别在没查清前对外声称能做。

三条候选路（由低到高）：① 查清后把纹样进高度图（让它成为真的表面起伏）；② 着色器里做程序化纹样，
由 `data/card-styles.yaml` 生成一张 `STYLE_PAT` 表（纹样参数的单一事实源仍在 YAML）；③ 把 CSS 纹样烘进卡面纹理 ——
会复制事实源，不推荐。

### 3D 轴现状（别重复劳动）

`RANK_3D` 已是 6 档 × 22 通道、**每档每通道必写且六档单调不减**（`check-deck.mjs` 守卫），
卡背也已按档分级（`back: 0~5` + `BACK_RING_A/W` 两张表：素背 → 单细环 → 单环 → 加粗 → 双环+等级带 → 背光）。
即「3D 卡面 + 工艺 + 卡背等级化」这套在 2026-09-21 就落地过，剩下的只是 `disp/relief` 那一档（见上）。

## 等级层落地（2026-09-24 夜，拆三块做，未提交）

**已推**：3D 层（展示台/分层浮起/层离按钮，判据单一副本 `card-3d.js` 的 `layerRank()`）+ 管理台「收藏库」面板（`#deck`，明度体检与预演—落盘两步，规则唯一实现在 `scripts/deck-edit.py`）→ `4fd3260`；卡面工艺 16→8 → `8fd5256`。

**等级层半成品在工作区未提交**：`assets/css/decks/21-card-deck.css`（+542/−286）、`layouts/_partials/deck-wall.html`、`layouts/_partials/home-cards.html`。六档类与令牌体系已落（`.home-card-rank--*` 的 `--rank-frame/mat/band/band-a/line/craft/foil/orn/mark` 与 `--cframe`），`hugo` 与三条门禁全绿。

**1:1 目视（真站、卡宽 206px）结论（已复核，前一条判据曾错）**：
- 源码里的色带阶梯是 `.020/.031/.042/.053/.064`（相邻差 .011 ≈ 2.3px@206px，累计差约 9px），**已经比设计初稿（.020→.052）拉宽过**。第一次判读读到的 `.028/.036/.044/.052` 是**旧构建**的值（本地服务当时提供的是加宽前的产物），那条"宽度递进感知不到"**判的是旧版本、已作废**。重构建后复核：前五档色序成立、厚度阶梯有节奏，但相邻档仍主要靠色相区分。
- **真正待修的是融色**：收藏（灰融灰，最重）、秘藏（紫融深蓝）、史诗（蓝融蓝，轻微）、奇迹伪装态（白融白）。判读给的方案很具体：**给色带加 1px 内描边 —— 亮带配深描边（灰/白/金）、暗带配浅描边（紫/蓝）**，既保留色相识别又把色带从卡面里切出来。

**别把「奇迹像默认档」当缺陷**：`21-card-deck.css:1275` 起 `.home-card-rank--miracle:not(.is-revealed)` 与收藏档同款（`--rank-craft: .35`）—— 显形前伪装是设计，判读奇迹必须分「伪装态 / 显形态」。

**拆三块（不并行，三块都改同一个 CSS 文件）**：① 框 + 色带可读性（进行中）② 花纹 + 角花 ③ 卡背金轴五档 + 奇迹虹彩。整体当成一个任务发出去**连续卡死两回**（8 分钟无响应被取消），粒度不能再大。

**验收配方（别重犯）**：截图用 `python shots.py`（本机 `python` 是 Hermes 自带 venv，有 `websockets`；ml 环境的 python 没有，直接 ModuleNotFoundError）；`--full` 全页截图**前必须逐屏滚到底**，卡片图是 lazy 的，否则截到未加载的空白卡（已因此把判据弄错过一次）；卡片墙每卡 206×288 CSS px，本地服务在 `http://127.0.0.1:8831/my-blog/collection/`。
