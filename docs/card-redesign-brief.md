# 卡片重做：设计纲要（2026-09-23 逐项问定）

这份文档是**用户逐条拍过的决定**，动代码前先看它。三条轴：卡面工艺、卡框、3D。

## 一、风格主线（总纲）

**分层：低档真实印刷、高档幻想光效。** 低档要"看得懂"（像一张真印刷卡），高档要"有惊喜"（数字卡才有的光效）。
「正交」不等于低档也会有幻想光效 —— 光效强度由等级给：收藏档的流光/星屑/全息通道一律为 0，
往上逐级长出来。这条与现有 `RANK_3D` 的口径一致，继续沿用。

## 二、卡面工艺：8 种

**保留 4 种（光系）**：星芒全息、全息、烫银、星屑。
**新设计 4 种（印刷系）**：凸印压痕、珠光纸、烫金箔、墨染。

| 工艺 | 纹样 | 材质 | 与谁最容易混 → 靠什么拉开 |
|---|---|---|---|
| 星芒全息 | 几何星芒（六/八芒） | 彩虹衍射，**只在线内出彩虹** | vs 全息：有图案 / 无图案 |
| 全息 | 无图案 | 大面积彩虹箔，整体随角度变色 | vs 烫银：会彩虹 / 不彩虹 |
| 烫银 | 细斜纹（弱） | 镜面银，冷白高光，**不彩虹** | vs 烫金：冷银 / 暖金 |
| 星屑 | 大小不一的点 + 少量十字星光 | 深底上发亮的点状发光，**无面** | vs 星芒：点 / 几何线 |
| 凸印压痕 | 沿画面轮廓的压痕（无独立图案） | 纸白，**只改光不改色**，左上高光右下阴影 | 唯一的"零颜色"工艺 |
| 珠光纸 | 细密横向丝光 | 暖白珠光，随角度**微**变色，带纸纹颗粒 | vs 全息：弱一个量级、偏暖白 |
| 烫金箔 | 云纹/缠枝等传统纹样 | 暖黄金属，边缘锐利闪光，**不彩虹** | vs 烫银：暖 / 冷 |
| 墨染 | 水墨晕染（边缘羽化、浓淡不匀） | 冷灰黑、亚光、纸纹吸墨 | 唯一的"暗面"工艺 |

- **工艺与等级正交**：工艺管纹样与材质，等级管框与光效强度。同一种工艺在收藏档是"素面印刷"，在传世档才满力。
- **叠加：1 主 + 最多 1 副**；副工艺只出纹样层、不出材质与框；**只有传世与奇迹**能有副工艺。
- **墙上 205px 必须可辨**：同一副纹样、**两级强度** —— 卡小的时候按显示尺寸放大线宽与对比，弹层回归细腻。
  同一份参数、不维护两套图形。

## 三、卡框：全档同一副框

同宽、同角饰、同徽记位置，**几何完全一致**。等级只靠两处：

1. **材质色阶（金属系）**：纸白 → 赤铜 → 白银 → 黄金 → 黑金 → 幻彩。
2. **徽记**的分级：六档曲线的徽记沿用（圆 → 星形线 → 双纽线 → 内摆线 → 玫瑰线 → 蝴蝶）。

**物理卡边（左上受光/右下背光）与内衬线保留** —— 那是"这是一张实物卡"的两条关键线索。

## 四、3D 卡

四块全要，按贵贱分阶段：

1. **能转的真卡**（现有基础，保留）。
2. **工艺纹样真的做进 3D 表面**：前提**已满足**（2026-09-25 查清）。「高度图改了没反应」是旧实验的**方法错**：它把图案写进了 `public/` 里的构建产物，而页面服务自 hugo 开发服务器（从 `assets/` 现渲染），改的是一个没人服务的副本；同一个 0.055 格线图案写进源码后是卡面内 **5.99 / 14.65% 像素**（判读为「表面压出格状起伏」，无撕裂）。机制、读数与坑记在 `assets/js/card-3d.js` 里 `parallax()` 那段注释、`lab/README.md`「3D 卡效果的量测口径」与 `docs/pending.md`。**方向也已定**（2026-09-25 样张轮）：纹样要「有结点、有线宽变化、有边框」的规整格（掐丝格那一版判读最好），均匀细网格禁入（读成数字 moiré），高斯软脊读成「糊」；默认幅度 ≈0.11、上限 ≈0.15，且加法要留头寸防削顶。详表见 `docs/pending.md` 的「候选样张轮」。**已落管线**（2026-09-25）：`reliefPattern`（YAML 单一事实源）+ `tools/cards/make-depth.py` 的图案层（`--pattern auto|none|<kind>`，叠在滤波之后）—— **五档已上**：烫金箔/烫银（缠枝卷草 0.09）、珠光纸（帘纹 0.08）、凸印压痕（硬菱格 0.08）、星屑夜天（星点 0.07）—— 都经过 1:1 判读判「可定档」；**星芒全息与墨染先关掉**，但**已定案：这两档不做浮雕纹样**（不是「待改」）—— 病根是**与工艺本体的视觉语言冲突**：全息卡面本身就是斜向镭射光，任何线状浮雕都跟它打架（最后一轮判读原话「主芒太淡、和卡面斜线混在一起」），墨染卡面本体是水墨画，线状浮雕读成扫描线。三轮形态重做（等宽细线 → 锥形长芒/变径毛笔 → 更少更粗 → 砍光次要元素）都被 1:1 判读否掉。遮罩这条路查清了：**分割模型（face-parsing / segformer_b2_clothes）在这套插画卡上不合用**（域外过覆盖 / 漏检），真正的修法是**阈值化主体遮罩**（窄过渡，主体内部压到 6%），不需要新依赖。`foil`（默认兜底档）不配纹样。详表见 `docs/pending.md` 的「落地轮」。
3. **展示台**：卡悬浮 + 背景压暗 + 投影 + 景深。
4. **分层浮起**：
   - 基础（全部档位）：**2 层**（主体 + 背景），转到侧面/背面**自动收平** —— 保住实卡手感。
   - ~~**只有传世与奇迹**有触发方式：点一下进入"层离"（更多层、各自视差），并与**奇迹的显形合成一个动作**。~~
     **2026-09-25 已下掉（用户拍板「关掉整个层离」）**：四轮对照（6 条等高度分带 → +视角视差/接触阴影 →
     三层 → 四层刚性平移）之后，结论是这条**在 430px 显示尺寸上读不出"分层"**：等高度分带的边界就是等高线、
     平滑位移场会把画自带的细线纹样绞成满卡摩尔纹，而唯一干净的刚性平移版读感又太弱。
     移除面：`card-3d.js`（`RANK_3D.layer`、整段着色器、`layerOn/layerRank/layerOff/layerAvailable/`
     `layerState`、`stats().fx.layer*`）、`home-deck.js` 的按钮、`22-card-3d-stand.css` 的样式、
     `i18n` 的 `deckLayer/deckLayerBack`、两个模板的 `data-layer*`、`check-deck.mjs` 的通道表。
     过程与读数见 `docs/pending.md` 的层离那一行；探针留在 `lab/工具/layerab.py`。
     **同轮修掉一个老 bug**（与层离无关，保留）：色散那两行是整体赋值 `alb.r = …`，会把先前的乘性压暗吃掉，
     症状是"只有 G 通道被压、画面偏洋红"。
   - 纪律（仍然成立）：层越多越像"盒子里的景"，所以基础档只做 2 层。

## 五、预算与素材

- **体积预算：放宽到 20MB**（现状 gzip 10.0 / 上限 11.3；raw 26.6）。还要更高再申请。
- **弹层清晰度的天花板是源图**（中位原生宽 498px），不是档位：**用户去换源图**，规格 **≥1000px 宽原图**；
  设计层由我这边做。低分辨率清单见 `docs/pending.md` 那 12 张表。

## 六、节奏

**先出设计稿（纹样与卡框的静态样张）→ 用户拍 → 再动代码。**
样张要带**双尺寸对照**（205px 墙 / 弹层尺度），因为"墙上可辨"是硬要求。


## 八、样张 v1 的实测结论（2026-09-23）

样张：`lab/redesign/sheet1.html`（截图 `lab/结果/collreview/sheet1-full.png`），8 种工艺 × 两种尺寸 + 6 档框材质。

**成立的两条技术路线：**

1. **凸印压痕不能靠画面本身做**（用原画做掩膜会把人洗成白模）。正确做法是**由深度图算"高度场梯度"**：
   `lab/工具/bevelmap.py` —— 法线与左上方光源做 Lambert 得受光图、再做一次高次幂得脊线镜面图，
   两张灰图分别用 `soft-light`（受光，改光不改色）与 `screen`（镜面）叠在画面上。**画面颜色完整保留，压痕沿人物本身的起伏走**。
   代价：每张卡多两张小灰图（可按需求生成 webp，约 10~20KB/张）。
2. **烫金箔的金属感来自渐变本身，不要靠 mask**：把金渐变直接烘进云纹 SVG 的 stroke（`linearGradient` + 多个 stop 做明暗交替），
   再用一道斜向镜面 `screen` 扫过 —— 比"渐变层 + mask"稳得多。

**v1 暴露的问题（要修）：**

- **205px 下凸印压痕与珠光纸分不开** —— 两者都"保留原色 + 轻微纹理"。修法（走两级强度那条机制）：
  珠光在墙上**整体偏暖**（可见的暖白偏色），凸印在墙上**给主体加一层接触阴影/明暗包边**，让两者的"视觉签名"在小尺寸下就不同。
- 墨染在小尺寸下最可辨（黑团），烫金次之（暖金云纹）；光系 4 种待同样做"视觉签名"检查。


## 九、纹样路线 v2：CSS 矢量平铺 → 生成纹样板（2026-09-23）

用户反馈：**有图形的工艺（烫金箔 / 墨染 / 星芒全息 / 星屑）"太生硬"**。根子在两处：矢量线稿边缘硬、粗细一致；平铺 = 等距重复，一眼就是壁纸。

**改法：把纹样从 CSS 矢量平铺改成"生成出来的整张纹样板"**（`lab/工具/mkpatterns.py` → `lab/redesign/plates/*.webp`）：

- 每块板**只做一张、全库共用**（不随卡数增长，4 块板合计约 0.3~0.6 MB）；做成整张而不是 tile，从根上消灭接缝与等距重复。
- 生硬靠三件事治：**不规则**（位置/大小/旋转随机）、**化边**（羽化 + 分形噪声做轮廓与断续）、**密度不均**（成团分布，不是均匀撒）。
- 墨染用分形噪声当晕染、烫金用随机卷云曲线 + 箔的断续、星芒/星屑用稀疏随机 + 柔光。

A/B 判读结果（`lab/结果/collreview/sheet2-ab.jpg`）：**烫金箔、星芒全息、星屑三对 v2 都成立**（更自然、没糊、没盖住画面）；
磨金箔 v2 偏含蓄，可再提一点存在感。

**墨染 v2 仍不到位**（明显比 v1 好，但还是"数字噪点/罩层"，不是水墨）。下一版的修法（三条，按重要性）：

1. **用深度图做主体避让遮罩** —— 墨只落在背景区域，脸/手/裙摆保持干净。深度图本来就有（主体亮、背景暗），反相即可当遮罩。
2. **降颗粒**：现在 fbm 直接阈值化 → 盐缩噪点感；改成低频团块决定"墨在哪"、高频只负责轮廓的轻微扰动。
3. **水痕边**：用「膨胀 − 原图」做出一圈略深的水痕边，比现在的碎边更像纸上吸墨。


## 十、墨染定稿与两条通用设计规则（2026-09-23）

用户反馈"墨染还是有点脏"。查清了"脏"的三个来源，逐个消掉：

1. **高频阈值化的分形噪声 = 盐缩颗粒** → 改成低频团块定浓淡、高频只做轻微扰动。
2. **模糊填充出来的软边 = 灰雾罩层** → 改成「沿路径盖章」的笔触（`bez` + `brush`：粗细随行笔起伏、两端收尖、
   干笔处断开），收尾只糊 1.3px，浓心用 `(w-0.05)*1.75` 快切到纯黑。
3. **暗墨压在白裙上，一定读成脏** —— 这条是最关键的：墨只走灰底背景，一笔都不压白裙。

**两条通用规则（对 8 种工艺都适用）：**

- **暗部元素不要压在高调主体上**：白裙/白发上任何深色都读成污渍。工艺的深色成分要有"避让"或只落在中间调的背景上。
- **两级强度不只是"放大对比"，而是"减元素 + 加粗主线 + 抹掉细颗粒"**：细线和细颗粒在 205px 下会变成噪点，
  不是"更精致"。墙上版要**删掉最细的那一笔**（墨染的右上清笔在小尺寸会像划痕），主笔放大 15~20% 并轻度模糊。

**当前状态**：弹层尺度版**可定稿**；205px 墙上版按上面的适配也读得出来（`lab/结果/collreview/ink-final.jpg`、`ink-wall-adapt.jpg`）。


## 十一、光系四工艺与「靠材质分级」的实测结论（2026-09-23）

**一、光系四工艺在 205px 上还没解决（烫银最急）**

- **烫银过曝**：白裙白发被银光吃掉。根因是我把银写成了"白光带"。银是有暗面的金属 ——
  改成**中灰金属体（multiply）+ 窄亮边（screen，宽度 ~12%）+ 一道暗侧**，并加冷灰反射。已改，仍需再验。
- **星屑**：密集小点在 205px 下就是灰尘/噪点（细颗粒→噪点的规则再次应验）。已改成"少而大的火花 + 十字长臂"，
  大尺寸下成立，小尺寸仍偏弱，需要再加大少元素的比例。
- **星芒全息**：硬边白星在小尺寸读成"贴上去的贴纸"。修法：保留硬边、降不透明度、加虹彩边、允许被人物的光晕吃掉一部分。
- **全息**：偏白偏平，把人物的明暗压掉了 —— 要降强度，把体积感还给画面。

**二、「靠材质分级」在小尺寸不成立（这条最重要）**

实测：六档材质在 205px 下只能读出"不同"，读不出"高低"。亮度序列是 **高-低-高-中-低-高（纸白/赤铜/白银/黄金/黑金/幻彩）**，
不是单调的；色相冷暖交替。结论：**材质色阶必须重排成单调明度 + 递增彩度/光泽**，而且等级必须有**第二个在小尺寸看得见的通道**。

两条修法：

1. **材质色阶重排（单调明度 + 递增彩度）**：纸白(最亮、无光泽) → 珠银(次亮、柔光泽) → 黄金(中、金属) →
   赤铜(较暗、深金属) → 黑金(最暗 + 金线) → **幻彩（黑底幻彩流光：明度仍低、彩度最高）**。
   幻彩做成"暗底流光"而不是"亮彩虹"，这样六档的明度仍然是单调下降的。
2. **徽记必须在 205px 上看得见** —— 用户原本的方案里就有徽记（六档曲线：圆/星形线/双纽线/内摆线/玫瑰线/蝴蝶），
   但它要成为**小尺寸下真正承担分级的那一个通道**：够大、够对比、位置固定。样张 v1 根本没画徽记，这是缺口。


## 十二、色阶重排的实测：问题不在色阶，在**带宽**（2026-09-23）

重排后的六档（素纸→珠光→烫银→烫金→黑金→幻彩，明度相邻差 ≥10）在 205px 上实测**仍然排不出高低**。
原因不是我排得不够开：**205px 时框带只有约 7 个物理像素宽，一条 7px 的细带承载不了"材质色阶"** ——
相邻两档差 10 个明度值，落在 7px 宽的边上，人眼读不出来；反而"饱和度"会跳出来干扰判断。

**结论（重要）：靠材质色阶分级，在墙尺寸上不可行 —— 这是面积问题，不是配色问题。**
等级必须在墙上换到一个**有面积**的通道。四条候选：

| 通道 | 做法 | 代价 |
|---|---|---|
| A 框内缘等级色带 | 内地缘一圈 4~6px 的等级色带（整圈，像集换卡的 rarity strip） | 改框结构，但面积足够、规整、最耐读 |
| B 徽记单独扛 | 徽记放大到卡面 1/5 宽 + 金属压印感 + 固定角标 | 不动框；徽记要重画 |
| C 框整体加厚 | 框加厚到 6~8px，靠材质读档 | 改变卡片比例观感 |
| D 卡面底衬色调 | 低档补浅色衬、高档压暗角（大面积） | 会动到画面上的一切工艺 |

**徽记实测**：205px 下深色底托只有十多像素，圆/三尖/四尖/五尖/六瓣全被像素吃掉，读成"黑点/污渍"。
所以徽记若要在墙上承担分级，必须**放大 + 高对比**（或做成金属压印那种"实心徽记"而不是细线轮廓）。

**星屑 → 星闪（重做中）**：已从"点阵"改成"四芒星闪（亮核 + 光臂渐隐）"，弹层尺度不再像灰尘；
但"串珠"的轨迹还没连起来（星点仍偏散），下一步补连续轨迹/拖尾与大小节奏。


## 十三、等级通道定了：一条色阶梯，两处同源（2026-09-23 实测通过）

**用户拍板：墙上用「框内缘一圈等级色带」承担等级信号，材质色阶保留（当弹层里的材质语言）。**

**关键教训（踩过两次）**：

1. 先试"材质色阶承担分级" → 205px 上框带只有 ~7px，承载不了，失败（见 §十二）。
2. 再试"材质色阶 + 另一套颜色的色带" → **两套色阶梯互相打架**，判读直接指出"看框和看色带得到的顺序不一致"，
   比只有一套还差。

**成立的做法：一条色阶梯，两处同源。**

- 色阶梯：**灰纸 → 青玉 → 蓝钢 → 紫金 → 黄金 → 幻彩**（色相逐档推进、彩度与"贵重感"递增）。
- **框材质 = 该档色的金属版**；**框内缘色带 = 该档色的纯色版**；两处同一个色，谁也不抢谁。
- **色带宽度随档递增**（2.4% → 5.4% 卡宽）：面积本身成为第二个单调通道。
- 素材：`lab/redesign/sheet1.html`（自包含样张），实拍 `lab/结果/collreview/v2-rank3.jpg`。

判读结论：**能一眼排出高低，双色冲突已解决，方案基本成立。** 顺手改掉的四条精修：蓝/紫拉开（蓝提亮、紫压深）、
黄金提纯（亮金 #f0cf76 + 深金 #a8760f，去掉土黄）、幻彩固定暗底（虹彩只做流光，避免像 RGB 故障／花屏）、
色带宽度递增从 3.0–4.2% 拉到 2.4–5.4%。

**还欠的两件（下一步）**：

1. **非颜色的冗余编码**：徽记要放大、形状差异要更大（色盲与小图都要能分档）—— 现在徽记在 205px 下仍偏小。
2. **全档多尺寸总校验**：六档 × 双尺寸 × 全部 8 种工艺混在同一条墙上，看整体是否和谐、有没有互相抢。


## 十四、边框花纹定案：单色压印「双细线」（2026-09-23 实测）

四个候选（素带 / 双细线 / 珠链 / 回纹）在同一档「秘藏·黄金」下双尺寸实测：

| 候选 | 205px | 弹层 | 是否抢等级色带 | 结论 |
|---|---|---|---|---|
| 素带 | 干净 | 干净 | 不抢 | 保底方案 |
| **双细线** | 仍读得出"内框"结构 | 精致、有层次 | 基本不抢 | **定案** |
| 珠链 | 珠子变小、读成断续噪点 | 有珠宝感 | 小尺寸略抢 | 否 |
| 回纹 | **糊成花边/噪带** | 华丽 | 最抢 | 否 |

**结论：边框花纹用「单色压印的双细线」**，与等级色带同色系、低对比、只做收边 —— 不新增色相，所以不构成第二个"等级声音"。
细密花纹（回纹、卷草、珠链）在 205px 一律变噪点，**要华丽只能放在弹层的第二档强度里**。这是"细密高频在小尺寸=噪点"这条规则的第三次应验。

## 十五、混排墙首过暴露的两件事（2026-09-23）

混排墙（8 工艺 × 6 档，205px）整体和谐度只给到 **4/10**，暴露两件：

1. **墨染在浅色卡面 + 205px 下读成污痕**（判读原话："像渲染错误、污痕、没擦干净的笔刷"）。
   由此得到一条新规则：**工艺要与卡面适配** —— 深色成分的工艺（墨染、黑金类）只适合**中低调**的卡面，
   浅色卡面上一律读成脏。这条单卡样张看不出来，必须有**工艺 × 卡面适配表**。
2. **同档相邻会出现"重复感"**：8 格排 6 档，必然有两档重复出现，重复的档挨在一起时就显得"没秩序"。
   这是**排布问题**（实际站点 57 张随机分布），但说明墙上要考虑**等级在排布上的分散**，不能让它扎堆。

**顺带**：徽记放到卡面 10.5% 仍然偏小（205px 下形状还是认不准），下一步要改"实心压印 + 高对比"，而不是继续放大细线轮廓。


## 十六、工艺 × 卡面适配表（2026-09-23 实测，205px）

按真实卡面明度挑三张（暗 37 / 中 197 / 亮 230），与 6 种工艺交叉排，205px 上逐格判读：

| 工艺 | 亮卡面 230 | 中 197 | 暗 37 | 适配 |
|---|---|---|---|---|
| 墨染 | **脏**（像漏墨） | **脏**（像咖啡渍） | **优秀**（像烟雾/云层） | **只适合暗卡** |
| 星闪 | 糊（白点在亮底显不出） | 糊（融成噪点） | 优秀（星光璀璨） | **只适合暗卡** |
| 烫金 | 正常 | 正常 | 最佳（黑底金字） | 暗卡主场，亮中可用 |
| 全息 | 略脏 | 正常 | 最佳 | 暗卡主场，亮中可用 |
| 凸印 | 好（光影清楚） | 好 | 偏平（缺对比） | **通用**，亮中更好 |
| 珠光 | 好（通透） | 正常 | **脏**（像划痕/水渍） | **只适合亮/中卡** |

**一句话：亮卡面配"微光与物理压印"（凸印、珠光）；暗卡面配"强对比与悬浮特效"（墨染、星闪、烫金、全息）。**
乱配在 205px 上一定"脏 / 糊 / 像坏了" —— 这也解释了混排墙里"墨染像污痕"那件事：不是墨染坏了，是**配错了卡面**。

落地含义（要动清单结构）：`data/home-cards.yaml` 每张卡要么按明度自动过滤可选工艺，要么在清单里显式声明**这张卡允许哪种工艺**；
`check-deck.mjs` 要新增一条守卫（工艺 × 明度适配）。**这条会在代码阶段改到清单与校验脚本，必须在设计阶段先定。**

## 十七、徽记：实心金属压印（已改）

六枚改成实心剪影（圆盘 / 三角 / 菱形 / 五角星 / 六瓣花 / 蝴蝶），同色系激光渐变 + 左上高光、右下暗边，卡面 12.5%。

实测：**前四枚在 205px 稳定可辨、读得出"金属压印钢印"**；六瓣花缩小后接近糊、**蝴蝶对比太低会融进背景**
（奇迹档 `--emb` 用了过淡的 #e8dcff）—— 下一步把奇迹档徽记改成高对比色或加深轮廓。

**弹层第二档花纹**（回纹/珠链/卷草）已写进样张样式：`.card.motif-rich:not(.sm)` —— 细纹只在 420px 以上出现，**墙上永不出现**。尚未实拍验证。


## 十八、工艺 × 明度：规则草案与现有 57 张体检（2026-09-23）

**规则（由实测表推出，三档）**：

| 卡面明度 | 允许的工艺 |
|---|---|
| 亮 ≥185 | 凸印 / 珠光 / 烫银 / 烫金 / 全息 |
| 中 140~185 | 上面的 + 星芒全息 |
| 暗 <140 | 凸印 / 烫金 / 全息 / 星芒全息 / 星屑 / 墨染 |

明度按**卡面图的灰度均值**量（脚本 `lab/工具/craftfit.py`，缩到 60×84 再取均值，稳定且快）。

**现有 57 张体检结果**：明度分布 亮 32 / 中 12 / 暗 13；**违规只有 2 张** ——
「夜思」（中 146）与「观星」（中 180）现在挂的是「星屑」，而星屑只适合暗卡。**换掉这两张的工艺即可，不用动清单结构。**

**落地口径（代码阶段照这个做）**：

1. 明度由构建脚本现算（`make-cards.py` 顺手输出，或 `craftfit.py` 同款逻辑进 `check-deck.mjs`），
   **不写死进清单** —— 换图后自动重算，避免清单与图不同步。
2. `check-deck.mjs` 加一条守卫：每张卡的工艺必须落在其明度档的允许集合里，违反即失败并打出卡名与明度。
3. 若某张卡的艺术意图要求"破例"（例如暗卡偏要珠光），在清单里加 `craft_exempt: true` 显式豁免，守卫放行但要能 grep 到。

**明细**：`lab/结果/collreview/craft-fit.md`、`craft-fit.json`。

## 十九、徽记与弹层花纹的最终口径（2026-09-23）

- **徽记不再追求在 205px 上承担分级**。实测：六枚实心剪影里 圆盘/三角/菱形/五角星 稳定，
  六瓣花接近糊、蝴蝶即使换高对比色也仍是"一团色斑"。**结论：等级由色带（已验）+ 墙上已有的文字标签承担，
  徽记是"弹层里的做工细节 + 墙上的弱冗余"**，不再为它加复杂度。
- **弹层第二档花纹**：420px 实拍验证 —— 珠链、回纹都能看清，**回纹更稳更清晰**；两者都不抢等级色带，
  前提是**同色系、低对比、别太密**（传世黄金本身高饱和，配高亮珠链会显花）。墙上仍只用「双细线」。


## 二十、定稿样张 v2 与「定稿预演」（2026-09-23）

**样张**：`lab/redesign/sheet2-final.html`（自包含，截图 `lab/结果/collreview/final-full.png`）。四节：

1. 等级通道：六档 × 双尺寸（205 / 420）。
2. 八种工艺 × 双尺寸（统一挂「史诗·蓝钢」档，避免等级色干扰工艺判读）。
3. 边框花纹：墙上「双细线」、弹层「回纹」。
4. **定稿预演**：12 张真实卡面，**按明度规则配工艺、等级打散排布**（205px）。

**预演判读：和谐度 6/10（上一版混排墙 4/10）** —— 进步来自"尺寸统一 + 标签统一 + 边框色按等级系统化 + 按明度分组的意识"。
没有一格读成坏图。剩下两点：

1. **整墙节奏**：明度会扎堆（左半全暗、右半全亮）—— 这是我的取样方式造成的（按明度排序后直接排），
   真实站点按清单顺序，但仍说明**排布时要考虑明暗节奏**，不能顺其自然。
2. **顶档的"尊贵感"**：判读指出"奇迹用黑框未必像最高级"。这是**有意的反转**（色阶梯单调下降到黑金/幻彩，
   跟"越高级越亮"的直觉相反，用"暗底 + 幻彩流光"表达"超越金属"）。若用户不认这个反转，可以改成
   "顶档既最亮又最花"（但会破坏明度单调性）。

**设计阶段的收口条件**：用户拍过顶档反转 + 预演节奏，即可进入代码阶段。


## 二十一、顶档反转被否，改成「亮彩炫流」（用户 2026-09-23 拍板）

用户选：**奇迹既最亮又最花**，接受"六档明度不再单调"。落地：奇迹 `m0/m1 = #ffffff / #f6f0ff`，
外圈彩虹流光（conic 高不透明度），加**内圈一层深色细描边**（`rgba(58,22,84,.55)`）压住亮部，免得糊成一片。

**等级排序改为靠三条并行通道**（明度不再承担）：① 色相序列（灰→青玉→蓝钢→紫→金→虹彩）
② 色带宽度递增（2.4% → 5.4% 卡宽，仍单调）③ 徽记计数阶梯 + 墙上文字标签。

**同轮判读的三处修正（已改，待用户终判）**：
- 传世改「暗金/红金」（`#e0b45c → #8a4a08`，降明度提饱和）—— 原亮金被判为"氧化/旧铜"。
- 秘藏紫提亮（`#6a3cb0 → #7d4bd0`）—— 防在暗光下与史诗蓝混。
- 收藏档加**细网格纸纹**（`.m-strip-light .mat::after`）—— 原判"像没渲染/占位图"。


## 二十二、边框花纹「复杂版」与等级标「去几何化」（2026-09-23）

### 花纹：L 型边框砖 + 四角角花 + 压印对偶
做法：同一条纹样在**相邻两边各画一遍**（L 型砖），平铺即得整圈连续纹样；线一律**压印对偶**（暗线 + 反向错位亮线，`translate(±0.75)`）→ 有雕琢感而不是平涂。四角另加角花（回纹方旋，四层 `no-repeat` 定角）。

四个候选实测（205 / 420 同一纹样两个尺度）：

| 候选 | 205px 墙 | 420px 弹层 |
|---|---|---|
| 双细线 | 最清楚，但不算"花纹" | 太素，像普通简约金框 |
| 回纹 | 偏密，读成花边噪带 | 硬朗、偏传统 |
| 卷草 | **糊成噪带**（细碎卷曲） | **最像精装卡/证书烫印** |
| **如意云纹** | **块面大、最抗缩**，仍读得出纹样 | 端庄、偏中式国风 |

**定案：墙上用如意云纹，弹层用卷草**。角花在 205px 只能看到"四角有装饰"的轮廓，读不出纹样 —— 这一点不能再要求更多。

### 等级标：统一底场 + 负形镂空（v3）
v1（几何图形）、v2（六枚各自设计的剪影）都被判"像六个各画各的小图标，不是纹章家族"。v3 的改法：

- **统一底场**：六档共用同一枚圆角盾（占满视图 92%）+ 内圈 0.80 缩放的家族花边；
- **图案全部改负形镂空**（`fill-rule=evenodd`，图案是"洞"不是"贴上去的形"）→ 家族感与对比度一起解决；
- **外形差异靠负形的质量分布拉开**：竖向 Y / 空三角 / 鸢尾 / 十字 / 星 / 横向翼；
- **两级强度**：墙上是核，弹层加镂空花边（判读同意的做法）。

**客观判据（黑白剪影测试，全填黑）**：32px、24px 六枚一眼分清；**20px 临界；16px 不分**。标在墙上的实际尺寸约 33px → **通过**。

判读结论：v3 相对 v2 是**方向正确的进步**（家族感、系统感明显增强），代价是个别符号辨识度下降。三条对比度修正已改（统一深色外轮廓、收藏档底场压深、奇迹档粉提饱和），**未复验**。


## 二十三、收尾三件的复验结果（2026-09-23，含一条翻案）

① **鸢尾已简化**（改成三叉矛形，硬折角、负空间大）—— 剪影测试与墙上表现待重测。
② **复杂花纹砖已接进定稿样张**：墙上 `t-ruyi`（如意云纹）、弹层 `t-vine`（卷草），四角角花统一；等级标已换成 v3 的 `.mark.m1..m6`。
③ **复验翻案（重要）**：上一轮判读说"如意云纹在 205px 最抗缩"，这一轮在定稿样张上复验却判**"不成立 —— 读成虚线或小点，沦成纯肌理，读不出云纹"**。

两次判读结论冲突 → 按**保守口径**定：**墙上不做"可读的纹样"，只保留两条"能看见的结构"**：
- **色带中央一条高光细线**（肌理级，不指望读出母题）；
- **四角角花只留加粗的几何轮廓**（L 角），复杂角花留给弹层。

**复杂纹样（卷草/回纹/如意云头）一律只在 420px 弹层出现**。这条与"细密高频在小尺寸＝噪点"完全一致，落地也更便宜。

**另两处待改（判读点名）**：
- **收藏档（第 1 档）**：白框 + 浅卡 + 浅底 → 对比极低，判"像没擦干净的噪点/抠图毛边"，需加**极细暗色内描边**。
- **奇迹档（第 6 档）**：虹彩渐变在 205px 出**色差污染（转角脏边）**，建议改**双色渐变 + 金属镶边**（或高亮粉 + 银边）替代六色 conic。

**等级标的定位正式确认**：墙上它是**弱冗余/水印级**，不再加码；弹层（420）才是它承担"做工细节"的地方。


## 二十四、花纹「自然化」与等级标尺寸（2026-09-23 实测定案）

### 自然化的三个来源（不靠画细）
① **笔势收锋**：图案用**填充带**（起笔粗、收笔尖）而不是等宽描边；② **不等间距**：叶芽落点不规则、大小/角度各异；③ **非对称砖**：一块砖内左右不镜像 → 平铺出 period-2 节奏。

四候选实测（205 / 420）：

| 候选 | 205px | 420px | 自然度 |
|---|---|---|---|
| n-vine 藤蔓 | 有结构但机械 | 一眼看出平铺单元 | **最低** |
| n-branch 细枝 | **接近噪点** | 最像手绘枝芽 | 最高 |
| **n-ink 墨线** | **结构最清楚** | **最像手工压箔（高级）** | 高 |
| n-soft 柔云 | 糊成均匀亮带 | 柔但"平"，无笔势 | 中 |

**定案：墙上 n-ink（少而粗、高对比），弹层用同族加密版（更长更多笔触）。** 与"同款简单缩放"相比判读明确推荐**分尺寸独立调整**。

### 等级标尺寸（判读给出识别下限）
墙上 **12%（≈25px）**、弹层 **16%（≈67px）**；判读原话："约 20–25px 是徽章识别下限，再小就退化成小点"。9%（18px）已糊。

### 同轮实测的遗留项（未改）
**色带宽度差在 205px 区分度不足** —— 判读建议把六档宽度差**再拉大 10–15%**（现 2.4% → 5.4%）。这是当前唯一还开着的一处微调。


## 二十五、角花自然化（2026-09-23 实测定案）

用户反馈「卡框四个角的图案太生硬」。原角花是**方旋回纹**：直角、等宽、四角同一朝向 —— 三样都硬。

四个候选（卷草角 / 抽芽角 / 墨扫角 / 圆化方旋），共同改法：**填充带收锋 + 非对称（主枝 + 大小不等叶）+ 每角各自旋转 90° 朝内生长**（原来是同一张砖同朝向硬贴）。

| 候选 | 判读 |
|---|---|
| **c-scroll 卷草角** | **定案** —— "自然感、清洁度、可读性最平衡，明显软化了几何感" |
| c-bud 抽芽角 | 太细，420px 下像轻微划痕 |
| c-ink 墨扫角 | 毛糙边缘，易显脏/破损 |
| c-rounded 圆化方旋 | 干净但本质仍几何、机械 |

**已接进定稿样张**（`orn4 c-scroll`）。复验遗留：**浅色档（收藏/奇迹）的角花线在浅底上读成灰渍/掉色** —— 已加 `.card.m-strip-light .orn4 { filter:brightness(.74) contrast(1.4) }`，**未复验**。

**判读同轮再次点名色带宽度差**：六档宽度差在 205px 区分度不足，建议再拉大 10–15%（现 2.4% → 5.4%）。仍是唯一开着的微调项。


## 二十六、等级标移到卡背 + 色带宽度差拉大（2026-09-23 用户拍板）

### 等级标挪到卡背
正面去掉 `.mark`（正面已够忙：色带 + 花纹 + 角花），等级标改住卡背：**该档材质底 + 内衬框 + 居中等级标 + 底部档位文字**。定稿样张新增「卡背六档」两段（205 / 420）。

**同轮修掉两个 bug**：
1. **奇迹档的 `--strip` 是 `conic-gradient`，不能当颜色塞进 `linear-gradient`** → 整条 background 失效 → 徽章彻底不可见。修法：给该档单独写实色渐变（并把锚点改暗，白底上才看得见）。
2. **白底 + 白字 + 白标 = 看不见**：浅色档（收藏/奇迹）的档位文字改深色（`rgba(42,32,14,.78)` + 白描边阴影）。

### 色带宽度差拉大
`0.024 / 0.030 / 0.036 / 0.042 / 0.048 / 0.054` → **`0.020 / 0.028 / 0.036 / 0.044 / 0.052 / 0.060`**（相邻差 0.006 → 0.008，**+33%**；总幅 2.25x → **3.0x**），满足判读要求的"再拉大 10–15%"。

### 复验（卡背，判读原话）
- 前五档色相阶梯清楚；**奇迹卡背"大面积白底，像未加载完的占位卡背"**，收藏也偏空。
- **收藏的等级标在浅灰底上对比偏弱**（"能看见但不醒目"）。
- **卡背要算完整还差**：等级数字/星数、背景暗纹、卡名与编号位、浅色档对比增强、六档光效逐级增强、205/420 两尺寸的留白规则。

**未复验**：色带宽度拉大后的正面表现（改完未再拍读）。


## 二十七、卡背定案方向：统一黑底反光背（用户拍板）

用户定：**卡背沿用原来那套统一黑色反光卡背（可升级），六档只有「等级标 + 名字」不同** ——
上一版"按档变色卡背"作废。

落地（沿用站点原配色 `--cardback-bg1 #33384a / bg2 #1b1e2b / accent #d9b673`）：
统一黑底渐变 + **一道斜向反光** + **金色内衬细线**（`rgba(217,182,115,.42)`）+ 暗纹（复用 n-ink 砖，`opacity .22`）
+ 居中等级标（亮金属渐变 `#fff → #d9b673 → #6b5a2e`）+ 底部档位名字（白字 + 金字下线）。

**复验（判读原话）**："统一感够，很像同一副卡背 —— 底板、反光、暗纹、金内衬、护角、排版全部共用，唯一变量是标和名字。"
同轮点名的升级项（**已改，未复验**）：暗纹降对比（.30 → .22）、名字加大加粗（0.070 → 0.082, 700）、**等级标加外发光**
（原"收藏"的灰银标在暗底上对比最弱）。

**仍开着的升级项**：第二识别通道（角饰/下线颜色随档微调、星级或罗马数字）、卡背的旋转对称性（现为上下定向）。


## 二十八、卡背复验：统一成立，但"只差标与名字"分档偏慢（2026-09-23）

**已改的三处（暗纹降淡 / 名字加大加粗 / 等级标统一亮金属金 + 外发光）复验结果**：
统一感"够，甚至偏强"；黑底上的标"够醒目"（收藏档也不糊）；名字"够清楚"。
但 **"只差标 + 名字"导致六档"能分清、不够快"** —— 小尺寸下六张先被看成同一张卡。

**判读给的升级方向（不破坏黑底统一，只做"微差"）**：
- **同一金色的强度阶梯**：外发光强度 / 金色明度随档递增（低档哑金 → 顶档亮金）；
- **盾底座形状微调**、内衬线颜色或粗细随档变化、名字色温微调；
- **顶档加流光**（动态），低档保持静态。

即：**卡背与正面共享同一条"强度阶梯"逻辑**（正面是色带宽度与色相，背面是金的强度），而不是给背面再加一套颜色。


## 二十九、卡背金色阶梯（2026-09-23 实测定案）

用户定：**卡背统一黑底反光，六档只差等级标与名字（可升级）**。升级做法**不加第二套颜色**，而是四条单调通道并行：

| 通道 | 收藏 | 珍稀 | 史诗 | 秘藏 | 传世 | 奇迹 |
|---|---|---|---|---|---|---|
| **底暗色调**（呼应正面色相） | 素黑 #262a38 | 墨绿黑 #16302a | 蓝黑 #1b2440 | 紫黑 #2a1c42 | 金黑 #33280f | 虹黑 #2a1638 |
| **金量累加**（图标/内框/文字/角饰） | 全中性银灰 | +图标 | +内框 | +文字 | +角饰 | 全金 + 虹彩 + 流光 |
| **底纹密度** | 10% | 14% | 18% | 22% | 26% | 32% |
| **外发光强度** | 0 | .15 | .30 | .50 | .75 | 1.0 |

落地方式：`--gold / --glowk / --glw / --backtint / --dens / --cl / --cn / --corngold` 一组内联变量（`sheet_final.py` 的 `GOLDS/BACKTINT/DENS/LINE/NAME` 表是唯一事实源），卡背 CSS 全部读变量。
顶档额外一道 `backflow` 流动光（**动而非色**）。

**复验（判读原话）**："**能排出高低** —— 收藏<珍稀<史诗<秘藏<传世<奇迹"；收藏→奇迹"**是质变**"；原先最弱的"史诗→秘藏"**基本补上**（文字转金 + 紫黑底 + 底纹更密三者叠加）。

**仍开着的唯一弱环**：「**珍稀→史诗**」—— 该档新增的只有"内框金"，细线在 205px 暗底上最容易消失。改法（判读给）：内框加粗/双层/内发光；或史诗图标升级为"星 + 外圈金环"；或底纹密度在史诗这一步明显跳一档；或把史诗蓝黑调得更冷，与珍稀墨绿黑拉开。


## 三十、补弱环 + 等级标全息质感（2026-09-23）

### 补弱环（珍稀→史诗）
初版让「史诗起暗纹整体转金」（大面积锚点）→ 判读："**基本分开了**"，但同时警告"史诗提前吃掉金色，会把弱环推到史诗→传世"。
**已按判读把大面积转金后移到传世**（`PAT = [0,0,0,0,1,1]`），史诗只留小面积金（图标 + 内框 + 上金下划线）；
底纹密度同步改成**在史诗跳一档**：`0.10 / 0.13 / 0.20 / 0.26 / 0.31 / 0.37`。

### 等级标全息质感
做法：`--holo` 作为**第 5 条单调通道** `[0, .12, .26, .42, .62, 1.0]` ——
金属底（`--gold`）+ 虹彩层（conic，`mix-blend-mode:screen`，强度随档）+ **衍射细颗粒**（`repeating-conic` 6°/12°）+ **旋转扫光**（`holosweep`，随虹彩强度）+ 边缘高光（`inset 0 0 0 1px rgba(255,255,255,.35)`）。

**复验（判读原话）**："**有金属+虹彩感，比纯色标高级，但不太像真正的全息箔**" —— 还缺：更明显的彩虹相位偏移、可见的细颗粒对比、更亮的边缘高光、微凸起；205px 下这些容易被压掉。

### 仍开着的项（判读点名）
1. **六档同屏验证未做**（当前只确认了可见四档的相邻区分度，"没有分不开的相邻对"）；
2. **秘藏的金字可能抢传世的风头** —— 需确保传世的**金色面积/亮度/质感**明显高于秘藏（累加式阶梯的面积必须是单调的）；
3. 色盲与小尺寸的非颜色区分（图标形状/角饰/纹样）仍需补强；
4. 全息颗粒在 205px 的实际可见度需 100% 缩放与不同背景下测。


## 三十一、奇迹 = 轴外特殊档（用户 2026-09-24 拍板，等级语言定案）

**问题**：判读两次咬住"金色峰值在传世，到奇迹断崖式减少"→ 传世/奇迹视觉倒挂。这不是参数问题，是定义问题。

**用户拍板**：**奇迹是「平行特殊档」，与金量无关，需要另给一套"最高档"的视觉理由。**

**于是等级语言分成两条**：
- **金轴（收藏 → 传世，五档）**：金量**严格递增** —— 收藏全中性银灰 → 珍稀 +图标金 → 史诗 +内框金 +底纹跳档 → 秘藏 +角饰金 → **传世 = 金轴顶点**（+文字金 +暗纹整体转金）。传世"最金"从此是**正确**的，不是倒挂。
- **轴外（奇迹）**：不参与金量递增，整套走**虹彩** —— 大面积虹彩底（conic 叠底）+ 虹彩内框（白边 + 紫光）+ **虹彩标**（不再用金）+ 白字 + 虹彩下划线 + 流动光。定位＝"金之上"，而非"更多金"。

**正面同构**：正面同样是"色带宽度与色相（五档阶梯）+ 奇迹的亮彩炫流（轴外）"。

**样张**：`lab/redesign/sheet2-final.html`（唯一事实源：`sheet_final.py` 的 `GOLDS / BACKTINT / DENS / LINE / NAME / PAT / HOLO` 七张表）。

**状态**：本条改动**已落地未复验**（定稿前需再走一轮视觉判读）。


## 三十二、奇迹虹彩加强（用户指令）—— 判读结论：可定稿（2026-09-24）

**改四处**：
1. **前框深底对彩**：淡底 → 深紫黑底 + 高饱和虹彩 conic（`saturate(1.55) brightness(1.12)`）—— 彩虹边靠**明度差**撑住，205px 才不退成淡紫；
2. **背底加深**：淡紫 → `#2a1030 / #12060f / #241026` 深紫黑，虹彩叠层提到 .34 级，内框改 `border-image` 彩虹 + 外发光；
3. **专属图标**：**六角星 + 四道星芒 + 星尘**（不再是共用盾徽）+ 虹彩填充 + 强发光；**正面四角改星徽**（不再是共用卷草角）→ 正反面共享同一套专属符号；
4. **专属 205 LOD**：墙上换暗衬底、关扫光、图标换简化版（六角星放大 + 外环）、四角星徽放大到 15.5%。

**复验（判读原话）**：
- 420px："**没有倒挂** —— 奇迹的视觉语言完全跳出金属递增轴"；
- 205px："**无硬倒挂**，彩虹边与中央六角星留得住；四角星徽放大后**认得出来、基本不像噪点**"；
- "**奇迹现在算特殊档了**"；
- **结论："当前版本可定稿"**。

**唯一优化项（非阻塞）**：205px 的正面四角星徽与浅色背景对比再抬一点（暗描边或轻微外发光）。

**验收页**：`lab/redesign/verify.html`（只放六档正/背两行，页高＝两行，**不再需要裁图** —— 这条是修掉"猜裁图位置"的失败方法后得到的可用验收物）。

## 三十三、验收口径（照现有纪律）


- 单旋钮对照：任何"强度/高度/锐度"类参数，先证明指标对它**有响应**再批量调（反面教材见 `blog-audit` §19）。
- 视觉判读不能定罪：重复/瑕疵类结论必须做**同尺寸对拍**。
- 每个阶段落地后跑 `node scripts/check-deck.mjs` + `bash scripts/push-blog.sh`（含体积预算、链接、拆包校验）。


## 三十四、色带阶梯得拿「框深」换 + 色带必须带两条深色描边（2026-09-24 晚，1:1 实测定案）

判读点名两条：① **色带宽度递进感知不到**；② **色带与卡面融色**（灰融灰 / 蓝融蓝 / 白融白）。

**三条候选在同一张 206px 1:1 六档接触表 + 左边缘水平扫描线上对拍**（判读原话）：

| 做法 | 相邻差@206px | 判读 |
|---|---|---|
| 现状（框深 0.060w，阶梯 0.020–0.060，无色带描边） | 1.6px | 「不能一眼看出逐档递增，相邻档宽度差只有约 1px，需要仔细并排比对或测量才明显」 |
| 只加两条深色描边（框深与阶梯不动） | 1.6px | 同样排不出来 —— 描边治好了 ②，救不了 ① |
| **框深 0.075w + 阶梯 0.020–0.075 + 两条描边（采用）** | **2.3px** | 「能把六档正确排序」；最难分的是 rare–epic，legend–miracle 次之 |
| 框深 0.080w + 阶梯 0.020–0.080 + 两条描边 | 2.5px | 阶梯最清楚，但整墙对拍被判「边框占面积明显增大…像厚相框」，画面感被削弱 |

**定案**：框深 **0.060w → 0.075w**（画面内缩 12.3 → 15.4px@206px），色带阶梯
**0.020 / 0.031 / 0.042 / 0.053 / 0.064 / 0.075**（相邻差 0.011 卡宽 = 2.3px@206px = 4.6px@420px，
总幅 3.0x → 3.75x）。代价认清：画面每边小 3px。**这是唯一的办法** —— 框深 0.060w 时六档色带全挤在
0.060w 里，相邻差上限只有 ~0.010 卡宽（约 2px），怎么排都读不出来（§十二 的「这是面积问题」在
框深上同样成立）。

**色带的两条深色描边**（做法）：`.home-card-strip` 本体 = 深色衬底（`--strip-ink: rgba(11,8,15,.68)`，
结构件、六档同值）的整条环；`::before` = 该档纯色版色带（`inset: 1px` + 自己的 content-box 反相
mask，环厚 = `--rank-sw` − 1px）。衬底在色带内外各露 1px，于是色带的两条边被定住：外缘那条把它
与框材质分开（灰融灰 / 蓝融蓝），内缘那条贴在画面边上（白融白）。实测依据：不加描边时色带与框材质的
最大通道差只有 **30–44**（灰 vs 灰、蓝 vs 蓝），加了之后描边在两侧都是 1px 的 70 左右的深线。

先试过 `border` + 负 `outline-offset`（1:1 扫描线上完全正确，两条线各就各位），但 8× 放大下
outline 的拐角不跟 `border-radius` 缩半径、出现方角小方块/断口 —— 换成衬底 + 子层这条路，两层
走同一条 mask，圆角天然同心。**另一条坑**：`::before` 必须是「一圈」而不是「一整块」—— 只填满
衬底里圈的话，mask 会把它一直顶到画面边上，把衬底内缘那 1px 也盖掉（1:1 扫描线上就是这么发现的）。

**遗留（下一轮）**：① 收藏档那条灰带（`#84888f`）在浅画面上仍偏弱：扫描线上色带 132 对框材质 220
（差得不小），弱的是它与浅画面那一侧 —— 要不要把灰再压沉一档（例如 `#6d7280`）留待判读；② 8× 放大
下 1px 描边在圆角处仍是像素阶梯（3× 与 1:1 看不出来），未做专项修；③ 样张
（`lab/redesign/verify.html` / `sheet2-final.html`）尚未同步这三处数值，反推这一节请以 CSS 为准。

## 三十五、等级层复验（2026-09-25）与两处裁定

上一轮把三个块一次性落进了 `90e8d91`，但提交信息与交接文档都写着「块②③ 未做」——**状态描述与实际 diff 矛盾**，接手时先对账（`git show --stat` 与源码），别照文档重做。首次 1:1 复验（真站、206px、六档正/背接触表，证据在 `lab/结果/collreview/`）三件事：

### m3（史诗）负形：五角星 → 三叉矛（用户 2026-09-25 裁定）
现状 m3 与 m5 都是五角星、只差旋转；剪影测试里 **20–24px 一眼分不出**（旋转在小尺寸丢失），§二十二「外形差异靠负形的质量分布拉开」在 m3/m5 上没落实。改成三叉矛（硬折角、横档厚、中央矛尖最高）：20px 仍读得出「一尖居中 + 左右两叉」，也不与 m1 的竖向 Y 撞。窄横档那一版（m3A）20px 退化成 Y，弃。
落地三处必须同步：`21-card-deck.css` 的 `--pat-mark3` 与两张样张页的 `.mark.m3`（各含 `.motif-rich` 变体）。

### 墙上花纹：n-ink 的可读性上限（这条把 §二十四 的判读作废）
`n-ink` 的 tile = 0.115w（206px 上 23.7px），**带厚与波纹周期都 ≈3.4px**，而花纹环本身只有 0.020w ≈ **4.1px 厚** —— 特征尺寸与容器同量级，206px 上只能读成肌理（1:1 判读「像噪点」，8× 下才看得出钩状小叶）。要读出母题得让花纹环 ≥8px（0.04w），而框深 0.075w 里还要放材质与色带，§三十四 又已否掉 0.080w 的框 —— **墙上「可读纹样」在当前预算里不成立**，§二十三 的保守口径（墙上不做可读纹样）成立。保留当肌理还是换成 §十四 的双细线，等用户定。

### 复验中修掉的一处
奇迹显形时卡背档位名停在服务端渲染的 `rankWallLabel`（写「收藏」）—— `deck:reveal` 现在与 `.deck-tile-meta` 同口径一起换真名（`764920d`）。

### 仍开着的偏弱项
① 秘藏紫带在暗卡面上偏闷；② 收藏档灰带在浅画面上弱（与 §三十四 遗留① 同一条）；③ 卡背标的 3× 判读与 6× 判读不自洽（奇迹：五角星 vs 六角星+外环），真值是 `--pat-mark6` 横向翼 + 0.80 盾环 —— 低分辨率那次不算数。


## 三十六、卡背升级：第一轮访谈定案（2026-09-25，用户回编号）

**起手对账（读源码，不照文档）**：正面清框（`e5f007a`、`5448767`）后等级只剩卡外文字与卡背；而「卡背」在仓库里是**两套**：
- **能看见的那套** = 弹层三维查看器里画布现画的 `drawBack()`（`card-3d.js:970`）。构图：统一黑底斜格 + 双层金线框 + **居中的一枚系列纹样**（`emblem()`：卡夫卡彼岸花／黑长直少女新月＋星／绫华六角刺）+ 卡名/系列/分隔线 + 传世以上才有的档位名 + 出处 + 「KAGAMI · 卡片组」；等级只有**一圈同形金环**（`BACK_RING_A/W` 六档同半径同轮廓，只差 alpha 0→.95 与线宽 0→6px；传世/奇迹加第二圈 2px；奇迹加径向背光）。
- **看不见的那套** = DOM 卡背（`.home-card-back` + `--bk-*` 十一个令牌 + `--pat-mark1..6` 六枚负形镂空标 + 档位名 + 罗马数字）。**全仓没有脚本会挂 `.is-flipped`**（`21-card-deck.css:1426`、`deck-wall.html:112`、`home-cards.html:94` 三处注释自己承认）→ 墙上与首页都翻不出来，是「已落地的静态卡背 + 一个没人接的类名钩子」。

**用户拍板（本轮）**：
1. **载体：只升画布卡背**（弹层里翻到的那面）。DOM 金轴那面本轮不动。
2. **构图：两层** —— **外圈等级标 + 内里系列纹样**（现状是系列居中、等级只有一圈同形环；现在等级标升为独立一层）。
3. **通道：四条全开** —— ① 等级标形状差异 + 金量按元素累加（图标→内框→文字→角饰）② 星级或罗马数字 ③ 全息/虹彩覆膜 ④ 四角角饰 + 卡背专属外框几何。
4. **上一轮 `3b9884d`（星级 + 全档档位字 + 金色强度阶梯 + 文字上下镜像）被 `ca30a5c` 撤回的原因 = 文字上下镜像**（用户原话）。星级、档位字、金色阶梯三样**不在否决范围**，可重提（不带镜像）。

**载体选定后的一条直接推论**：卡背只出现在**弹层尺度**（约 1050 物理 px），墙上 205px 那条约束**不适用于卡背** —— 星级、角饰、覆膜、衍射细颗粒这些在墙上会被压掉的通道，在卡背上都上得起；卡背的验收尺寸从「205 + 420 双尺寸」改为「弹层 1:1 + 放大」。（墙上的等级可读性仍是另一件事，本轮不碰。）

**待定（本轮未拍）**：
- 六档等级标的**形态方向**：用户未选四选一里的任一项，改问「能否有一些背景的图案（例如 Claude 各个模型的命名）」→ 理解为「每档一套与档名对应的背景纹样/命名体系」，第二轮访谈再定。
- 奇迹是否给独有卡背形态（用户未答；我的倾向：要）。
- 上一轮那三样是否重提（用户未答；我的倾向：重提，减去镜像）。


## 三十七、卡背升级：第二、三轮访谈定案 + 设计提案（2026-09-25）

**用户拍板（第二轮）**：
5. **命名体系：每档另起一个「短名 + 意象」**（学 Claude 的 Haiku/Sonnet/Opus 那种取法），**现档名降为副标**。
6. **背景形态：每档一个独立母题**（六种图形，共用同一套压印骨架与构图）—— 不是同一母题加密。
7. **落位：满幅低对比暗纹**（整面铺，压在所有元素之下）。
8. **上一轮那三样全部重提**：星级 + 全档档位字 + 金色强度阶梯，**不含文字镜像**。
9. **奇迹给独有卡背形态**：专属底纹 + 六角星徽 + 彩环 + 虹彩内框。

**用户拍板（第三轮）**：
10. **短名用「A 星象」套**：Ⅰ 落尘 / Ⅱ 流萤 / Ⅲ 星座 / Ⅳ 月轮 / Ⅴ 日冕 / Ⅵ 蚀。
11. **卡背那行档位名：短名在前、现档名作副标** —— 形如 `Ⅵ 蚀 · 奇迹`。
12. **短名只在卡背出现**：筛选条 chip、卡片外 meta、`rankLabel` 等其余消费点保持现档名（`rankNo`/`rankWallNo`「不进 i18n」那条口径不变）。

**设计提案（我提的，**待样张确认，不算用户决定**）**：
- **等级标形态**（第一轮那条用户没直接答，被命名题吸收）：把「意象」做成**两级强度、一处两用** ——
  - **单枚（等级标本体）**：保留「统一底场 + 负形镂空」的外轮廓（六档同一枚圆角盾，家族感与对比度靠这个解），内部**负形**换成该档意象的简化形：落尘＝细点簇 / 流萤＝三点带尾 / 星座＝点与折线 / 月轮＝月牙＋内弧 / 日冕＝长短交替放射 / 蚀＝全食环。
  - **满幅（背景母题）**：同一意象的平铺版，低对比、密度按档递增。
  - 理由：一轮下来要解决的是**排序**（第一轮的核心问题），而现有 m1–m6 是「同族变形」（m3/m5 在 20px 曾分不开）；意象套的六个轮廓差异大得多，且同时当图案与标用，不必画两套图形。
- **星级取法**：`Ⅰ–Ⅵ` 罗马数字已经存在（`rankNo` 阶序标记），直接复用；位置在等级标下方或环上，**不做镜像**。
- **验收尺寸**：卡背只在弹层出现（显示宽约 430px、纹理 800×1120），样张按 **1:1（430px）+ 2× 局部**出图；墙上 205px 那条约束对卡背不适用。


## 三十八、卡背升级样张 v1→v3（2026-09-25，自审三轮，未动源码）

**样张源**：`lab/redesign/cardback-v1.html`（画布按卡背真实纹理 800×1120 画，显示按弹层 1:1 的 430px；v2/v3 是逐轮快照）。**拍摄**：`lab/工具/cardbackshot.py`（Edge headless、DPR2、视口 3000）+ `cardbackcrop.py`（按页面自报矩形裁切）。

**踩到的坑（必须记住）**：整页图是 **CSS × 4**，不是 ×2 —— DPR2 与 `captureScreenshot` 的 `clip.scale=2` 是**相乘**的。按 ×2 裁切会把页头标题当成卡背判读（v1 第一轮判读整批作废）。

**三轮差**：

| 版本 | 判读结论（1:1 行 + 2× 局部 + 剪影行） |
|---|---|
| v1 | 六枚意象镂空都能认出（三点簇／两点连线／折线星座／月牙／放射／环）；短名行六档全可读；奇迹与其余五张明显不同。**但 Ⅱ/Ⅲ 最难分**、**月轮（Ⅳ）镂空被读成「竖向矩形」** |
| v2 | 月牙读出来了（「不完整圆环 + 右侧月牙」）；背景母题被认成「月牙与弧线」，**不是噪点**。判读改点名 Ⅲ/Ⅳ 最难分 —— 查因：环宽 2–6 纹理 px 在 430px 上只剩 1–3px，**这条通道被显示尺寸吃掉了** |
| v3 | 阶梯成立（「从左到右能排成越来越华丽」，Ⅵ 判为独立档）。**但** ① Ⅰ/Ⅱ 成了最难分的一对（都偏暗、低饱和）②「外框没有随档明显变粗、Ⅱ–Ⅴ 都是细环」—— 环宽拉到 3–12 纹理 px 仍读不出来 |

**证据**：`lab/结果/collreview/cb-v{1,2,3}-{row-backs,mark-123,mark-456,sil-row,back0..5}.png`。

**本轮真正的设计收获**：
- 430px 上**有效**的档间通道 = 底色明度/彩度、覆膜虹彩强度、大面积金量与线宽**按显示 px 定值**；**纹理 px 定的线宽（环宽、外框宽）效率最低**。上一轮的 `BACK_RING_W = 0,2.5,3,3.5,5,6` 正是这个问题（显示后 0–3.2px）。
- 剪影测试：六枚意象标 **20px 以上可区分**；16px 时 落尘/流萤/日冕 糊在一起。卡背只在弹层出现（430px），这条不构成约束。

**待用户拍（两条）**：
① Ⅰ/Ⅱ 那对用什么拉开：a) 明度阶梯更陡　b) Ⅱ 起就上覆膜（现为 Ⅲ 起）　c) 标尺寸随档递增。
② 等级标尺寸是否允许随档递增 —— 唯一还没用上的大面积通道，代价是「统一底场」的家族感略弱。


## 三十九、卡背升级：v4 定样（2026-09-25，两条调参已拍）

**用户拍板**：
13. Ⅰ/Ⅱ 用**明度阶梯**拉开（不做「Ⅱ 起上覆膜」）—— 保住「覆膜 = 高档」这条信号的纯度。
14. **等级标尺寸随档递增 ±8%**（Ⅰ 0.92 → Ⅵ 1.08，逐档 3.2%）。

**v4 判读（1:1 行）**：
- 阶梯成立：「能排成单调递增」，六档档名判读确认为 收藏 → 奇迹 单调。
- Ⅰ 与 Ⅱ 已分得开（黑灰底/灰标/三点簇 vs 深绿底/米色标/两点连线）。
- 标尺寸「有可见的总体增大」，但 **Ⅱ–Ⅳ 相近** —— 相邻只差 3.2%，是「±8% 总幅」的必然结果，采用户口径。
- 新的「最像」一对是 Ⅱ/Ⅲ（深绿 vs 深蓝、两点连线 vs 折线星座）；判读用词是「最像」，**不是「分不开」**。

**定样**：`lab/redesign/cardback-v4.html`；证据 `lab/结果/collreview/cb-v4-*.png`、拼图 `cardback-v4-sheet.png`。
**下一步**：落地到 `card-3d.js` 的 `drawBack`（画布现画，不引入素材），真站 1:1 复验后提交推送。


## 四十、卡背纹样与标的做工改造（2026-09-25，样张 v5/v6）

**起因**：用户「图案能再设计的更有美感一些吗」。自审同意：旧满幅是**等距平铺的小图标**（壁纸感 / 撒贴纸），旧标是**等宽笔画的 UI 图标**。

**做法**（按 `surface-craft-design` §1/§5 的三把杠杆）：
- 收锋填充带代替等宽描边（`taper()`：起笔粗、收笔尖）；
- 不再等距、不镜像（笔触长短/粗细/角度全随机，period-2 节奏）；
- 密度成团（低频团块决定「墨在哪」）+ 偶发件；
- 两条路线并列：**精修矢量**（canvas 现画，零素材）与**位图纹样板**（`lab/工具/backplate.py`，numpy+PIL 生成整张 800×1120 白色 alpha 板，运行时 `source-in` 染档位金）。
- 标的新版：金色底 + 斜向高光带 + **双线内刻线** + 收锋件当「洞」+ 向上 1.2px 的刻痕高光。

**判读（v6，1:1 行 + 2× 局部 + 标的对照）**：
- **标：新版判「像金属徽章」**（旧版被判「边缘生硬、等宽笔画、像平面贴图、层次单薄」）。收锋「有但不统一」——Ⅰ/Ⅱ 明显，Ⅲ 基本等宽，Ⅳ/Ⅴ/Ⅵ 因形状本身带尖而不吃笔锋。
- **纹样：精修矢量两处都判最好** —— 1:1 行「最像真卡背印刷纹样」，2× 局部「若只能选一态，选精修矢量（保留手绘自然感 + 矢量清晰）」。旧平铺「重复感明显、偏平」。
- **位图板**：降强度前被判「像撒贴纸/噪点、抢中央徽标」；降强度后（覆盖 13%、alpha 0.13+dens·0.20）「不再抢徽标，但颗粒/噪点感使其细腻度弱于精修矢量」，只在「最像印刷/手绘笔触」这一条上占优。
- 标的剩余项（可选的下一步）：判读点名 **Ⅰ/Ⅱ 最相近**（都是点＋轨迹）、**Ⅴ/Ⅵ 相近**（都是心圆＋放射）。

**素材账**（若走位图板路线）：六张板覆盖 0.15%–13.0%，单张 8–271 KB，**合计约 0.96 MB**；生成脚本 `lab/工具/backplate.py`，板在 `lab/结果/collreview/plates/`。精修矢量路线零素材。

**样张**：`lab/redesign/cardback-v5.html`（三态并列）、`cardback-v6.html`（收口版）；证据 `lab/结果/collreview/cb-v6-{marks-new,marks-old-new,pat-cards,pat-zoom}.png`。


## 四十一、收锋精修与三处方法论修正（v7→v10，2026-09-25 定样）

**用户拍板**：纹样定**精修矢量**（位图板那条路收进 lab 作参考，不进主线）；标**再拉强一轮收锋**。

**修到的三处真问题（以后别再犯）**：
1. **`taper()` 对两点直线不插值** —— `t = i/(len-2)` 让只有两个端点的笔画只画一段、宽度恒为 `w0`，收锋根本不发生。日冕的射线、蚀的细芒、月牙的撇全是等宽的，判读没冤枉。修法：点数 < 12 时先线性重采样成 15 点，并把归一化改成 `i/(len-1)`。
2. **判读的裁图必须按显示尺寸** —— 六枚标拼进一张 2400px 宽的图里，每枚只剩约 150px，比 430px 卡背上的实际尺寸还小，于是「笔锋」被判成「等宽」。改成**单枚单独裁**(约 2× 显示)后，Ⅴ/Ⅵ 立刻判出「内粗外尖」。凡判细节，一张图只放一枚/一张。
3. **负形的颜色描述不可信** —— 件是深色「洞」（实测 RGB 47,52,68），判读连续几轮描述成「白色图形」。判负形一律**量像素**：件色值 + 腐蚀曲线看宽度分布（本次 89%→41% 渐变 = 存在收锋；等宽会在某一级突然塌掉）。

**其它取值**：标的笔画粗端按**显示 px** 反推 ≥4.5 显示 px（≈8.4 纹理 px，即件尺度 s 的 0.07–0.10）；Ⅲ 的弧拉长、端点亮化缩小后被判「有粗细变化」。

**v10 定样判读**（六档 1:1 行）：① 阶梯单调成立；② 纹样「像印出来的纹样」（不是撒贴纸）；③ 徽章「像有工艺的金属徽章」；④ 最强 Ⅵ、最弱 Ⅰ（Ⅰ 是素档，符合设计）；⑤ 无硬伤。**仍开着**：Ⅰ 灰黑底 + 银徽章对比过低、Ⅴ 棕金底与金徽章色相接近。

**证据**：`lab/结果/collreview/cb-v{7,8,9,10}-*.png`；定样页 `lab/redesign/cardback-v10.html`（含「五、六档定样 1:1」一行）。


## 四十二、落地（2026-09-25，真站复验通过）

**改了两个文件，零素材、零新请求**：

- `assets/js/card-3d.js`：新增 `BACK_SHORT`（六条星象短名）/ `BACK_RN` / `BACK_ART`（六档金轴：gold、goldLo、tint、dens、glow、line、name、holo、corgold）；`BACK_RING_A/W` 重定为 `.38/.55/.75/.88/1.00` 与 `3/6/9/12/15`（**纹理 px**，落到 430px 显示是 1.6→8.1px）；新增 `mixHex / rnd / sArc / sQuad / onQ / taper / shieldPath / goldGrad / PIECE / backPattern / conic / hexStar / starY`；`drawBack` 重写为两层构图十段（底 → 满幅纹样 → 斜向反光 → 外发光 → 框线 → 四角角饰 → 等级环 → 等级标＋系列徽记 → 文字 → 覆膜）。纹理仍 800×1120 现画。
- `assets/js/home-deck.js`：`setItem` 多传一个 `rankNo`（模板里本来就有，阶序标记的单一事实源），卡背那行用它拼「Ⅵ 蚀 · 奇迹」。

**口径**：短名**只在卡背**；`rankLabel` / 筛选条 / meta / aria-label 一律不动 —— 与 `rankNo` 不进 i18n 同一条。

**守卫**：`check-deck` / `check-cards` / `check-consistency` / `check-css-split` 四条全绿（`RANK_3D` 与 `back` 通道没动，六档单调性守卫照过）。

**真站复验**（`lab/工具/backlive.py`：六档各取一张**同系列**卡隔离档位轴、`setAngle(π, .06, true)` 翻到背、弹层实拍）：六张 `isBack` 全 true。判读：① 阶梯递增成立（Ⅰ/Ⅱ 无环 → Ⅲ 起加金环 → Ⅴ 更重 → Ⅵ 彩环＋专属件）；② 全档读成「金属徽章/铭牌」；③ 满幅纹样读成「像印刷/图层叠上去的」；④ 无叠字、无脏、无明显错位。最弱仍是 Ⅰ（素档，设计如此）。

**残余**（与样张同）：Ⅰ 灰黑底＋银徽章对比偏低；Ⅴ 金褐底与金徽章色相接近。

**证据**：`lab/结果/collreview/real-{collector,rare,epic,arcane,legend,miracle}.png`（弹层原地 2×）＋ `real-row.png`（六档 1:1 并排）；探针 `lab/工具/backlive.py`。

## 四十三、玩法层：探索进度 / 今日一抽 / 看工艺（2026-09-25，已落地）

这一轮是**功能**不是造型，三条一起做（用户拍板「A+B+C」）：

| 玩法 | 做什么 | 落点 | 量出来的 |
|---|---|---|---|
| 探索进度 | 打开过 = 看过；翻到背面 = 看过卡背。格子右上角一点（看过）／再多一圈（看过卡背），结果行下一条 `已探 n / 57` | home-deck.js 写（localStorage `deck:seen`，键是卡的 **id** 不是下标）、deck-wall.js 读与画 | 真站自检：打开一张后 `已探 1 / 57`、格子带 `is-seen`；翻背之后 `deck:seen` 值 `1 → 3`、格子带 `is-backseen` |
| 今日一抽 | 角落那枚小人：按访客本地日期洗一遍**当前看得见的**那些，给下一张没看过的；全看过退回第一张 | deck-wall.js（种子函数与 home-deck.js 逐行同源，check-deck 有守卫） | 连抽三次得到 3 张不同的未看过卡、进度 1→2→3；筛到「传世」再抽，抽到的是传世档 |
| 看工艺 | 弹层里第二颗按钮：固定斜角 0.40 rad + 拉近 1.8×，退出回原机位 | card-3d.js `setInspect()`（`zoom` 进 `resize()` 的机位距离，阻尼 8/s）、home-deck.js 的按钮 | 同一姿势下开关浮雕：正常机位 **7.66 均值 / 38.4% 像素**，近观 **21.54 / 80.9%**（≈2.8×）；卡面覆盖率 0.593 → 1.00，退出精确回 0.593 |

**三个判断**（都影响后续）：

1. **近观的角度不是杠杆**：0.40 rad 与 0.55 rad 的浮雕读数 1.71 vs 1.83，落在噪声里（对照组同姿势差 3.6% vs 3.5%）→ 取 0.40（能多看到卡面）。真正的杠杆是**放大**本身。
2. **「收录日期」只有两个值**（2026-09-20 × 29、2026-09-21 × 28，都是批量提交），所以**不做时间线**，只作弹层信息卡的一行。作品/角色按系列映射填，混源的「黑长直少女」那 4 张**逐张写**。
3. **探索进度按 id 不按下标**：清单中间插一张卡，它后面每一张的下标都会挪 —— 用下标记的话，进度会串到别人身上（深链 `#deck-07` 是另一回事，它本来就按清单顺序，且有「筛掉就不开」那道兜底）。

**新增的三条数据字段**（`data/home-cards.yaml`，可选）：`work` / `role` / `added`，`added` 由 git 现取（`git log -S <src>` 最早那一笔），check-deck 加了格式守卫。

## 四十四、玩法层第二批：卡册 / 搜索 / 系列点亮 / 对比 / 存图 / 开包（2026-09-25，已落地）

「全完成」那一轮把剩余六条一次推完。全部零新素材。

| 玩法 | 做什么 | 落点 | 量出来的 |
|---|---|---|---|
| 卡册 | 一行三张、每页 9 张，`‹ 第 n / m 页 ›`；顺序跟当前筛选与分组 | `deck-wall.js` + CSS | 57 张 → 7 页；`?book=1&page=2` 可分享 |
| 搜索 | 输入即筛，命中名字 / 系列 / 工艺 / 等级 / 作品 / 角色；结果行补 `· 搜「词」` | `deck-wall.js` | 「绫华」18 张、「原神」（按作品）18 张 |
| 系列点亮 | 某个系列**探满**时 chip 上亮一个点（与 chip 上的实时计数无关，两个数各说各的事） | `deck-wall.js` + CSS | 把「初音未来」4 张标满 → chip 亮、进度 `已探 4 / 57` |
| 对比 | 弹层「设为对照」→ 换一张 →「与《X》对比」：**同一固定斜角各拍一张**并排 | `home-deck.js` + `card-3d.snapshot()` | 两张卡面外框都是 612×946、中心 x=398.6（同角同尺寸）；每张 ~1.5 MB |
| 存图 | ✅ **2026-09-25 已下掉**（按钮 + `saveShot()` + i18n 键 + 模板绑定都删了，见四十六；要存图用 `card3d.snapshot()`） | — | — |
| 开包 | 今日一抽专用：先亮卡背、320ms 后翻正面、配一道扫光 | `home-deck.js` | 60–260ms `isBack()` 为真、420ms 起为假 |

**四条踩出来的约束**（下一次还会撞上）：

1. **3D 查看器是单例** —— `attach` 把 `host` / `canvas` / `GL` 放在模块作用域，两个活着的 3D 卡做不到。
   对比于是是「一人拍一张」：换贴图 → 钉在固定角（`setAngle(y, 0, true)`）→ **同一个任务里** `toDataURL`。
   钉住这一步不能省：WebGL 的绘图缓冲出了那一帧就是空的（lab/README 那条老坑）。
   面板先盖上再拍 —— 底下那两次换卡才看不见。
2. **对比面板必须不透** —— `--surface` 只有 .80，直接铺在还在转的活卡片上会透出重影（第一版截图里就能看见）。
   做法是「同一个半透明面叠在 `--theme` 实底上」，不是换一个更黑的颜色：深浅两套主题各自维持原来的色相。
3. **`var` 只提升声明** —— 新按钮的创建语句必须排在接线之前，否则 `X.addEventListener` 在读取时是 undefined（这次真踩了，整页白屏）。
4. **文字都走 `data-*` + i18n 键名** —— 少一个键是**静默**的（`data-save` 没绑 → 按钮上出现 `save`），
   所以 `check-deck.mjs` 按整串绑定查：`data-x="{{ i18n "key" }}"`，两边（键在不在 toml、模板有没有绑）都查。

## 四十五、点开卡「白板」的根因 + 开卡性能（2026-09-25，已落地）

### 1 白板（用户截图报的那个）

`.home-deck-compare-panel` 里写了 `display: flex`。**作者写的 display 会盖掉 UA 的 `[hidden] { display: none }`**
—— 于是这块「隐藏着」的实底面板一直按 flex 铺在弹层上：实测 `hidden === true` 而计算样式 `display: "flex"`、
盒子 663×454、屏幕正中心那个点是 `home-deck-compare-close`。点开一张卡只看得到一块白板 + 一颗关闭键。
修法一行：`.home-deck-compare-panel[hidden] { display: none; }`。

**教训**：凡是带 `hidden` 又要自己设 `display` 的浮层，必须跟一条 `[hidden]` 规则。判据不能靠「我以为它隐藏了」，
要看 `getComputedStyle(el).display`（探针固化为 `lab/工具/whiteboard.py`）。

### 2 开卡耗时（430×900、dpr 2、headless Edge，同一台机同一命令）

| 量 | 改前 | 改后 |
|---|---|---|
| 点开 → 弹层出现 | **740–870 ms** | **4–27 ms** |
| 点开 → 3D 出帧 | 906 ms | 66–75 ms（预热命中）/ 396–452 ms（预热未命中） |
| 关掉后 1.5 s 内帧增量 | 0 | 0（本来就不空转，改造没有引入空转） |
| 保存卡图（同步 `toDataURL`） | — | 21–29 ms，输出 836×1170 / 1.3 MB |
| 卡片墙空闲帧间隔 p50/p95 | — | 6.1 / 6.2 ms |

改前那 740–870 ms 全在 `attach()` 里：建 GL 上下文 + 编译 POM/浮雕那套着色器 + 传第一张贴图，
**同步**执行 —— 弹层必须等它，于是白板停一秒。两条改造：

1. **先亮弹层、后建 3D**（`openDialog`）：`dlg.hidden = false` 提到最前 → `fillDialog` 先摆平面大图
   （没有 viewer 时本就是安全的）→ `requestAnimationFrame ×2`（这一帧已经上屏）之后才 `attach`，
   建好再补一次 `fillDialog` 把这卡推给 3D。
2. **空闲预热**（`warmViewer`）：`requestIdleCallback`（2.5 s 超时兜底）里 `attach` + **真画两帧** ——
   只 attach 不够，第一次 `setItem` 与首帧浮雕自己还要 ~600 ms。预热只用页内那一档贴图、不传深度图；
   `connection.saveData` 与 `deviceMemory <= 2` 直接跳过（这笔钱是为「点得爽」付的，不该让省流访客付）。

### 3 顺手修掉的回归（性能改造自己撞出来的）

拆 `openDialog` 的分支时，第一条写成 `fillDialog(items[i])` —— **漏了 `opts`**。于是「今日一抽」的开包动画
（靠 `opts.deal` 带 done 钩子）在**热路径静默失效**：冷路径正常、预热命中后 `setItem` 的 `done` 变 `undefined`。
这种漏参不报错、四守卫也不管，只能靠 e2e 探针量出来（`lab/工具/dealprobe.py` 采 `face`/`is-dealing` 时序）。

## 四十六、「前一张卡的模型留在下一张卡上」的根因 + 下掉保存卡图（2026-09-25，已落地）

### 1 根因：深度图那一路没有 `seq` 守卫，换卡时也不清场

`setItem()` 里三条贴图任务，**面贴图那两条早就有** `if (seq !== faceSeq) return;`（2026-09-22 为「低清后到
把 xl 盖回去」加的），**深度图那条一直没有**：

```js
if (next.d) { jobs.push(loadImage(next.d).then(function (im) {
  if (texDepth && texDepth.tex) GL.gl.deleteTexture(texDepth.tex);
  texDepth = { tex: makeTex(im), ... };        // ← 没有 seq 守卫
}).catch(...)); }
```

后果：限速或快速翻页时，**上一张的深度图后到**，就把这一张的 `texDepth` 换掉 —— 卡面是新的、**浮雕还是
上一张的**，也就是报的「前一张卡的模型留在下一张卡上」。这是最坏的一类错：卡面正确、等级/工艺都正确，
只有「模型」错，肉眼很难指认，也不会报错。

线上旧包（`card-3d.min.626c52ee…`）里这一段的原样可查：面贴图分支带守卫、深度分支不带。

### 2 修法（两条一起）

1. **换卡的一刻先把深度清掉**（`deleteTexture` + `texDepth = null`）：新图到达前这一张渲染成平的，
   观感是「先平面、再浮雕」。留一张错的深度比留一张平的糟得多。
2. **深度任务补 `seq` 守卫**（`then` 与 `catch` 都加），并记 `texDepthFor` 归属。

**可验证性**：`stats()` 新增 `depthFor`（当前挂着的深度图 URL）。限速 150 KB/s + 150 ms 往返下的断言：

| 时刻 | `stats().depthFor` | 结论 |
|---|---|---|
| 打开第 4 张后 | 第 4 张的深度 | 正确 |
| 翻下一张后 **60 ms** | **空** | ★ 不再挂着上一张（修前这里就是上一张的文件名） |
| 翻下一张后 2 s | 第 5 张的深度 | 正确 |

### 3 下掉「保存卡图」

理由：与「对比」共用 `snapshot()`，而对比才是真正用到它的功能；单独导一张 PNG 属于本地收藏动作，
在弹层里多占一颗按钮。移除面：`saveBtn` + `saveShot()` + i18n `[deckSave]` + 两个模板的 `data-save`
+ `check-deck.mjs` 的键表与绑定表（探针加了一条「`.home-deck-save` 必须不存在」的断言）。

> 想拿回这张图：`window.card3d.snapshot()` 仍在（对比在用），控制台一行就能存。

## 四十七、下掉「收录日期」（2026-09-25，已落地）

用户拍板：**收录日期去掉**。它本来就是「只有两个值」的信息（09-20 × 29、09-21 × 28，都是批量提交，
见四十三的判断 2），进到弹层信息卡里只多一行噪声；当天晚些时候整体下掉。

移除面（一处不改就会「留一行空」或静默回落英文）：

| 层 | 文件 | 去掉的东西 |
|---|---|---|
| 清单 | `data/home-cards.yaml` | 57 行 `added:`（`added` 不再进清单） |
| 模板字典 | `layouts/_partials/deck-manifest.html` | `"added" (.added \| default "")` |
| 模板 | `deck-wall.html` / `home-cards.html` | `data-info-added="{{ i18n "deckInfoAdded" }}"` |
| 文案 | `i18n/zh.toml` | `[deckInfoAdded]` |
| 逻辑 | `assets/js/home-deck.js` | `infoAdded` 标签读取 + 那行 `bits.push(...)` |
| 守卫 | `scripts/check-deck.mjs` | `added` 的 `YYYY-MM-DD` 格式检查 |

`work` / `role`（作品 / 角色）**留着** —— 它们是随卡不同的信息，不是冗余。信息卡那一行现在是
`作品 原神 · 角色 神里绫华`。要恢复：`git revert` 这一个提交即可，格式检查与标签都还在提交里。

### 顺带清掉的另一处冗余：信息行里的「角色」

去 `added` 之后信息行还剩 `作品 X · 角色 Y`，而 Y 常常等于系列：

| 情况 | 张数 | 例 |
|---|---|---|
| 系列 == 角色 | **35** | 卡夫卡 / 卡夫卡 |
| 系列 ⊂ 角色 | **18** | 绫华 ⊂ 神里绫华 |
| 角色是独立信息 | 4 | 黑长直少女 / 原创角色 · 看板娘 |

前两种情况下这一行等于把**上面那格系列徽记**又说一遍。所以判据写成「`role` 与 `series` 相同或
`role` 含 `series` 就不显示角色」（不硬编码哪一批）：

- 绫华批 → `作品 原神`
- 初音未来批 → `作品 VOCALOID`
- 黑长直批 → `作品 原创 · 角色 原创角色`（角色确实带了新信息，留着）

`work`（作品）**留着**：57 张全有，且是随卡不同的信息。

## 四十八、弹层加载条（2026-09-25，已落地）

**等的是哪段时间**：点开一张卡到「这张卡的三档贴图（低清 / xl / 深度图）到位」之间。这段时间里
屏幕上原本只有平面大图顶着、没有任何「还在动」的信号。

### 三个决定

1. **慢才露头（120 ms）**：页内那一档多数据已在缓存里，几百毫秒就完事 —— 露一下就闪走比不露更烦。
   `BAR_DELAY = 120`；内测：**不限速时一次都不露头**（缓存命中），限速时才出现。
2. **进度是真进度**：分母是这一次 `setItem` 排出去的**贴图任务数**（低清 / xl / 深度 = 3 格），
   来自 card-3d 新增的 `progress(done, total)` 回调 —— 三条任务的 `.catch` 各自吞了失败，
   所以它们**都会** resolve，计数不会卡住（失败也算「这一格走完了」）。
   **没用定时器假走动**：假走动在「卡在最后一张图上」时反而更让人等。
   带 `seq` 守卫：翻页很快时上一次 setItem 的进度不该驱动现在这条卡上的条。
3. **关掉就收 / 翻页就重来**：`barSeq` 让上一次那个 120 ms 的定时器作废（否则它会往一张已经换掉的
   卡上画条）；`barFinish()` 把宽度补到 100%、`is-done` 半拍再收（直接抹掉像出错）。

### 实测（430×900 @2x，限速 250 ms 往返 / 180 KB/s）

```
0.12s  hidden=False 宽=18px  aria-valuenow=0      ← 起步就是 8%，不从 0 开始
0.72s  hidden=False 宽=26px  aria-valuenow=33
1.08s  hidden=False 宽=110px aria-valuenow=100    ← 单调不减
之后   hidden=True                                 ← 收走
不限速时：一次都没露头（好）
```

### 形态

台面下沿、压在卡的最下面一条细线上（`position: absolute; bottom: 10px`，宽 `min(62%, 240px)`，
高 4px，胶囊），**不另起一行** —— 另起一行会在露出/收起时把下面的按钮顶一下，布局跳动比一条细线烦。
颜色走 `--accent`，深浅两套主题各自成立（暗色下也是「轨道 + 填充」可读）。填的那段带一道 1.5s 扫光：
4px 的细线光看宽度不好判「是进度条还是分隔线」，有东西在动才读得出「在等」。
`prefers-reduced-motion` 下扫光与过渡都关掉。

平面大图那条路（没有 WebGL）也挂：那时「加载」= `<img>` 的 load（`complete` 命中就直接收）。

新增文案键 `[deckLoading]`（`aria-label`），两个模板都加了 `data-loading` 绑定，
`check-deck.mjs` 的 i18n 绑定守卫按整串查（反向验过：删掉模板里的绑定，守卫报错、exit 1）。

## 四十九、光晕与全息「莫名其妙地闪」——量出来三处（2026-09-25，已落地）

用户口径：「光晕与全息效果能不能自然一些，不要莫名其妙地闪」。三处根因都是**一帧之内的大跳**，
不是「好不好看」的问题，所以先量再改。探针：`lab/工具/flicker2.py`（逐帧像素签名）、
`lab/工具/govline.py`（档位权重时间线）。

### 1 按下不动就整张亮起来（跟手高光）

`fxEff.glint = dragging ? R.glint : 0`：**一按下**（还没动）就把指针灯的高光灌满整张卡。
「只按下、不动」量到：第一帧 **9619 个像素同时变**（占卡面 52%）、单像素最大跳 147.7。

改法两条：① **真拖过才亮**（位移 > 3 px，`dragMoved`）；② 亮/灭都走斜坡（τ = 0.14 s）。
A/B 同一探针同一段（按下不动 2.2 s）：跳像素 **9619 → 0**，单像素最大跳 **147.7 → 1.9**。

### 2 星屑闪点：单格白噪声 + 两道硬门

原式 `n = hash(floor(puv*280))`、`sp = pow(n,110)*2.5*(0.15+0.85*tw)*smoothstep(0.15,0.60,hv)`
—— 亮点直径就一格（≈2 px），`tw` 与 `hv` 都是硬开关，单像素**一帧跳满 255**（按住不动也一直在闪）。
四处改软：

| 项 | 前 | 后 |
|---|---|---|
| 取值 | 每格一个白噪声 | **格间双线性**（4 格点哈希 + smoothstep 插值） |
| hv 门 | `smoothstep(0.15, 0.60)` | `smoothstep(0.10, 0.95)` |
| 闪烁周期 | `uTime*3`（2.1 s） | `uTime*1`（6.3 s） |
| 底色 / 峰值 | `0.15` / `2.5`、夹 1.5 | `0.25` / `1.8`、夹 1.1 |

密度不动（还是 `pow(n,110)`，注释里量过 ≈470 颗）。单像素最大跳 **147.7 → 1.9**。
星屑**没有丢**：局部极亮点计数 517 / **520** / 517（包络版 / 现版 / 线上包络版）——
「看不清就是没了」这种判断要做像素计数，别交给模型看图（同一份代码它前后两次读数相反）。

### 3 档位跳变：每次开卡，整层效果一帧降 15%

调速器档位是**离散**的，而 `GOV[govTier].fx` 乘在所有立体通道上：`GOV[0].fx = 1.00`、
`GOV[1].fx = 0.85`。启动/开卡那一下档位由 0 升到 1 → 所有通道（sparkle/holo/halo/cliff/disp…）
在**一帧里整体降 15%**。改成**生效权重渐近**（`govFxk`，τ = 0.22 s）：

| 时刻 | govK | tier |
|---|---|---|
| 点开卡 +104 ms | 0.981 | 1 |
| +201 ms | 0.934 | 1 |
| +401 ms | 0.885 | 1 |
| +801 ms | 0.856 | 1 |
| +1100 ms | 0.850 | 1 |

单次采样最大步进 **0.048**（100 ms），约 1 s 收敛；改前是 1.00 → 0.85 一步到位。
档位本身照旧离散判（`govern()` 不动），只把生效权重做成斜坡。

### 4 试过并否掉：给时间通道加「包络」

一开始假设「余韵到点在动画中途冻结、下次交互从半相位跳回满值」也是闪源，于是加了 `fxEnv`
（进 0.45 s、退 0.9 s）。**量的结果是这个假设不成立**：`fxTime` 是**累积相位**、跨停机连续
（停机后画布 Δ=0，没有跳）。而包络会把**静止状态**的星屑与虹彩一并压掉（`uSparkle × 0 = 0`）——
`holo3D-card` 那套的静止观感本就是「退化成一层静态虹彩」，压掉等于丢了工艺。已整体撤除
（`fxEnv` 一组变量、`stats` 里的 `env/envSent/sparkleLive/holoLive` 都没了）。
留着这条记录是为了下次别再想当然：**「闪」要量到帧级证据再动手，方向错了加机器只会把别的特性压掉。**

### 5 顺带踩的坑：在 `frame()` 里读 `draw()` 的局部变量

`R`（等级表那行）是 `draw()` 里的 `var R = selectRank()`。第一版把 glint 斜坡写在 `frame()` 里、
写成 `(dragging && dragMoved) ? R.glint : 0` —— **`&&` 短路让它「没拖的时候」一直不报错**，
第一帧真拖动才 `ReferenceError`，把整个动画循环静默带走（量到 `frames` 停在 467、`drag` 还是 1、
之后画布全静止）。现在 `frame()` 只推进 0..1 的权重，`R.glint` 那一次乘法留在 `draw()` 里。
教训：**在 A 函数里读 B 函数的局部**，只要有短路/条件包装，就会延迟到某个特定分支才炸；
探针要盯 `stats().running` + `frames` 增量，别只看画面。

### 6 读数（`stats().fx`）

`govK`（档位斜坡的生效权重）、`drag`、`inertia`。`fx.sparkle/holo/...` 是**真正送进着色器的值**
（已过档位权重），六档单调性那些断言读的就是它。

## 五十 · 「故意的空」与「真的缺」要在接口上分开（2026-09-25）

**症状**：首页与收藏库**每访问一次**就各报一条 `[card3d] 这张卡缺深度图` —— 而 57 张卡一张不缺
（清单 57 条、`assets/images/cards/depth/` 57 个文件、`hugo` 无 warnf）。

**根因**：`warmViewer()`（空闲预热）**故意**把 `wp.d` 置空 —— 它只借当前这张卡编 POM/浮雕着色器、
传第一张贴图，深度图留给真打开那次（省一次 15 KB 请求）。而 `setItem()` 里「`d` 为空」只有一种解释：
这张卡缺深度图 → 告警。于是「我不给」被读成「它没有」。

**修法**：把 `d === null` 定成「调用方明确说不要」——静默；`''` / undefined 仍是「清单里查不到」——
继续告警。预热那条改传 `null`。

**读数**：`lab/工具/warnwhere.py`（新，抓调用栈）把告警定位到 `setItem ← oe@home-deck.min`；
改前 `/` 与 `/collection/` 各 1 条，改后两页各 **0 条**。

**纪律**：诊断信息的价值取决于它只在该响的时候响。凡是「内部调用方主动留下的空值」都要在接口上
与「数据真的缺」区分开，否则每访问一次都在给自己制造噪声 —— 真缺那次就淹在里面了。

**同轮（管理页）**：`tools/admin/ui/index.html` 顶栏 `.brand` 由 `div` 改成 `h1`（整页原来没有一级标题，
读屏从标题列表进来第一档是空的）。`.brand` 补 `margin: 0` 压掉 UA 给 h1 的 0.67em 外边距。
实测顶栏像素级不变：topbar 1680×50、brand (14,14) 68×21、tabs y=7 —— 改前改后逐项相同。

**体检工具（新，都在过程区 `lab/工具/`）**：`siteaudit.py`（横向溢出 / 控制台报错 / alt·h1·标题跳级 /
无名可点元素 / 窄小点击目标 / 体量与 DOM，两视口）、`warnwhere.py`、`a11yprobe.py`（Tab 序 + 焦点环 +
跳过导航 + 弹层焦点管理）、`adminsmoke.py`（管理页六面板 + Ctrl+K + 主题）。**本轮结论：两个界面在
这些机械项上都干净** —— 站点无横向溢出、无缺 alt、每页恰好一个 h1、跳过导航与焦点环齐、弹层
`role=dialog aria-modal` 有名字且 Esc 后焦点回到原格子；管理页六面板切换、命令面板、主题、57 行清单
全部正常，零控制台错误。
