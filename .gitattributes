# 附件类文件不做换行符转换。
# 课程章节里的 PDF 等文件会随页面发布，若被当作文本做 LF/CRLF 转换，
# 会改变字节长度并破坏 PDF 内部的 xref 偏移，导致下载后无法打开。
*.pdf   binary
*.zip   binary
*.docx  binary
*.pptx  binary
*.xlsx  binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
# 自托管字体（static/katex/fonts，KaTeX 公式用）同样不能做换行符转换
*.woff2 binary
*.woff  binary
*.ttf   binary
*.eot   binary

# 脚本与词表强制 LF。本机 core.autocrlf=true，若不做声明，全新 checkout 会把它们变成
# CRLF：bash 脚本会报 $'\r': command not found；词表被脚本按行解析时也会带上 \r。
*.sh    text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
