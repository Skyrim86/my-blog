{{- /* 数学库（/library/）的分级页面 —— 内容适配器（Hugo content adapter，0.126+ 支持）。

     为什么用适配器、而不是在 content/library/ 下放一堆 _index.md：
     这些页面没有自己的内容，只有一条身份（我属于哪个大类 / 哪个细分），名字、范围说明、
     计数、卡片清单全部来自 data/math-too...[truncated]