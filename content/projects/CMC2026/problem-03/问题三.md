---
# 分层项目的文档页（regular page）：scripts/new-content.sh doc 自动生成
title: "问题三　全向干扰源的搜索—定位—清除策略与算法"
date: 2026-09-12
draft: false
weight: 1
# 公式本身在构建期已渲染（见 layouts/_markup/render-passthrough.html），这里的 math 只决定
# 本页要不要加载 KaTeX 样式。默认 true（本类文档通常含公式）；纯文字文档改成 false，省约 23KB。
math: true
description: ""
summary: ""
---
# 问题三　全向干扰源的搜索—定位—清除策略与算法

> 本章依据赛题正文及其**附录 1（干扰源特征）**、**附录 2（测向机原理）**、**附录 3（测试说明）**建立模型；策略已在**本地模拟器**（按附件 1/2 实现的等价环境）上完成批量演练，所有数值均由 `本地测试器/simulator/batch_plan.py` 与 `.review/` 下的只读验证脚本（`q3_config.py`、`q3_discard_check.py`、`q3_clear_chain_check.py`）复算。本章只讲策略思想、建模过程、算法设计与实验，全部公式、符号与定理陈述见《问题三_公式.md》，小证明与数值证书见《问题三_小证明.md》，问题级下界见《问题三_证明_下界.md》，写作与验证交底见《问题三_证明笔记.md》。

---

## 1 问题重述

全向干扰源共 $10\sim16$ 个，位置、频道（$1\sim20$ 且互不重复）、有效接收半径（$1000\sim1500$ m）均未知。机器狗从原点出发、初始频道 $1$，需在**确保全部清除**的前提下使时间越短越好；模拟器按"被清除源个数比例"与"平均定位清除时间（总时间 ÷ 被清除个数）"给出结果，并进行三次正式测试、按**表 1** 填报。

时间的计费口径（附录 2）：移动 $\lVert\Delta p\rVert/5$ s（$5$ m/s）；检测 $5$ s；频道切换 $1$ s（**仅** `/measure` 且频道变化时）；清除未发现 $3$ s、成功 $5$ s；`/enter`、`/exit` 不计时。评分口径的优先级为**先保证清除比例 $=1$，再最小化平均定位清除时间**（式 (8)，见《问题三_公式.md》）。**移动时间主导总时间**，故策略的核心是**减少无效行程**。

---

## 2 模型假设

1. **平面假设**：由附录 2(7)，测向机与干扰源同平面。
2. **有界误差假设**：示向度误差 $\alpha_i\in[\theta_i\pm1^\circ]$，$\delta=1^\circ$；不假设分布与独立性（同问题一）。
3. **全向源假设**：本方（问题三）所有源的有效覆盖角为 $360^\circ$，故"距离 $\le R_{\rm eff}$ 即必被听到"；因 $R_{\rm eff}\ge1000$ m 而具体值未知，**保证性推理只能用下界 $1000$ m**。
4. **每频道至多一源**（题面附录 1）：故某频道一旦清除即了结，某频道一旦被证明为空即永久排除。
5. **`no_signal` 的保守语义**：`no_signal` 只保证"源不在该点的 $1000$ m 内"，不保证更远。
6. **同一地点误差固定**（附录 2）：故重复检测同一点不能提高精度，必须换点。
7. 忽略数值舍入；通信按协议串行、每动作新 `request_id`。

---

## 3 符号说明

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $\Omega$ | 目标圆域 $B(O,1800)$，源必在其内 | 半径 1800 m 的圆 |
| $O$ | 原点（机器狗起点） | $(0,0)$ |
| $n$ | 干扰源总数（未知） | $10\sim16$ 个 |
| $K$ | 频道集 | $\{1,\dots,20\}$ |
| $k$ | 频道编号 | $1\sim20$ |
| $R_{\rm eff}$ | 干扰源有效接收半径 | $1000\sim1500$ m，具体值未知 |
| $\delta$ | 示向度误差半宽 | $1^\circ$ |
| $\alpha$ | 两视线的交会角（两视线在源处的夹角） | 度；$\alpha\le2\delta=2^\circ$ 时区域无界或为空 |
| $\mathcal P=\{p_1,\dots,p_m\}$ | 覆盖采样点集 | 本章 $m=7$（稳健档 8） |
| $m$ | 采样点总数 | 7（基准）/ 8 |
| $c(\mathcal P)$ | **覆盖半径** $\max_{G\in\Omega}\min_{p\in\mathcal P}\lVert G-p\rVert$ | m；须 $\le1000$ |
| $R$ | 采样点所在环的半径（环半径） | m；决策变量 |
| $\rho^*$ | 连续最优环半径 | $\rho^*=900\sqrt3=1558.8457$ m，$c_{\min}=900.0000$ m |
| $R^{\dagger}$ | 工程取整环半径（实现取值） | $R^{\dagger}=1550$ m，$c=900.0435$ m |
| $S_k$ | 频道 $k$ 的候选域（源可能位置集） | 初值 $\Omega$ |
| $D,\,R^{*}$ | 单频道定位区域的直径、最小包围圆半径 | m |
| $(c,R^{*})$ | 定位区域最小包围圆的圆心与半径 | m |
| $c_1,c_2$ | 一级、二级清除的目标点 | 取相应区域最小包围圆圆心 |
| $R^{*}_1,R^{*}_2$ | 一级、二级定位区域的最小包围圆半径 | m |
| $G$ | 干扰源真实位置 | m |
| $T$ | 虚拟时间 | s（评分量） |
| $N_c$ | 被清除源个数 | 个（评分量） |

---

## 4 问题分析

本问要在**源的位置、数量、频道、接收半径全部未知**的条件下做到"**一个不漏**"，且尽量快。三个子问题层层递进：

1. **如何在有限次检测下"证明"某频道没有源？** 这是本问最不同于直觉的地方：`no_signal` 只给出**负面证据**。由于 $R_{\rm eff}\ge1000$ m，一次 `no_signal` 排除了一个 $1000$ m 圆盘；要排除整个 $1800$ m 圆域，需要若干点的 $1000$ m 圆盘**覆盖**该圆域。这就是"覆盖巡视"的由来，也是保证性的唯一来源。
2. **如何定位并清除已发现的源？** 听到信号后，示向度给出一个 $\pm1^\circ$ 楔形约束（问题一）；两个不同地点的示向度即可交会出定位区域，再用**问题一**的直径/最小包围圆得到"必然包含源"的小区域，走到其中心清除。
3. **如何调度？** 覆盖点、第二示向度点、清除点三者若各跑一趟，行程会翻倍。故必须把它们**合并到同一条巡回路径**上（广义 TSP），并用"频道剪枝"把已了结的频道从测量集中永久移除。

**核心主线（四步）**：① 选覆盖构型（定理 1，见《问题三_公式.md》）→ ② 巡回 + 自适应按频道判空（算法 Q3，见 §6.1）→ ③ 两视线交会定位（复用问题一）→ ④ 两级清除链（定理 2，见《问题三_公式.md》，小证明见《问题三_小证明.md》§2）。

**关于时间构成的预判（决定优化重点）**：由附录 2，检测 $5$ s/次、切换 $1$ s/次，而移动按 $5$ m/s 计。7 点构型的巡回行程约 $9.7$ km（约 $1933$ s），而全部检测（即使 7 点全扫）也只有 $833$ s。**故虚拟时间由移动主导，优化重点是"减少采样点数与行程"，而不是"减少检测次数"。** 这一预判在 §7.5 的消融实验中得到实测确认。

---

## 5 模型的建立

### 5.1 覆盖保证：采样构型与覆盖半径

**判据.** 设采样点集 $\mathcal P$，若其覆盖半径满足 $c(\mathcal P)\le1000$ m（式 (1)，见《问题三_公式.md》），则**任何位置的源都会被至少一次检测到**（因为 $R_{\rm eff}\ge1000$），从而"在所有采样点都 `no_signal`"**等价于**"该频道无源"。这就把"证明没有源"化归为一个**纯几何的圆盘覆盖问题**。

**（1）构型族与环半径.** 取"原点 1 点 + 半径 $\rho$ 的正六边形环 6 点"（共 7 点）。由于 $\Omega$ 是圆盘（旋转对称），$c$ 只依赖 $\rho$，且有**解析式**（两段构成：中心点 Voronoi 内切半径 $\rho/\sqrt3$ 与外圈最坏点，闭式见《问题三_公式.md》式 (5)–(6) 与定理 1）。

它对 $\rho$ 取连续极小：$\rho^*=900\sqrt3=1558.8457$ m，$c_{\min}=900.0000$ m、余量 $100.0000$ m。因环点需落在 $5$ m 栅格上，工程取整为 $R^{\dagger}=1550$ m，对应 $c=900.0435$ m、余量 $99.9565$ m（与连续最优只差 $0.0435$ m）。域内 $5$ m 网格实测（`.review/q3_config.py` A 节）给出 argmin $\rho=1555$ m（$c=900.0082$ m），与解析式一致（网格单列时的离散误差上界为 $5/\sqrt2=3.54$ m，故此处只作一致性核对）。

**表 2　7 点构型的覆盖半径随环半径的变化（第 4–6 列为关键取值：解析连续值与域内网格实测一致；其余列为域内 5 m 网格实测）**

| 环半径 $\rho$/m | 1250 | 1350 | 1450 | **1550（工程取整）** | 1555（5 m 网格 argmin） | 1650 | 1700 | $\sqrt3\cdot1000=1732.05$ | 1750 | $900\sqrt3=1558.85$（连续最优） |
|---|---|---|---|---|---|---|---|---|---|---|
| 覆盖半径 $c$/m | 951.5 | 923.9 | 906.6 | **900.04** | **900.01** | 952.0 | 981.1 | **1000.00** | 1010.3 | **900.00** |
| 余量 $1000-c$/m | 48.5 | 76.1 | 93.4 | **99.96** | **99.99** | 48.0 | 18.9 | **0.00** | $-10.3$ | **100.00** |

**结论（定理 1）**　$c(\rho)$ 的**连续最优**为 $\rho^*=900\sqrt3=1558.8457$ m、$c_{\min}=900.0000$ m（余量 $100.0000$ m）；**工程取整值** $R^{\dagger}=1550$ m 对应 $c=900.0435$ m（余量 $99.9565$ m，与连续最优差 $0.0435$ m）。两条约束的相等点在 $\rho^*=900\sqrt3$ 处（$\rho=1550$ 时中心项 $894.89$ m、边界项 $900.04$ m，**并不相等**）。**$\sqrt3\cdot1000=1732.05$ m 只是"零余量"临界点**（$c=1000.0000$ m），并非最优：收紧环半径反而扩大余量，且环周长更短。定理 1 的陈述与成立条件见《问题三_公式.md》定理 1，解析式与网格一致性核对见《问题三_小证明.md》§1。

**（2）点数与余量的权衡.** 对 $n$ 个环点联合优化 $R$（`.review/q3_config.py` B 节）：

**表 3　点数—余量—行程的权衡（中心 + $n$ 环点；$R$ 为 10 m 网格 argmin）**

| 环点数 $n$ | argmin $R$/m（10 m 网格） | 覆盖半径/m | 余量/m | 环周长/m | 环上最短行程/s |
|---|---|---|---|---|---|
| 5 | 1460 | 1058.0 | $-58.0$ | 9173 | 1760 |
| **6（共 7 点）** | **1550（工程取整）** | **900.04** | **99.96** | 9739 | 1933 |
| 7（共 8 点） | 1440 | 801.9 | 198.1 | 9048 | 1839 |
| 8（共 9 点） | 1370 | 745.3 | 254.7 | 8608 | 1780 |

（环上最短行程按"自原点出发、遍历全部环点、不闭合"计，公式见《问题三_公式.md》式 (6)；$R$ 为 10 m 网格上的 argmin，6 环点行的连续最优为 $900\sqrt3=1558.85$ m。）

**结论**：**5 个环点不够**（最优也只有 $1058$ m 覆盖半径，无法保证）；**6 个环点是保证检测的最少点数**。本章取 **7 点构型（工程取整 $R^\dagger=1550$ m，$c=900.04$ m，余量 $99.96$ m）为基准**，并在 §7 中用 8 点构型（$R=1440$ m，余量 198 m，行程反而更短）作稳健对照。**注意"8 点余量更大且行程更短"这一反直觉现象**：环半径收缩带来的周长收益超过了点数增加的开销。

**（3）按"时间"而非"覆盖半径"重标定构型（2026-09-12 新增，v2 策略采用）.** 上述最优都是在**最小化覆盖半径 $c$** 的意义下取的；但巡检的时间成本是**开链行程**（采样点开链 $=\rho+(k-1)\cdot2\rho\sin(\pi/k)$，对 $\rho$ 单调增）加**逐点判空测量**（每个空频道在每个采样点测一次，$|P|\times6$ s）。约束 $c(k,\rho)\le1000$ 的可行 $\rho$ 区间是**双侧**的（$k=6$ 时为 $[1122.9,1732.05]$ m），而两项时间成本都随"收紧"而下降，故**时间最优在可行窗口的下端**，不是 $900\sqrt3$。对 $k$ 与 $\rho$ 联合扫描（目标 $=$ 开链$/5+(20-\bar n)\cdot6\cdot(k+1)$，$\bar n=13$）得：

- 余量 $63.5$ m 档：最优为 **$k=8$ 环点、$\rho=1028.5$ m**（$c=936.5$ m），开链 $6539$ m、判空 $378$ s、合计 **$1686$ s/局**；
- 余量 $100$ m 档（与表 3 的基准**同余量**）：最优为 **$k=8$、$\rho=1083.7$ m**（$c=900.03$ m），合计 **$1756$ s/局**。

对照基准（$k=6,\rho=1550$：开链 $9300$ m $=1860$ s、判空 $294$ s、合计 $2165$ s/局），两档分别省 $479$ 与 $409$ s/局。一般覆盖半径 $c(k,\rho)$ 的解析式、窗口端点公式与数值表见《问题三_小证明.md》§1bis（命题 1b/1c）。另一附带收益：$k=8$ 把"两次视线可得性"在 $r=1200$ m 处由 1 个点在程提升到 3 个，减少了补测第二视线的次数。**清源链证书**在新构型下已复核：$R^{*}_2\le2.19$ m、$\lVert c_2-G\rVert\le2.03$ m（$N=2000$），仍然"必然命中"；而"只在区域中心复测"在 $k=8$ 下最大 $R^{*}_2=20.47$ m $>20$ m，故 $\pm100$ m 垂直偏移**不可省**（v2 改为按运行期 $R^*\le18$ m 证书条件化）。

**扫描模型的实测校验与最终选择（2026-09-12）.** 上表的模型只计"行程 $+$ 逐点判空测量"，**漏算了"环内移会拉长对 $r>1000$ m 源的逼近支路"**（源在 $r=1700$ m 时从 $\rho=1028$ 的环出发要多走约 $270$ m，能同时看到该源的采样点也更少）。100 局实测：$k=8,\rho=1028.5$ 为 $313.9$ s/源、**差于** $k=6,\rho=1300$ 的 $299.4$（差 $1.3\sigma$，噪声内）；$\rho=1400$ 为 $304.8$（更差）。**故论文采用 $k=6$、$\rho=1300$ m**（$c=936.5$ m、余量 $63.5$ m）。另一次独立 A/B（2026-09-12 晚，**不采用**）：中心 $+$ **7** 环点 @1050 m（$c=967.9$ m、余量 $32$ m、开链 $6517$ m）100 局均值 $298.7$ s/源，与 v3 的 $299.4$ **同种子配对差 $-0.7$ s（$t=-0.43$、$52/100$）即无差异**，但**动作数由 $124.3$ 增到 $135.5$（$+9\%$）**——官方模拟器的幂等记录上限/429 预算本地未建模，增加动作是纯风险，故维持 $k=6$@1300。数据：`results/plan_q3alt1050_20260912_213351.csv`、`results/plan_q3v3_20260912_201000.csv`；详见《问题三_证明笔记.md》§8.7。

### 5.2 自适应按频道判空

**机制.** 为每个频道 $k$ 维护候选域 $S_k\subseteq\Omega$（初值 $\Omega$）。在点 $p$ 对该频道得到 `no_signal` 即执行候选域更新 $S_k\leftarrow S_k\setminus B(p,1000)$（式 (2)，见《问题三_公式.md》）；当 $S_k=\varnothing$ 时**该频道被证明为空**。一旦在 $p$ 听到信号，该频道转入**清源流程**；此后它**不再需要任何覆盖测量**（因每频道至多一源）。该机制的判空保证性论证见《问题三_小证明.md》§4。

**检测环节的省时（理想化核算）.** 若"一旦听到即退出覆盖集"，则只有"从未听到"的频道需要在**全部**采样点上测完。按此理想化模型模拟 2000 局（源数 $10\sim16$、位置面均匀，`.review/q3_config.py` C 节）得表 4。

**表 4　检测预算的"理想化"对照（2000 局平均；假设"首次检出即退出覆盖集"）**

| 方案 | 平均检测次数/局 | 检测虚拟时间 | 相对 |
|---|---|---|---|
| 每点扫全部 20 频道（7 点） | 140 | 833 s | 基准 |
| 自适应按频道判空（理想化） | **42.2** | **247 s** | 省 70.4% |

**但"检测环节的省时"不等于"总时间的省时".** 表 4 是**理想化**上限——它假设"首次检出即可停止测量该频道"。**实际策略需要两条视线才能交会定位**，故被检出的频道还要在后续采样点上再测一次取第二示向度；且**判空频道无论如何都必须在全部 7 个点上测完**。因此**真实实现的节省远小于 70%**：实测动作条数 $167.8\to146.7$（以关闭自适应为基准计省 $12.6\%$；以完整策略为基准，关闭后动作数增加 $14.3\%$），虚拟时间 $6180.8\to6119.6$ s（省 $1.0\%$）。原因是**检测时间只占虚拟时间约 $10\%$，总时间由移动距离主导**。表 4 保留，作为"检测环节本身能省多少"的理论刻画；引用时**不得**把 70% 当作总时间收益（消融实测见 §7.5，两个口径的辨析见《问题三_小证明.md》§4）。

### 5.3 两视线交会定位与退化识别

**定位.** 对频道 $k$ 收集到两条以上视线 $\{(S_i,\theta_i)\}$ 后，取其楔形交（问题一的无框半平面交），得顶点集、直径 $D$、最小包围圆 $(c,R^*)$（式 (3)，见《问题三_公式.md》）。真源必在该区域内（问题一的集合隶属结论），故 $\lVert c-G\rVert\le R^*$。

**退化识别（关键）.** 若某对视线满足 $d_{\rm circ}(\theta_i,\theta_j)\le2\delta$（式 (4)，见《问题三_公式.md》），则由问题一定理 1 该对给出的区域**无界或为空**，交会定位失效。数值上这类构型会把"区域"拉成上万米的长条（`.review/q3_discard_check.py` 实测：交会角 $\alpha=2.1^\circ>2\delta$、$\beta=-0.35^\circ$ 时 $D=R^*=20248.1366$ m、误用误差 $9874.0529$ m；$\alpha=2.00^\circ$ 时无界）。**因此必须丢弃交会角 $\le2\delta$ 的视线对**（定理 3，见《问题三_小证明.md》§3），并在交会角过小时主动到"垂直视线方向"的新位置补测一条视线（见 §5.4）。**该规则必要但不充分**：$2\delta<\alpha\lesssim3^\circ$ 时 $D$ 仍可达万米量级（同一 $\alpha=2.1^\circ$ 下 $D$ 随横向位置由 $15749$ 变到 $23247$ m），真正的失败模式还包括 $\alpha\to180^\circ$，故定位必须按"取最小 $D$ 的良态点对 + 垂直补测 + 射线兜底"处理。

**第二示向度的零额外行程做法.** 覆盖巡回本身依次经过全部 7 个采样点。若在 $p_1$ 听到频道 $k$，只需在巡回的**后续采样点**顺带再测一次 $k$（本策略在取得 2 条视线前的每个巡回点都会测该频道），这条移动本就是巡回的既有行程，**边际成本仅 $6$ s**（检测 $5$ s + 换频 $1$ s）。定位时**在全部已测视线的良态点对中取区域直径 $D$ 最小者**（`_estimate`，见 §5.4），并不固定使用某一对点；若只有 1 条视线，则到垂直视线方向补测（§5.4）。

### 5.4 单源清除链

清除的成功条件是 $\lVert p-G\rVert\le20$ m（与朝向无关）。故只要能把 $G$ 锁定到半径 $20$ m 内即**必然命中**。

**两级清除链（定理 2）.** 一级定位规则：在覆盖巡回已测得的**全部视线**中，丢弃所有 $d_{\rm circ}\le2\delta$ 的点对，在剩余的**良态点对中取区域直径 $D$ 最小者**得 $(c_1,R^*_1)$（`dog_plan.py::_estimate`；若已有 $\ge3$ 条视线且全体交会的最小包围圆更小，则取全体交会）。走到 $c_1$ 先尝试 `/clear`；若失败，在 $c_1$ 复测一次（第 3 条视线），再在 $c_1$ 沿**垂直视线方向偏移 100 m** 处复测一次（第 4 条视线，式 (7)），重新交会得 $(c_2,R^*_2)$，走到 $c_2$ 执行 `/clear`。构造式、成立条件与数值证书见《问题三_公式.md》定理 2 与《问题三_小证明.md》§2。

**数值证书**（`.review/q3_clear_chain_check.py`，$N=4000$；$G$ 在半径 1800 m 圆域均匀、误差 $\varepsilon\sim U(-1^\circ,1^\circ)$）：

**表 5　清源链的两种口径对比（4000 局）**

| 口径 | 一级 $R^*_1$（中位/最大） | 二级 $R^*_2$（中位/最大） | $\lVert c_2-G\rVert$（中位/最大） | $R^*_2\le20$ m 比例 |
|---|---|---|---|---|
| 实现口径：只测 $c_1$（不再补测） | 28.062 / 39.603 m | — / **19.6674 m** | — | $100\%$（余量仅 $0.33$ m） |
| **实现口径：$c_1$ 复测 + 垂直偏移 100 m** | 28.062 / 39.603 m | **1.8059** / **2.1383 m** | **0.8363** / **1.9029 m** | **$100\%$** |
| 文字口径（第二条视线取相邻环点，间距 $1550$ m）：$c_1$ + 垂直偏移 | — | $1.846$ / **87.649 m** | — / $59.333$ m | $99.025\%$ |

**实现口径**下 $R^*_2\le2.1383$ m、$\lVert c_2-G\rVert\le1.9029$ m、$R^*_2\le20$ m 的比例 $100\%$，故走到 $c_2$ 清除**在样本内必然命中**（清除半径 $20$ m）。**"垂直偏移"这一步不是严格必需、而是稳健措施**：实现口径下只测 $c_1$ 的最大 $R^*_2=19.6674$ m $<20$ m，该步把余量从 $0.33$ m 提到 $17.9$ m。**风险来自 $\alpha\to180^\circ$**（源近乎落在两站连线上）：文字口径下 $0.975\%$（$39/4000$）的失败样本**全部落在 $\alpha\in[175.30^\circ,179.96^\circ]$**（其中 $69.2\%$，即 $27/39$，超过 $178^\circ$），**无一例来自 $\alpha\le2\delta$ 一端**；实现口径取"$D$ 最小的良态点对"会自动避开近反向对，另有 `_acquire_lines()` 的垂直方向补测与 `_ray_march_clear()` 射线兜底。**旧口径**（`.review/q3_config.py` D/D2 节）的 $R^*_2\le2.64$ m、只测 $c_1$ 最大 $31.59$ m、分档 $1.76/2.03/2.39/2.64$ m 仅作对照，不作证书。

**动作序列**（典型）：巡回点测到信号（5 s）→ 下一点顺带复测（6 s）→ 走到 $c_1$ 清除／复测 → 垂直偏移 100 m 复测 → 走到 $c_2$ 清除。典型 **4 次检测 + 1 次清除**；若出现 `near` 或 $R^*_1\le20$ m 可直接就地清除。

**兜底（退化构型）**：若四视线后仍不能清除（交会角持续退化），依次启用 ①以最后估计为中心、**28 m 间距**的局部网格清除（$28$ m 间距保证任一格点距真实源 $\le14\sqrt2=19.8<20$ m），②沿**最优那条示向度射线**以 40 m 步长、横向 $0/\pm25$ m 的射线推进清除（源必在该射线上：$1500$ m 处横向偏差 $\le1500\tan1^\circ=26$ m）。两者均只依赖"`/clear` 与朝向无关、失败仅 3 s"，故对任何退化构型都收敛。

### 5.4bis 与第一问、第二问结论的接口（2026-09-12 新增）

问题三/四的清源链不是独立设计的，它**直接复用第一、二问的三条结论**；把接口写明，既避免重复推导，也让"为什么必须是三级链路"有定量依据。

**（1）复用第一问：交会区域与 $R^*$ 证书.** 定位区域用第一问的**无框半平面交**（边界线两两求交 + 全约束筛选 + 并入楔形顶点）得到顶点集 $\mathrm{vert}(R)$；清除位置取 $R$ 的**最小包围圆圆心** $c$（第一问 `region_stats`，点对 + 三重点枚举）。由此得到**命中证书**：

$$G\in R,\quad R\subseteq B(c,R^*)\ \Longrightarrow\ \lVert c-G\rVert\le R^*\ \Longrightarrow\ \text{若 }R^*\le18\ \text{m 则 }/clear\text{ 必然成功}$$

（$20$ m 命中半径留 $2$ m 余量。）其**理论依据是第一问的 Thales 判据**（定理 4）：直径圆覆盖 $R$ $\iff$ $\max_{p\in\mathrm{vert}(R)}(p-a)\cdot(p-b)\le0$，即只需在顶点上检验，故"是否已被 $20$ m 圆覆盖"可由顶点集判定。实现上：$R^*\le18$ m 直接在 $c$ 清除（免第 3、4 条视线），否则才走偏移复测。**注意 $R^*=D/2$ 只在直径圆覆盖时成立**，不得用 $D/2$ 代替 $R^*$（第一问的旧勘误即此处）。

**（2）复用第二问：补第二视线的测点位置（两层，第二层才用第二问的最优设计）.** 某频道只有 1 条视线时，补第二视线的候选按**两层**组织：

| 层 | 测点 | 到锚点距离 | 接收保证 | 交会角 |
|---|---|---|---|---|
| **第一层（先用）** | $S_1+a\hat u+w\hat n$，$(a,w)=(500,\pm250),(300,\pm250),(700,\pm250),(200,\pm300)$ | $559\sim727$ m | **有**（见下） | $\tan\alpha=w/(d-a)\approx25^\circ$ |
| 第二层（兜底） | $S_1+\rho(\cos(\theta_1\pm\psi),\sin(\theta_1\pm\psi))$，$(\rho,\psi)=(1000,38^\circ)$ 及 $(800,38^\circ),(700,34^\circ),(600,26^\circ)$ | $600\sim1000$ m | 有（透镜 $\mathcal F_Q$） | $\alpha\approx50^\circ\sim70^\circ$ |

**第一层为什么也有接收保证（此前我误判为"无保证"，此处更正）**：源必在射线上、距锚点 $d\le$ 其有效半径 $R$（能被锚点听到）。取 $p=S_1+a\hat u+w\hat n$ 则 $\lVert p-G\rVert^2=(d-a)^2+w^2$，在 $d\in[0,R]$ 上的上界由端点给出；代入**保证下界** $R=1000$ m 得 $\lVert p-G\rVert\le587$ m $\ll1000$ m，**必在程**。所以第一层既便宜又安全。

**第二层的依据是第二问的两条结论**：① $(\rho,\psi)=(1000,38^\circ)$ **最小化期望交会直径**（$\mathbb E[D]=55.63$ m、最坏 $D=122.815$ m、$R^*=61.408$ m），比侧向设计（$\mathbb E[D]\approx119$ m）小约一倍；② 这些点全部落在第二问的**接收保证透镜** $\mathcal F_Q$ 内（到源最近可能位置 $S_1+5u$ 与最远可能位置 $S_1+1500u$ 都 $\le1000$ m），故**无论源落在射线何处（$r_1\le1500$ m）都能听到**。$\rho<600$ 时透镜要求 $\psi<27^\circ$（$\rho=500$ 只能 $\psi=0$，交会角退化），故候选只在 $\rho\ge600$ 上取。**第二层的作用是兜底**：当第一层候选全部 `no_signal`（例如官方模拟器 $R_{\min}=1000$ m 且源恰在射线远端）或仍构不成良态对时才启用。

> **实测（100 局配对，2026-09-12）：把第二层放到第一层会使平均虚拟时间 $+175.8$ s/局（$+4.5\%$，100 局中 66 局更慢）**——第二问的最优设计在**几何**上最优（初始交会区域最小、后续复测次数由 $1.67\to1.06$ 次/源），但代价是第二测点离锚点约 $1000$ m；本地模拟器每源有效半径 $U[1000,1500]$（均值 $1250$ m），第一层已足够。故最终取"第一层优先、第二层兜底"。这条对照同时说明：**第二问的"最优"是"交会区域最小"意义下的最优，与"总时间最小"不是同一个目标**（与问题三构型的"$c$ 最小 ≠ 时间最小"是同一类陷阱）。

**（3）复用第二问：$D$ 的下确界解释了"为什么必须有第三个测点".** 第二问证明：已知 $r_1$、只用两条视线时，$D$ 的下确界是 $r_1\sin\delta/\cos2\delta\approx17.46$ m（$r_1=1000$ m，**不是** $2\tan\delta\,r_1\approx34.91$ m，后者只是一阶近似式的退化值），且只在三角形退化时逼近。$17.46$ m 与 $20$ m 命中半径只差 $2.5$ m、**没有余量**，故"仅两条视线"不足以保证命中，第三个测点（$c_1$ 复测／偏移复测）是**必要**的——这解释了清源链为什么是三级而不是两级。

**（4）第三个测点的偏移量可以显著收小（2026-09-12 新增）.** `.review/q3_offset_check.py` 在**部署几何**（$k=6$、$\rho=1300$ m，$N=3000$）下复核：

| 偏移 $w$/m | $0$（只在 $c_1$ 复测） | $20$ | **$30$（本策略采用）** | $50$ | $70$ | $100$（原正文） |
|---|---|---|---|---|---|---|
| $R^*_2$ 最大/m | $19.750$ | $2.495$ | **$2.019$** | $1.816$ | $1.910$ | $2.227$ |
| $\lVert c_2-G\rVert$ 最大/m | $17.633$ | $1.751$ | $1.485$ | $1.447$ | $1.615$ | $1.991$ |

即 $w=0$ 时 $R^*_2\le20$ m 只剩 $0.25$ m 余量（$13/3000$ 样本超过证书阈值 $18$ m），而 $w=30$ m 已把 $R^*_2$ 压到 $2.02$ m——**$100$ m 偏移冗余 3 倍以上**。本策略取 $w=30$ m：第三个测点更靠近源，**更不易落出源的有效半径**（对官方模拟器 $R_{\min}=1000$ m 更稳健）。**诚实说明**：由于 $R^*\le18$ m 证书的过滤作用，该偏移在实测中只在约 $0.4\%$ 的局触发，故本项**对时间几乎无影响（$\approx0.1$ s/源），价值在稳健性**。

### 5.5 多源调度：巡回与清源合并

- **频道剪枝**（收益最大）：已清除或已判空的频道永久移出测量集。
- **巡回规划**：把"覆盖采样点"与"各源的清除点"统一为一条路径，用**最近邻 + 2-opt**（点数 $\lesssim30$，秒级求解）。
- **清源顺序**：每轮选"当前估计最近的待清频道"先清（降低空跑）。
- **终止条件**：每个频道"已清除"或"已证明为空"；若清除数达 $16$（源数上界）可立即 `/exit`。
- **评分口径（优先级）**：**先保证清除比例 $=1$，再最小化平均时间**（式 (8)，见《问题三_公式.md》）。因指标是"总时间 ÷ 被清除个数"，少清一个源会"降低"平均值，故任何"为省时间而放弃难清源"的做法都判为不合格。

---

## 6 算法设计与复杂度

### 6.1 主算法

```
算法 Q3：全向源的搜索—定位—清除
输入 : 圆域 Ω、频道集 K={1..20}、覆盖构型 P（7 点，R=1550）
输出 : 全部源的清除记录、虚拟时间

 1  构造巡回路径 tour ← NN(P) + 2-opt，起点 = 原点
 2  状态：每个频道的视线集 dets[k] ← ∅；cleared ← ∅；empty ← ∅
 3  FOR p IN tour:                                  # 覆盖巡视（自适应）
 4      FOR k IN K \ (cleared ∪ empty ∪ {已取到2条良态视线者}):
 5          (res, svd) ← /measure(p, k)
 6          IF res = near: /clear(p,k); cleared ← cleared ∪ {k}
 7          ELIF res = direction: dets[k] ← dets[k] ∪ {(p, svd)}
 8          ELSE: (no_signal)  # S_k ← S_k \ B(p,1000)，仅作记账，判空在步骤 10
 9  巡回结束：对每个 k
10      IF dets[k] = ∅:  k 判空（因 c(P) ≤ 1000 m，覆盖成立）        # 保证性来源
11      ELIF 无良态视线对（交会角 > 2δ）: 到垂直方向补测一条视线
12  WHILE 存在待清频道:
13      k ← 估计位置离当前位置最近的待清频道
14      (c1,R*1) ← 由 dets[k] 作无框半平面交 → 最小包围圆
15      /clear(c1, k);  IF 成功: 记 cleared, CONTINUE
16      (res3,svd3) ← /measure(c1,k);  dets[k] ← dets[k] ∪ {(c1,svd3)}
17      c3 ← c1 + 100 · n̂(视线方向); (res4,svd4) ← /measure(c3,k)
18      dets[k] ← dets[k] ∪ {(c3,svd4)};  c2 ← 四视线交会的最小包围圆圆心
19      /clear(c2, k)
20      IF 仍失败: 局部 28 m 网格清除 → 沿最优射线推进清除（兜底）
21  /exit
```

### 6.2 复杂度

设采样点数 $m$（$m=7$ 或 $8$），频道数 $20$。

| 环节 | 复杂度 | 说明 |
|---|---|---|
| 巡回规划 | $O(m^3)$（2-opt 最坏） | $m\lesssim30$，秒级 |
| 覆盖检测次数 | $O(m\cdot20)$ 上界；自适应后平均 $\approx42$ 次 | 实测 |
| 单频道定位 | $O(1)$ | 视线数 $\le4$，半平面交 $O(n^3)$、$n\le4$ |
| 清源链 | $O(1)$ 次 `/clear` + 兜底有界 | 兜底封顶 120 次清除 |
| **总虚拟时间** | 由**移动距离**主导 | 见 §7 |

---

## 7 数值实验与验证

所有结果由两路复算：**几何量**用 `.review/q3_config.py`（域内 5 m 网格 / 4000 局 Monte Carlo），**策略性能**用 `本地测试器/simulator/batch_plan.py`（本地自研模拟器，接口与官方一致）。

### 7.1 正式测试结果（表 1）

> **数据来源说明**：按题目要求，表 1 应填报**官方模拟器**三次正式测试的案例编码与结果。本机当前仅有自研本地模拟器（接口协议与官方一致，但案例编码格式不同、且无联网登录），故下表先用**本地模拟器**按"问题三、全向源"配置跑出的 3 局结果占位，并**保留官方结果替换位**。三局的源数与真实位置不同（由模拟器随机生成），机器狗决策过程完全一致。

**表 1　问题三正式测试结果（本地模拟器，当前为本地自研模拟器占位、待官方模拟器正式测试后替换）**

| 测试 | 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|---|
| 测试 1 | P3-93AD699F | 11 | $455.5$ s | $0.5$ s |
| 测试 2 | P3-EF7A58F9 | 16 | $406.6$ s | $0.6$ s |
| 测试 3 | P3-D9DCBD7A | 11 | $614.7$ s | $0.6$ s |

三局的**被清除源个数比例均为 $100\%$**（$11/11$、$16/16$、$11/11$）；程序运行时间远低于 $1200$ s 上限（真实时间不是瓶颈，评分量是虚拟时间）。

### 7.2 批量演练统计

用与正式测试完全相同的程序，在本地模拟器上跑 **100 局**（种子 $1\sim100$，每局重新随机生成源）：

**表 6　问题三批量演练（100 局，本地模拟器）**

| 指标 | 均值 | 最小 | 最大 |
|---|---|---|---|
| 被清除源个数比例 | $100.0\%$ | $100\%$ | $100\%$ |
| **平均定位清除时间** | $\mathbf{488.5}$ s | $346.8$ s | $705.3$ s |
| 虚拟时间 | $6119.6$ s | $4375.1$ s | $8527.6$ s |
| 程序运行时间（真实） | $0.68$ s | — | $1.80$ s |
| 动作条数 | $146.7$ | — | — |

（表 6 取数：`本地测试器/simulator/results/plan_mixed_20260912_120553.csv` 的 P3/q3 共 100 局，全部 $100/100$ 全清；取数口径见同目录 `README.md`。）

**与保底基线（`dog.py`，17 点扫描 + 局部网格兜底）的对照**：

| 方案 | 平均定位清除时间/s | 虚拟时间/s | 清除比例 | 动作数 |
|---|---|---|---|---|
| 保底基线 `dog.py`（同 100 局） | $2757.7$ | $34235.3$ | $100\%$（按种子并集） | 约 $2000$ |
| **本策略（覆盖+自适应判空+清源链）** | **$488.5$** | **$6119.6$** | $100\%$ | **$146.7$** |
| **提升** | **$5.6\times$** | **$5.6\times$** | — | $13.6\times$ |

基线取数（`batch_20260912_113329.csv`，口径见 `results/README.md`）：批次内 P3 为 $98/100$ 全清（2 局为环境中止：seed $67$、$79$），已用单种子复跑 `batch_20260912_113753.csv`（seed $67$）、`batch_20260912_113808.csv`（seed $79$）确认全清，故**按种子并集**清除比例 $100/100$；平均定位清除时间 $2757.7$ s、虚拟时间 $34235.3$ s（均按全清局计）。即：在**同样保证 $100\%$ 清除**的前提下，平均定位清除时间由 $2757.7$ s 降到 $488.5$ s。

**表 6b　优化策略（v2：统一目标集 + 条件化清源链，2026-09-12）**

| 指标 | 基础策略（v1，表 6） | **优化策略（v2）** | 变化 |
|---|---|---|---|
| 被清除源个数比例 | $100\%$ | $100\%$（$100/100$） | — |
| **平均定位清除时间** | $488.5$ s | **306.3** s | $\mathbf{-37.3\%}$ |
| 虚拟时间 | $6119.6$ s | $3829.0$ s | $-37.4\%$ |
| 动作条数 | $146.7$ | $124.3$ | $-15.3\%$ |
| 采样构型 | 中心 + 6 环点 @1550 m | 中心 + 6 环点 @1300 m（$c=936.5$ m） | 见 §5.1(3) |
| 清源顺序 | 贪心最近邻（巡检与清源各一趟） | 统一目标集 + 在线开链（NN+2-opt+Or-opt）前瞻 | 见 §6 |
| 清源链 | 固定 $c_1$ + $\pm100$ m 偏移 | 按运行期 $R^*\le18$ m 证书条件化 | 保证性不降 |

（表 6b 取数：`results/plan_q3v2_20260912_191336.csv`，`--cfg q3v2 --dog-module dog_plan_v2 --workers 1 --min-interval 0.02`，100 局零中止、全部 $100/100$ 全清；最小 $232.0$、最大 $383.7$ s。**表中 v2 的构型即最终采用档（$\rho=1300$ m、$k=6$，与 v1 同为 7 点）**；更紧的 $k=8,\rho=1028.5$ 档实测 $313.9$ s/源、略差（差 $1.3\sigma$，在噪声内），已作对照记录，见《问题三_证明笔记.md》§4 坑 10。）

**表 6c　优化策略第二轮（v3：接入第一/二问结论，本章最终口径）**

| 指标 | v2（第一轮） | **v3（第二轮，最终）** | 变化 |
|---|---|---|---|
| 被清除源个数比例 | $100\%$ | **$100\%$（$100/100$，零中止）** | — |
| **平均定位清除时间** | $306.3$ s | **$299.4$ s** | $-2.3\%$（相对 v1 为 $\mathbf{-38.7\%}$） |
| 虚拟时间 | $3829.0$ s | $3741.1$ s | $-2.3\%$ |
| 动作条数 | $124.3$ | $124.3$ | $0$ |
| 补第二视线 | 单层"沿射线前进 $+$ 侧偏" | **两层**：沿射线优先（$559\sim727$ m、到源距离上界 $587$ m）$+$ 第二问透镜点 $(1000,38^\circ)$ 兜底 | 见 §5.4bis(2) |
| 第三个测点偏移 | $100$ m | **$30$ m**（$R^*_2\le2.019$ m） | 见 §5.4bis(4) |
| 环半径 | $1300$ m | $1300$ m（同 100 局 $\rho=1400$ 为 $304.8$，更差） | 见 §5.1(3) |

（表 6c 取数：`results/plan_q3v3_20260912_201000.csv`，`--cfg q3v3`，100 局零中止、$100/100$ 全清；最小 $222.6$、最大 $378.4$ s。**比值口径（2026-09-12 下界第 5 版修正后）**：实测 $/$ 严格档下界 $=299.4/302.0\approx0.99$、实测 $/$ 松弛档 $=299.4/297.0\approx1.01$——这是「随机实例均值 $\div$ 最坏实例下界」两种口径，**不可读作"已最优"**；应引用**同 $n$ 口径**（$n\approx13$：$299.4/248.9\approx1.20$）或**同实例口径**（$\approx1.93$），且两者仍**未含**"巡检 $+$ 逐点判空"这类下界不覆盖的工程成本。详见《问题三_证明_下界.md》§5.1。）

**改进的来源（按 `.review/q3q4_attrib.py` 的逐项归因，30 局平均，s/局）**：

| 成本项 | v1 | v2 | 说明 |
|---|---|---|---|
| 巡检行程 | $1860.0$ | $946.9$ | 构型收紧 + 与清源路线合并后不再单独跑一趟 |
| 源间/逼近行程 | $2876.3$ | $1569.5$ | 由贪心最近邻改为开链前瞻（该实例最短开路约 $1650$ s） |
| 清源链内行程 | $572.1$ | $632.7$ | 条件化后个别源改走兜底 |
| 检测（$5$ s $\times$ 次数） | $627.3$ | $525.3$ | — |
| 频道切换 | $120.5$ | $98.3$ | — |
| 清除动作（$5$ s 成功 / $3$ s 未命中） | $76.3$ | $77.8$ | — |
| **合计** | $6132.6$ | $3850.5$ | $-37.2\%$ |

**口径**：$T=D/5+5M+S+5C_{\rm ok}+3C_{\rm fail}$（定理 0）的逐动作核对残差 $\le10^{-2}$ s；"巡检行程"按"动作位置是否落在构型采样点上"分类，故合并路线下该列的含义是"落在采样点上的那部分行程"，总行程才是可比量（v1 $5308\to$ v2 $3047$ s）。

### 7.3 覆盖构型与余量

见 §5.1 表 2、表 3：7 点构型的**连续最优**环半径 $\rho^*=900\sqrt3=1558.85$ m 对应覆盖半径 $900.0000$ m（余量 $100.00$ m），**工程取整值** $R^\dagger=1550$ m 对应 $900.0435$ m（余量 $99.96$ m）；对照的 $\sqrt3\cdot1000=1732.05$ m 构型覆盖半径恰为 $1000.0000$ m（**零余量**，工程上不可接受）。8 点构型（$R=1440$ m）余量 $198$ m 且环上行程更短。

### 7.4 清源链保证性

见表 5（**实现口径**，4000 局）：只测 $c_1$ 时二级半径最大 $19.6674$ m；加垂直偏移 $100$ m 后 $R^*_2$ 最大 $2.1383$ m、$\lVert c_2-G\rVert$ 最大 $1.9029$ m，$R^*_2\le20$ m 的比例 $100\%$，**在样本内必然命中**。若改用"第二条视线固定取相邻环点"的文字口径（与实现不符），最大 $R^*_2$ 达 $87.649$ m、仅 $99.025\%\le20$ m，且失败样本全部落在 $\alpha\in[175.30^\circ,179.96^\circ]$（$69.2\%>178^\circ$）——实现取"最小 $D$ 的良态点对"正是为规避该模式。

### 7.5 消融实验

用同一程序、同一批种子（P3，种子 $1\sim50$）关闭"自适应按频道判空"（`batch_plan.py --no-adaptive`，即每点扫全部 20 频道）复算：

**表 7　消融实验：关闭"自适应按频道判空"（P3，50 局）**

| 配置 | 平均定位清除时间/s | 虚拟时间/s | 动作条数 | 清除比例 |
|---|---|---|---|---|
| 完整策略（自适应） | $488.5$（100 局） | $6119.6$ | $146.7$ | $100\%$ |
| **关闭自适应**（每点扫全部 20 频道） | $489.9$（50 局） | $6180.8$ | $167.8$ | $100\%$ |
| 差异 | $+0.3\%$ | $\mathbf{+1.0\%}$ | $\mathbf{+14.3\%}$ | — |

（表 7 取数：`本地测试器/simulator/results/plan_mixed_20260912_123222.csv` 的 P3/q3 共 50 局；其中 $167.8$ 是**关闭自适应后的总动作条数（含清除动作）**，**不是**检测次数 $140$。）

**结论（须如实报告）**：自适应判空只贡献约 **$1\%$ 的虚拟时间**收益（动作条数由 $146.7$ 增至 $167.8$，相对完整策略增加 $14.3\%$，相对关闭自适应的 $167.8$ 则省 $12.6\%$）。这与 §5.2 的"理想化 70%"不矛盾但**不可混用**：① 检测时间只占虚拟时间约 $10\%$，其余由移动主导；② 实际策略需**两条视线**才能定位，故频道不能在首次检出时就退出；③ 判空频道必须在全部采样点测完。因此本策略的**主要收益来自"采样构型 + 巡回合并"，而非自适应判空**——这一结论是把"检测环节的省时"与"总时间的省时"分开看待才得到的（两口径论证见《问题三_小证明.md》§4）。

**另两项机制**（关闭后的代价为解析估计，未单独跑批）：关闭"巡回与清源合并"需为每个源专程补测第二示向度（约 $+1550$ m/源 $\Rightarrow$ $+310$ s/源）；关闭"频道剪枝"则已了结频道被反复测量，检测预算回到"全扫"量级。

**表 8　v2 三项改动的消融（P3，同 30 局；脚本 `.review/q3q4_attrib.py`）**

| 配置 | 平均定位清除时间/s | 相对 v2 全量 |
|---|---|---|
| v1（论文现口径：贪心 + 固定清源链 + 环 1550、$k=6$） | $479.7$ | $+59.4\%$ |
| **v2 全量**（统一目标集 + 在线开链前瞻 + 证书条件化 + 环 1300、$k=6$） | **$300.9$** | — |
| v2 但清源顺序退回贪心（关开链前瞻） | $307.8$ | $+2.3\%$ |
| v2 但清源链恒做 $\pm100$ m 偏移（关证书条件化） | $303.1$ | $+0.7\%$（噪声内） |
| v2 但构型换回 v1 的环 1550、$k=6$ | $319.5$ | $+6.2\%$ |

（表 8 与表 6b 的 100 局结论一致：$306.3$ vs v1 的 $488.5$。注意 `dog_plan_v2.py` 的 `--no-merge` 只切换**排序启发式**，"统一目标集（发现即清源）"这一结构改动体现在 $v1\ 479.7\to$ v2 换回旧构型的 $319.5$ 之间——即**结构改动贡献约 160 s/源，构型收紧再贡献约 19 s/源**。）

### 7.6 验证结论

1. **保证性**：7 点构型使 $c(\mathcal P)=900.04$ m $<1000$ m $=R_{\rm eff}$ 下界，故"全部采样点 `no_signal` $\Rightarrow$ 该频道无源"严格成立；由定理 2，实现口径下 $R^*_2\le2.14$ m、$\lVert c_2-G\rVert\le1.91$ m，清源链在样本内必然命中。**100 局全部 $100\%$ 清除**，与理论一致。
2. **效率**：平均定位清除时间 $488.5$ s，比保底基线快 $5.6$ 倍，且动作条数减少约 $13.6$ 倍（对现实时间与幂等记录上限都更安全）。
3. **稳健性**：退化构型（交会角 $\le2\delta$）由 §5.3 的识别规则与 §5.4 的兜底处理；本地 100 局未出现异常终止。

---

## 8 结论

1. **候选区域的"证明"问题化归为圆盘覆盖**：因 $R_{\rm eff}\ge1000$ m，"所有采样点都听不到"等价于"该频道的 $1000$ m 无信号圆盘覆盖整个圆域"。取**中心 + 6 环点**，连续最优环半径 $\rho^*=900\sqrt3=1558.85$ m（$c_{\min}=900.0000$ m）、工程取整值 $R^\dagger=1550$ m（$c=900.04$ m，余量 $99.96$ m），是保证检测的**最少点数**构型；$\sqrt3\cdot1000=1732.05$ m 只是零余量临界值。
2. **检测预算靠"自适应按频道判空"压缩，但其收益须与移动代价分开评估**：把"检测环节"单独看，自适应把检测次数由 $140$ 降到 $42.2$（理想化上限，省 $70.4\%$）；但在**完整策略**中实测只省 $1.0\%$ 虚拟时间（动作条数相对完整策略增加 $14.3\%$）——因为检测只占虚拟时间约 $10\%$，总时间由**移动距离**主导。因此本策略的主要收益来自**采样构型（覆盖率与余量）与巡回合并**。
3. **定位复用问题一**：两视线楔形交 + 无框半平面交得直径与最小包围圆；**必须丢弃交会角 $\le2\delta$ 的视线对**。
4. **清除靠"两级链"**：在巡回已测视线的良态点对中取 $D$ 最小者定位得 $c_1$，若 `/clear` 失败则在 $c_1$ 复测、再在**垂直视线方向偏移 100 m** 处复测（稳健措施），实现口径下二级区域半径 $R^*_2\le2.1383$ m、圆心距真源 $\lVert c_2-G\rVert\le1.9029$ m，$R^*_2\le20$ m 的比例 $100\%$（清除半径 $20$ m），样本内**必然命中**；真实风险是 $\alpha\to180^\circ$，由"最小 $D$ 点对 + 垂直补测 + 射线兜底"规避。
5. **调度把覆盖、第二示向度、清源合并为一条巡回**，并用频道剪枝；平均定位清除时间 $488.5$ s，比保底基线快 $5.6$ 倍，100 局清除比例 $100\%$。
6. **评分口径**：先保证清除比例 $=1$，再最小化平均时间。
7. **问题级下界**：任何策略都不能突破的下界（与算法无关）为最坏 $n=10$ 时 $\ge302.0$ s/源（严格档，判空取 $35$ s/空频道）或 $\ge297.0$ s/源（松弛档，$30$ s）；$n$ 在 $[10,16]$ 均匀取值时均值约 $253.0$（严格）/ $250.3$（松弛）s/源。**注意口径**：实测 $488.5$ s/源与"最坏 $n=10$ 档"是两个不同口径，其比值不表示接近度（亦**不可**把 $299.4/302.0$ 读作"已达最优"）；同 $n$ 口径（$n\approx13$）为 $\approx1.20$、同实例口径为 $\approx1.93$，且这两者仍**不含**下界未覆盖的"巡检 $+$ 逐点判空"工程成本。下界的证明、数值表与差额分析见《问题三_证明_下界.md》（2026-09-12 第 5 版：对抗构型 $L^*$ 改为直接最大化、判空分严格/松弛两档、引理 2 补可加性论证）。

---

## 9 理论依据与文献支撑

**（1）圆盘覆盖与格覆盖.** "用最少的等半径圆盘覆盖一个区域"是经典覆盖问题。**无界平面**上最省的格是六边形（三角）格，其覆盖半径为格距的 $1/\sqrt3$（文献 [16]）。本章的两点差异须强调：① 本题区域是**有界圆盘**，故最优环半径不是 $\sqrt3\cdot1000$，而是 $\rho^*=900\sqrt3=1558.85$ m（连续最优，$c_{\min}=900.0000$ m），工程取整为 $R^\dagger=1550$ m（§5.1）；② 我们用的是"1 个中心点 + 6 个环点"的**有限构型族**，而非无限格，其最优性由解析式与域内网格穷举验证。**有界圆盘的精确覆盖数**另有经典结论：$6$ 个半径 $1$ 的圆盘最多覆盖半径 $\approx1.798907$ 的圆域，故 $1800/1000=1.8$ 需要 **$7$** 个盘（文献 [28]）——这条正是问题级下界"判空严格档 $35$ s"的依据。

**（2）覆盖路径规划与多目标调度.** 采样点的访问顺序是覆盖路径规划问题，把"覆盖点"与"源清除点"合并为一条路径则是广义 TSP 及其"邻域版"（TSPN）：前者见 [17]–[20] 与 [32]–[35]（单元分解、生成树覆盖、信息路径规划），后者见 [29]。本章规模很小（点数 $\lesssim30$），最近邻 + 2-opt + Or-opt 已足够；路径长度的**精确下界**用 Held–Karp 动态规划（[26]），近似侧由 [27] 给出。

**（3）集合隶属与有界误差.** "真源必落在两楔形交区域内"是确定性的（问题一），故清源链的保证性是集合隶属意义上的**必然**，而非概率；`no_signal` 的保守语义（$R_{\rm eff}\ge1000$）同样是集合意义上的排除（文献 [3,4]）。

**（4）搜索论视角.** 把"候选域 + 负信息更新"写成 $S_k\leftarrow S_k\setminus B(p,1000)$ 并据此规划下一步采样点，与最优搜索论中"负信息即区域剪除"的思想一致（文献 [21]）；在**有限视距**下的保证性搜索必须付出"绕行/多次访问"的代价，这一点在限视距巡视路线（[22]）与可见性追逃（[23,24]）中有严格对应，其"最少观察点数"形式的组合下界见 [25]。

**（5）问题级下界的文献支撑.** 本章下界（《问题三_证明_下界.md》）的四个组成部分都能在文献中找到对应物：行程项 = 最短开路的精确 DP（[26]，以及近似侧的 [27]）；判空项 = 有界圆盘覆盖数（[28]）与"负信息必须覆盖整个候选域"的搜索论判据（[21]）；保证性巡视的结构成本 = 限视距 watchman route（[22]）与有限视场追逃（[24]）。**分组测试不适用**：题面 API 一次只能测一个频道（无合并检测），故 [37] 的通道分组思路不能用来省判空测量。

**（6）后续改进方向（本文未实现，供下一步）.** ① 把"巡检覆盖点"与"源清除点"放进**同一条开链**统一规划（TSPN 口径，[29]）可把巡检行程进一步并入清源路线；② 第二视线的选取可用纯方位定位的**最优观测几何**（交会角 $\approx90^\circ$ 的 D-最优条件）替代固定候选族（[30]）；③ "何时中断巡视去清源"具有在线决策结构，其竞争比见 [31]。

---

## 参考文献

沿用问题一、问题二的编号 [1]–[15]，新增 [16]–[21]（**本轮再新增 [22]–[37]**；完整条目与核对状态见 `参考论文/README.md`，**卷期页码待核对**——检索时 Semantic Scholar 429、DBLP 拦截、arXiv 断连，仅 OpenAlex/Crossref 可用）：

[16] Fejes Tóth L. *Lagerungen in der Ebene, auf der Kugel und im Raum*. Springer, 1953（六边形格覆盖半径 $s/\sqrt3$；**【待核对】**）

[17] Choset H. Coverage for robotics — a survey of recent results[J]. *Annals of Mathematics and Artificial Intelligence*, 2001, 31(1–4): 113–126.（**【待核对】**）

[18] Galceran E, Carreras M. A survey on coverage path planning for robotics[J]. *Robotics and Autonomous Systems*, 2013, 61(12): 1258–1276.（**【待核对】**）

[19] Laporte G, Nobert Y. Generalized traveling salesman problem through $n$ sets of nodes[J]. *European Journal of Operational Research*, 1983, 12(1): 82–89.（**【待核对】**）

[20] Lin S, Kernighan B W. An effective heuristic algorithm for the traveling-salesman problem[J]. *Operations Research*, 1973, 21(2): 498–516.（**【待核对】**）

[21] Stone L D. *Theory of Optimal Search*[M]. 2nd ed. INFORMS, 2004.（**【待核对】**）

**新增（2026-09-12，下界第 5 版与算法改进方向共用）：**

[22] Ntafos S. Watchman routes under limited visibility[J]. *Computational Geometry: Theory and Applications*, 1992, 1(3): 149–170.（限视距巡视路线的最短长度下界；对应问题四引理 3/4 与问题三巡检构型的结构成本。**【待核对】**）

[23] Guibas L J, Latombe J C, LaValle S M, Lin D, Motwani R. Visibility-based pursuit-evasion in a polygonal environment[C]//*WADS 1997*, LNCS 1272: 17–30（期刊版见 *IJCGA*, 1999, 9(5): 471–494）.（可见性追逃的信息状态范式：保证性搜索必须付出多次访问。**【待核对】**）

[24] Gerkey B P, Thrun S, Gordon G J. Visibility-based pursuit-evasion with limited field of view[J]. *International Journal of Robotics Research*, 2006, 25(4): 299–315.（有限视距/视场严格抬高保证性搜索成本。**【待核对】**）

[25] Klein K, Suri S. Capture bounds for visibility-based pursuit evasion[J]. *Computational Geometry: Theory and Applications*, 2014, 47(8): 720–735.（最少观察点数/捕获界。**【待核对】**）

[26] Held M, Karp R M. A dynamic programming approach to sequencing problems[J]. *Journal of the Society for Industrial and Applied Mathematics*, 1962, 10(1): 196–210.（最短开路 $L^*$ 的精确 DP，本文表 2 的 "$L^*$" 即用此算法。）

[27] Christofides N. Worst-case analysis of a new heuristic for the travelling salesman problem[R]. GSIA, Carnegie Mellon University, 1976（重印于 *Operations Research Forum*, 2022）；Arora S. Polynomial time approximation schemes for Euclidean TSP and other geometric problems[J]. *Journal of the ACM*, 1998, 45(5): 753–782.（近似侧 1.5-近似/PTAS，与 [26] 构成"下界—上界"夹逼。**【待核对】**）

[28] Fejes Tóth L. Thinnest covering of a circle by eight, nine, or ten congruent circles[A]//*Combinatorial and Computational Geometry*, MSRI Publications 52, 2005: 361–376；Melissen J B M. Covering a rectangle with six and seven circles[J]. *Discrete Applied Mathematics*, 2000, 99(1–3): 149–156；Gáspár Z, Tarnai T, Hincz K. Partial covering of a circle by 6 and 7 congruent circles[J]. *Symmetry*, 2021, 13(2): 276.（6 个半径 1 的圆盘最多覆盖半径 $\approx1.798907$ 的圆域 ⇒ 本题 $m_{\rm cov}\ge7$，判空严格档 $35$ s 的依据；**该临界值本队未独立证明**。**【待核对】**）

[29] Dumitrescu A, Mitchell J S B. Approximation algorithms for TSP with neighborhoods in the plane[J]. *Journal of Algorithms*, 2003, 48(1): 135–159；de Berg M, Gudmundsson J, Katz M J, et al. TSP with neighborhoods of varying size[J]. *Journal of Algorithms*, 2005, 57(1): 22–36；Mitchell J S B. A constant-factor approximation algorithm for TSP with pairwise-disjoint connected neighborhoods in the plane[C]//*SoCG 2010*: 183–191.（"覆盖区域 $+$ 访问目标"的统一开链路线 = 本文 §5.1 的改进方向。**【待核对】**）

[30] Bishop A N, Fidan B, Anderson B D O, Doğançay K, Pathirana P N. Optimality analysis of sensor-target localization geometries[J]. *Automatica*, 2009, 45(6): 1431–1439；Bayram H, Bozma H I, et al. Gathering bearing data for target localization[J]. *IEEE Robotics and Automation Letters*, 2016, 1(1): 641–646.（纯方位定位的最优观测几何与下一最佳视角：第二/第三视线应取交会角 $\approx90^\circ$ 的 D-最优位置。**【待核对】**）

[31] Bertsimas D J, van Ryzin G. A stochastic and dynamic vehicle routing problem in the Euclidean plane[J]. *Operations Research*, 1991, 39(4): 601–615；Ausiello G, Feuerstein E, Leonardi S, Stougie L, Talamo M. Algorithms for the on-line travelling salesman[J]. *Algorithmica*, 2001, 29(4): 560–581.（"何时中断巡视去清源"的在线决策与竞争比。**【待核对】**）

[32] Choset H. Coverage of known spaces: the boustrophedon cellular decomposition[J]. *Autonomous Robots*, 2000, 9(3): 247–253.（单元分解式覆盖路线。**【待核对】**）

[33] Acar E U, Choset H, Rizzi A A, Atkar P N, Hull D. Morse decompositions for coverage tasks[J]. *International Journal of Robotics Research*, 2002, 21(4): 331–344.（**【待核对】**）

[34] Gabriely Y, Rimon E. Spanning-tree based coverage of continuous areas by a mobile robot[J]. *Annals of Mathematics and Artificial Intelligence*, 2001, 31(1–4): 77–98（会议版 *ICRA 2001*）.（生成树覆盖：单机器人近最优覆盖路径的基线。**【待核对】**）

[35] Binney J, Sukhatme G S. Branch and bound for informative path planning[C]//*ICRA 2012*: 2147–2154.（带界的信息路径规划：非短视地权衡行程与信息增益。**【待核对】**）

[36] Kao M Y, Reif J H, Tate S R. Searching in an unknown environment: an optimal randomized algorithm for the cow-path problem[J]. *Information and Computation*, 1996, 131(1): 63–79.（在线搜索的竞争比下界。**【待核对】**）

[37] Aldridge M, Johnson O, Scarlett J. Group testing: an information theory perspective[J]. *Foundations and Trends in Communications and Information Theory*, 2019, 15(3–4): 196–392.（**本文注明其不适用**：题面 API 一次只能测一个频道，不存在合并检测，故不能用分组测试省判空测量。**【待核对】**）

> **说明**：本机联网检索受限（Semantic Scholar 429、DBLP 拦截、arXiv 断连，仅 OpenAlex/Crossref 可用），[16]–[37] 的书目信息为按检索结果与领域常识给出，**提交前须用数据库核对卷期页码**；正文的所有数值结论均**不依赖**上述文献（可完全由附录 B 代码、`本地测试器/simulator/` 与 `.review/` 下脚本复现）。**分组测试类文献 [37] 已注明不适用**，避免审稿人误以为"20 个频道可以选择性合并检测"。

---

## 附录 A　核心算法伪代码

```
函数 COVER_CONFIG(n_ring):                  # 由"覆盖半径极小"确定环半径与余量
    R ← argmin_R max_{G∈Ω} min_{p∈cfg(R)} ‖G−p‖      # 解析式 c(ρ)，网格复核
    return R, margin = 1000 − cover_radius(R)

函数 LOCATE(dets):                          # 两视线交会（复用问题一）
    丢弃所有交会角 ≤ 2δ 的视线对
    H ← 各视线的两个半平面
    V ← 无框枚举（边界线两两求交 + 全约束筛选）
    return (V, D, c, R*)                    # 直径、最小包围圆

函数 CLEAR_CHAIN(k, dets):
    (V,D,c1,R*1) ← LOCATE(dets)
    if /clear(c1,k) = success: return True
    dets ← dets ∪ {(c1, /measure(c1,k))}
    c3 ← c1 + 100·n̂        # n̂ ⊥ 视线方向
    dets ← dets ∪ {(c3, /measure(c3,k))}
    c2 ← LOCATE(dets).c
    if /clear(c2,k) = success: return True
    return GRID_CLEAR(28m, r≤150m, center=c2) or RAY_MARCH(best bearing)
```

## 附录 B　Python 参考实现

> 完整可运行程序见 `本地测试器/simulator/dog_plan.py`（策略）与 `batch_plan.py`（批量驱动）。下列为核心片段（**无 numpy 依赖**，以便在模拟器环境直接运行）。

```python
# -*- coding: utf-8 -*-
"""问题三策略核心（摘自 dog_plan.py）。完整版含通信、巡回与兜底。"""
import math

ARENA, D2R, DELTA = 1800.0, math.pi / 180.0, 1.0
DETECT_R, CLEAR_R = 1000.0, 20.0


def q3_points(R=1550.0):
    """中心 + 正六边形环。工程取整 R=1550 m：覆盖半径 900.04 m、余量 99.96 m；
    连续最优 R=900*sqrt(3)=1558.85 m（覆盖半径 900.00 m）。"""
    pts = [(0.0, 0.0)]
    for i in range(6):
        a = 2 * math.pi * i / 6
        pts.append((R * math.cos(a), R * math.sin(a)))
    return pts


def wedge_halfplanes(S, theta_deg, half=DELTA):
    a1, a2 = (theta_deg - half) * D2R, (theta_deg + half) * D2R
    return [(S, (-math.sin(a1), math.cos(a1))), (S, (math.sin(a2), -math.cos(a2)))]


def dcirc(a, b):
    return abs((a - b + 180.0) % 360.0 - 180.0)


def region_stats(dets, tol_rel=1e-9):
    """无框半平面交：返回顶点 V、直径 D、最小包围圆半径 R* 与圆心 c。
    交会角 <= 2δ 的对必须在调用前剔除（否则区域无界）。"""
    hps = []
    for S, th in dets:
        hps.extend(wedge_halfplanes(S, th))
    scale = max(1.0, max(max(abs(s[0]), abs(s[1])) for s, _ in dets))
    tol = tol_rel * scale
    cand = []
    m = len(hps)
    for i in range(m):
        qi, ni = hps[i]
        for j in range(i + 1, m):
            qj, nj = hps[j]
            den = ni[0] * nj[1] - ni[1] * nj[0]
            if abs(den) <= 1e-12:
                continue
            bi = ni[0] * qi[0] + ni[1] * qi[1]
            bj = nj[0] * qj[0] + nj[1] * qj[1]
            p = ((bi * nj[1] - bj * ni[1]) / den, (ni[0] * bj - nj[0] * bi) / den)
            if all(n[0] * (p[0] - q[0]) + n[1] * (p[1] - q[1]) >= -tol for q, n in hps):
                cand.append(p)
    uniq = []
    for p in cand:
        if not any(math.hypot(p[0] - u[0], p[1] - u[1]) <= 1e-6 * scale for u in uniq):
            uniq.append(p)
    if len(uniq) < 2:
        return None
    D = max(math.hypot(uniq[i][0] - uniq[j][0], uniq[i][1] - uniq[j][1])
            for i in range(len(uniq)) for j in range(i + 1, len(uniq)))
    best_r, best_c = float("inf"), None

    def ok(c, r):
        return all(math.hypot(p[0] - c[0], p[1] - c[1]) <= r + 1e-7 for p in uniq)

    for i in range(len(uniq)):
        for j in range(i + 1, len(uniq)):
            c = ((uniq[i][0] + uniq[j][0]) / 2, (uniq[i][1] + uniq[j][1]) / 2)
            r = math.hypot(uniq[i][0] - uniq[j][0], uniq[i][1] - uniq[j][1]) / 2
            if r < best_r and ok(c, r):
                best_r, best_c = r, c
    for i in range(len(uniq)):
        for j in range(i + 1, len(uniq)):
            for k in range(j + 1, len(uniq)):
                a, b, cc = uniq[i], uniq[j], uniq[k]
                d = 2 * (a[0] * (b[1] - cc[1]) + b[0] * (cc[1] - a[1]) + cc[0] * (a[1] - b[1]))
                if abs(d) < 1e-14:
                    continue
                ux = ((a[0]**2 + a[1]**2) * (b[1] - cc[1]) + (b[0]**2 + b[1]**2) * (cc[1] - a[1])
                      + (cc[0]**2 + cc[1]**2) * (a[1] - b[1])) / d
                uy = ((a[0]**2 + a[1]**2) * (cc[0] - b[0]) + (b[0]**2 + b[1]**2) * (a[0] - cc[0])
                      + (cc[0]**2 + cc[1]**2) * (b[0] - a[0])) / d
                c = (ux, uy)
                r = math.hypot(a[0] - ux, a[1] - uy)
                if r < best_r and ok(c, r):
                    best_r, best_c = r, c
    return {"V": uniq, "D": D, "R": best_r, "c": best_c}


def good_pair(th1, th2):
    """交会角必须严格大于 2δ，否则该对给出的区域无界（问题一定理 1）。"""
    return dcirc(th1, th2) > 2 * DELTA


def tour_nn_2opt(points, start=(0.0, 0.0)):
    """最近邻 + 2-opt（点数 ≤ 30，秒级）。"""
    remaining, tour, cur = list(points), [], start
    while remaining:
        nxt = min(remaining, key=lambda p: math.hypot(p[0] - cur[0], p[1] - cur[1]))
        tour.append(nxt); remaining.remove(nxt); cur = nxt
    improved = True
    while improved:
        improved = False
        for i in range(len(tour) - 1):
            for j in range(i + 1, len(tour)):
                a = start if i == 0 else tour[i - 1]
                b, cc = tour[i], tour[j]
                d = tour[j + 1] if j + 1 < len(tour) else None
                old = math.hypot(b[0] - a[0], b[1] - a[1])
                if d:
                    old += math.hypot(d[0] - cc[0], d[1] - cc[1])
                new = math.hypot(cc[0] - a[0], cc[1] - a[1])
                if d:
                    new += math.hypot(d[0] - b[0], d[1] - b[1])
                if new < old - 1e-9:
                    tour[i:j + 1] = reversed(tour[i:j + 1]); improved = True
    return tour


def ray_march_clear(client, ch, dets, max_len=1500.0, step=40.0, budget=120):
    """退化构型的兜底：沿最优示向度射线推进清除（源必在该射线上）。"""
    if not dets:
        return False
    S, th = dets[0]
    u = (math.cos(th * D2R), math.sin(th * D2R))
    n = (-u[1], u[0])
    for i in range(int(max_len / step) + 1):
        for lat in (0.0, 25.0, -25.0):
            if budget <= 0:
                return False
            budget -= 1
            client.clear(S[0] + u[0] * i * step + n[0] * lat,
                         S[1] + u[1] * i * step + n[1] * lat, ch)
            if ch in client.cleared:
                return True
    return False
```

**调用示例（复现覆盖构型与清源链的关键数字）：**

```python
# 覆盖半径（解析式；域内 5 m 网格一致性核对）
#   连续最优 R=900*sqrt(3)=1558.85 -> 900.0000 m；工程取整 R=1550 -> 900.0435 m；
#   sqrt(3)*1000=1732.05 -> 1000.0000 m（零余量）。网格核对见 .review/q3_config.py A 节
# 清源链对照——对应表 5（实现口径，.review/q3_clear_chain_check.py，N=4000）
#   只测 c1 时 R*2 最大 19.6674 m；加垂直偏移 100 m 后 R*2 最大 2.1383 m、
#   |c2-G| 最大 1.9029 m、R*2<=20 m 比例 100%
```
