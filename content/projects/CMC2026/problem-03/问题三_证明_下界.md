---
# 分层项目的文档页（regular page）：scripts/new-content.sh doc 自动生成
title: "问题三 平均定位清除时间的理论下界（问题级，算法无关）——大证明"
date: 2026-09-13
draft: false
weight: 4
# 公式本身在构建期已渲染（见 layouts/_markup/render-passthrough.html），这里的 math 只决定
# 本页要不要加载 KaTeX 样式。默认 true（本类文档通常含公式）；纯文字文档改成 false，省约 23KB。
math: true
description: ""
summary: ""
---
# 问题三 平均定位清除时间的理论下界（问题级，算法无关）——大证明

> **文档定位**：本文给出问题三「平均定位清除时间」的**下界**，只使用题目正文与附录 1/2 的规则，**不涉及任何具体策略的结构**，因此它不是本队算法的下界，而是**问题本身的下界**（任何策略都无法突破）。配套正文：《问题三.md》§7 的实测值可用本文作评价基线。公式与符号另见《问题三_公式.md》定理 5，写作与验证交底见 `main/过程记录/问题三_证明笔记.md`。
>
> **结论标注体系**（全文严格遵守）：
> - **【定理】** = 严格可证，证明写在 §3；
> - **【数值证书】** = 依赖网格/优化计算并给出离散误差说明；
> - **【数值估计】** = 仅供参照，不作为下界使用。

---

## 1 口径：要下界的是哪一个量

题面给出的统计指标原文为

$$\text{被清除干扰源个数的比例}=\frac{\text{被清除干扰源的个数}}{\text{干扰源总数}},\qquad
\text{平均定位清除时间}=\frac{\text{定位清除总时间}}{\text{被清除干扰源的个数}},$$

且"定位清除总时间包含机器狗移动时间、频道切换时间、检测时间、光学精确定位时间及清除时间等"（题目问题 3 原文；`B题_AI可读版.md`「AI语义索引 / 问题3统计指标」）。

**本文只讨论清除比例 = 1 的策略**（先保证比例、再最小化时间，与《问题三.md》§5.5 的评分口径一致）。此时分母 = 干扰源总数 $n$，故

$$\boxed{\ \text{平均定位清除时间}=\frac{T}{n}\ },\qquad T=\text{整局累计虚拟时间}.$$

于是"求平均清除时间的下界"等价于"求总虚拟时间 $T$ 的下界再除以 $n$"。$n$ 未知但已知 $10\le n\le 16$，故下界按 $n$ 分别给出（表 1）。

---

## 2 过程模型与记号

### 2.1 单步成本（题面附录 2 与附件 1、2）

| 动作 | 虚拟时间成本 | 备注 |
|---|---|---|
| 移动 | $\lVert\Delta p\rVert/5$ s | 速度 5 m/s，按两次提交位置的**直线距离**计（附件 2 §4.2） |
| `/measure` | 移动 + **5 s** + 换频 **1 s** | 换频耗时仅在本次频道 ≠ 测向机当前频道时计入 |
| `/clear` 成功 | 移动 + **5 s** | 3 s 光学精定位 + 2 s 激光清除；**不换频** |
| `/clear` 未发现 | 移动 + **3 s** | 只做光学精定位 |
| `/enter`、`/exit` | 0 | 不推进虚拟时钟 |

测向机同一时刻只能检测一个频道；移动中不能检测（附录 2(4)(5)）。因此一次动作只服务一个频道，**没有任何"合并动作"**。

### 2.2 合法实例类（对抗方能选的东西）

$$\mathcal I(n)=\Bigl\{\,(\kappa,G,R,\text{频道})\ :\ G_i\in\Omega,\ \kappa_i\ \text{互异},\ R_i\in[1000,1500],\ i=1..n\,\Bigr\},\quad \Omega=B(O,1800),\ O=(0,0).$$

- 源位置 $G_i$ 可在 $\Omega$ 内**任意**选取（题目只给"总数 10–16、具体个数与位置未知"）；
- 有效接收半径 $R_i$ 逐源未知，**保证性推理只能取 $R_i=1000$**：若某检测点与源距离 $\le 1000$ m，则不论该源真实 $R_i$ 取何值（$\ge1000$）都必然收到信号（全向源覆盖角 360°）；反之 $>1500$ m 必然收不到，$1000\text{–}1500$ 之间不确定。故**下界证明中凡用到"必然收到"的地方，阈值一律取 1000 m**；
- 起点恒为 $O=(0,0)$（附件 2 §1.4）。

### 2.3 记号

| 符号 | 含义 |
|---|---|
| $\Omega=B(O,1800)$ | 目标圆域 |
| $n$ | 该实例的干扰源个数，$10\le n\le16$ |
| $G=\{G_1,\dots,G_n\}$ | 源位置集合 |
| $P_k$ | 本次运行中**频道 $k$ 的 `/measure` 位置（动作）集合** |
| $Q_k$ | 本次运行中**频道 $k$ 的 `/clear` 位置（动作）集合** |
| $D$ | 总移动距离 |
| $M$ | 总检测（`/measure`）次数；$S$ 总换频时间（每次 1 s） |
| $C_{\rm ok},C_{\rm fail}$ | 成功、未发现的清除次数 |
| $T$ | 总虚拟时间 |
| $m_{\rm cov}$ | 用半径 1000 m 的圆盘覆盖 $\Omega$ 所需的最少个数 |
| $a,b$ | 某频道上 `no_signal` 测量次数、未发现清除次数（$a=\lvert P_k\rvert,b=\lvert Q_k\rvert$） |
| $\text{arc}(d,r)$ | 半径 $r$ 的盘、盘心距 $O$ 为 $d$ 时覆盖 $\partial\Omega$ 的弧长（§3.3） |
| $c_{\rm det}$ | 每个空频道的判空成本下界（严格档 $\ge35$ s（文献档 $m_{\rm cov}\ge7$）／松弛档 $\ge30$ s（自证三约束）） |
| $L^*(O;G)$ | 从 $O$ 出发、访问 $G_1..G_n$ 各一次的最短开路长度 |
| $\Pi(n)=20+40(n-1)$ | 清除点可在源周围 20 m 内任取带来的行程折扣（引理 4） |

### 2.4 成本恒等式（后续所有下界的出发点）

**【定理 0】** 任一局运行满足

$$T=\frac{D}{5}+5M+S+5C_{\rm ok}+3C_{\rm fail},\qquad 0\le S\le M .$$

**证明.** 逐动作求和：第 $j$ 次动作的成本为 $\lVert\Delta p_j\rVert/5+\delta_j$，其中 $\delta_j=5+1\!\!\upharpoonright_{\text{换频}}$（`/measure`）、$5$（清除成功）或 $3$（清除未发现）。对 $j$ 求和即得第一式；换频只在 `/measure` 且频道改变时发生，故换频总时间 $S\le M$。$\blacksquare$

由 $S\ge0$，在推下界时可**忽略 $S$**（放掉一项只会让下界更保守、仍然有效）。

---

## 3 引理与主定理

### 3.1 引理 1（清除下界）

**【定理】** $C_{\rm ok}\ge n$，故 $5C_{\rm ok}\ge 5n$。

**证明.** 每个源必须被清除（比例 = 1 的定义）；题面附录 1(1)：**每个干扰源的频道互不相同**，且"同一个干扰源只能被清除一次"（附件 2 §8.2）。因此 $n$ 个源至少需要 $n$ 次互不相同的成功 `/clear`，每次成本恰为 5 s（横向移动成本另计入 $D$）。$\blacksquare$

### 3.2 引理 2（每源的定位成本下界）

**【定理】** 记频道 $k$ 上的测量动作集合为 $P_k$、清除动作集合为 $Q_k$。则

1. **（per-run 档）** 记 $z$ 为**没有任何测量**的源频道数。则 $5M+3C_{\rm fail}\ \ge\ 5(n-z)$：$|P_k|\ge1$ 的源频道各贡献 $\ge5$ s，而 $|P_k|=0$ 的源频道在 per-run 口径下**不保证有额外成本**。由于 $z$ 不受策略控制，**per-run 没有只依赖 $n$ 的正下界**。
2. **（$\exists$-实例档）** 存在合法实例，使需要"纯清除"（$|P_k|=0$）才能清掉的那个频道上有 $|Q_k|\ge8100$、即该频道额外成本 $\ge8099\times3=24297$ s $\gg5$ s；与情形一（$|P_k|\ge1$ 时该频道额外成本 $\ge5$ s）合并，在**该实例**上每源额外 $\ge5$ s，合计 $\ge5n$。

**证明.** 固定一次运行，记其频道 $k$ 的测量次数 $|P_k|$ 与清除动作集合 $Q_k$。

**情形一：$|P_k|\ge1$。** 该频道至少做过一次 `/measure`。这一次测量的成本为（移动 $+5$ s $+$ 换频 1 s 或 0）$\ge5$ s；而 `/clear` 的返回值只有"已清除/未发现"、不返回示向度，故它**不可能是**那次成功的 `/clear`，属于"成功清除之外"的额外动作。得额外成本 $\ge5$ s。

**情形二：$|P_k|=0$。** 该频道从未做过任何测量，机器狗对该源**没有任何测向信息**，只能盲清。**per-run 下该频道不保证有额外成本**：若唯一一次清除即命中，则 $|Q_k|=1$、除成功清除外无任何动作（$3$ s 光学定位已含在成功清除的 $5$ s 内，不得重复计入）；若它先后做过若干次“未发现”清除，则每次 $\ge3$ s。**要把该频道提到 $\ge5$ s，必须改用对抗实例**（本引理服务于 §3.7 的 $\exists$-实例下界）：


固定除频道 $k$ 外的全部源（位置、类型、朝向不变），考虑把频道 $k$ 的源放在 $z\in\Omega$ 处的一族实例 $\{I_z\}$。机器狗在该频道上从不测量，故它在频道 $k$ 上得到的观测只可能取"未发现"这一种值；而在**命中之前**这些观测与 $z$ 无关，其余频道的源已固定、其观测在整族实例上完全相同。因此机器狗在频道 $k$ 上的清除点序列 $q_1,q_2,\dots$ 由"全未发现"前缀唯一决定，**对一切 $z$ 都相同**（直到首次命中为止；策略在频道之间的调度、交错次序不影响该结论——这里数的只是频道 $k$ 上的动作）。策略要保证清除比例 $=1$，就必须对每个 $z\in\Omega$ 都能命中，即前缀序列必须覆盖 $\Omega$：

$$\Omega\ \subseteq\ \bigcup_{j\ge1}B(q_j,20)\quad\Longrightarrow\quad \#\{j:\ \text{命中之前的清除}\}\ \ge\ \Bigl\lceil\frac{\pi\cdot1800^2}{\pi\cdot20^2}\Bigr\rceil=8100 .\tag{1}$$

（面积下界：半径 $20$ m 的盘覆盖半径 $1800$ m 的圆域至少需要 $\lceil(1800/20)^2\rceil=8100$ 个。）取"在序列中覆盖顺序最靠后"的那个 $z^*$：在实例 $I_{z^*}$ 上，该频道的前 $8099$ 次清除全部返回"未发现"（每次 $3$ s），直到第 $8100$ 次才命中，故

$$|Q_k|\ \ge\ 8100,\qquad \text{额外成本}\ \ge\ 8099\times3=24297\ \text{s}\ \gg\ 5\ \text{s}.$$

合并：per-run 得 $5M+3C_{\rm fail}\ \ge\ 5(n-z)$（$z$ 为无测量的源频道数）；存在实例使每源额外 $\ge5$ s，得 $\ge5n$（$\exists$-实例档，本文表 1 采用）。$\blacksquare$

> **口径说明（勿误引）**：情形二必须按**对抗实例（$\exists$-实例）**口径读——"命中之前"的清除点序列由全未发现前缀唯一决定、对源位置 $z$ 一致，故**存在实例**使 $|Q_k|\ge8100$、该频道额外成本 $\ge24297$ s $\gg5$ s。**per-run 口径只能写成 $5M+3C_{\rm fail}\ge5(n-z)$**（$z$ 为**没有任何测量**的源频道数）：若某源频道被唯一一次盲清直接命中，该频道除成功清除外无任何额外动作（$3$ s 光学定位已含在 $5$ s 内，不得重复计入）。该口径的完整说明见 `main/过程记录/问题三_版本与对比.md` §3.4。
>
> **两点前提（须与策略假设一致）**：① 策略按观测**确定性地**决策（若策略内部含随机性，则对每个随机种子各取一次论证）；② 策略必须**持续**在该频道上清除直到命中（中途放弃等于漏源，与"清除比例 $=1$"矛盾）。同一手法在引理 3（**空频道**）中是 per-run 合法的，因为空频道没有成功清除、不存在分歧点，理由见 `main/过程记录/问题三_证明笔记.md` §5。
>
> **可加性的两情形修补（补齐的是"各频道的对抗放置可在同一实例上求和"这一步）**：常见写法"对每个待判频道各自固定其余源，故可加"只是提示，尚需论证。设策略 $s$ 固定，取其**环构型实例** $I$（$n$ 个源，位置即表 2 的对抗构型）：
> - **情形 A（$I$ 上每个源频道都有 $\ge1$ 次测量，即 $z=0$）**：由情形一立即得 $5M+3C_{\rm fail}\ge5n$，与引理 4 的行程项相加即 (4) 式；
> - **情形 B（存在盲清频道 $k$）**：对该频道做情形二的家族论证，得实例 $I_{z^*}$（其余 $n-1$ 个源位置不变，仅频道 $k$ 的源落在 $z^*$），在该实例上 $T\ge24297$ s。$I_{z^*}$ 与 $I$ 的行程项之差为 $\bigl|L^*(O;G)-L^*(O;G_{z^*})\bigr|/5\le 2\cdot 1800/5=720$ s（开路每多访问一个点，长度增加不超过直径 $2R=3600$ m；`.review/q3q4_bound_audit_probe.py` **S5 段**在 $n=10/12/16$ 上实测该差仅 $21\!-\!50$ s），故
>   $$T\ \ge\ 24297-720\ \gg\ \frac{L^*(G)-\Pi(n)}{5}+5n+5n+\mathbb 1_{n\le15}(20-n)\,c_{\rm det},$$
>   即情形 B 的值**不弱于** (4) 式的右端。
>
> 两情形合起来即得：对**任意**策略，都存在合法实例使 (4) 式成立——这正是表 1 采用的口径。

### 3.3 引理 3（每个空频道的判空成本下界）

**【定理】** 设 $n\le15$。对实例中**每个没有源的频道** $k$，记 $a=|P_k|$（`no_signal` 测量次数）、$b=|Q_k|$（未发现清除次数），则

$$\Omega\ \subseteq\ \bigcup_{p\in P_k}B(p,1000)\ \cup\ \bigcup_{q\in Q_k}B(q,20),\tag{2}$$

于是 $(a,b)$ 必须满足三个约束（下详），从而

$$5a+3b\ \ge\ 30\ \text{s}\quad\text{（三约束松弛解：最优 }(a,b)=(6,0)\text{）},\tag{3}$$
$$\text{严格档：}\ 5a+3b\ \ge\ 35\ \text{s}\quad\text{（}b=0\ \text{时 }a\ge7\text{）}.$$

**两档都进表 1**：$35$ s 为严格档（用 $m_{\rm cov}\ge7$，其外部依据见 §3.4 的"文献档"标注），$30$ s 为三约束松弛档（不依赖任何外部结论，$30$ s 作为下界同样有效）。

**证明.**

**(i) 覆盖条件 (2)**。频道 $k$ 无源 $\Rightarrow$ 该频道全部测量返回 `no_signal`、全部清除尝试返回"未发现"。取 $z\in\Omega\setminus\bigl(\bigcup_{P_k}B(p,1000)\cup\bigcup_{Q_k}B(q,20)\bigr)$（这是"仍可能藏有源"的位置集），构造 $I_z$：在频道 $k$ 上**新增**一个位于 $z$、$R_k=1000$ 的源（$n\le15$ 故 $n+1\le16$ 仍合法）。此时 $I_z$ 下**观测序列与本次运行逐位相同**——每个 $p\in P_k$ 处 $\lVert z-p\rVert>1000=R_k$ 仍无信号，每个 $q\in Q_k$ 处 $\lVert z-q\rVert>20$ 仍未发现，而**空频道本来就没有成功清除**（这正是本论证与引理 2 的关键差别：不存在"已清除"的观测，故无分歧点）。于是策略在 $I_z$ 上执行同一动作序列、在同一停止条件下结束，清除个数仍为 $n$、总数 $n+1$，**比例 $<1$，矛盾**。故 (2) 成立。

**(ii) 三个约束**。设 $a=|P_k|$、$b=|Q_k|$：

- **打靶约束**：$\Omega$ 内存在 5 个两两距离 $>2000$ m 的点（§3.4，内接正五边形，相邻弦 $2116.0$ m）。半径 1000 或 20 的圆盘至多覆盖其中 1 个，而 (2) 要求覆盖全部 5 点，故

$$a+b\ \ge\ 5 .$$

- **面积约束**：$\pi\cdot10^6\,a+\pi\cdot400\,b\ \ge\ \pi\cdot1800^2$，即 $1000^2a+20^2b\ \ge\ 1800^2$。
- **边界弧约束**：覆盖 $\Omega$ 必覆盖 $\partial\Omega$；而

$$\boxed{\ \text{arc}(d,r)=2\arccos\frac{d^2+1800^2-r^2}{2\cdot1800\cdot d},\qquad d\in[\,|1800-r|,\ 1800+r\,]\ }$$

是"半径 $r$ 的圆盘、盘心距 $O$ 为 $d$ 时能覆盖的 $\partial\Omega$ 弧长"（由余弦定理，$\cos\Delta\theta\ge(d^2+1800^2-r^2)/(2\cdot1800\cdot d)$）。对 $d$ 取最大（脚本 1 m 扫描 + 0.01 m 细化）：

$$\max_d\text{arc}(d,1000)=67.4980^\circ\ \ (d^*=1496.66\ \text{m}),\qquad
\max_d\text{arc}(d,20)=1.2733^\circ\ \ (d=1799.9\ \text{m}).$$

于是 $\dfrac{67.4980^\circ}{360^\circ}a+\dfrac{1.2733^\circ}{360^\circ}b\ \ge\ 1$，即 $67.4980a+1.2733b\ \ge\ 360$。**特别地** $b=0$ 时必须 $a\ge\lceil360/67.4980\rceil=6$，即

$$m_{\rm cov}\ \ge\ 6\qquad\text{（严格）}.$$

**(iii) 成本最小化**。动作成本 $\ge5a+3b$（`/measure` 每次 $\ge5$ s；空频道上 `/clear` 全部为"未发现" $=3$ s）。对每个 $a$ 取满足三约束的最小 $b$：

| $a$ | 0 | 1 | 2 | 3 | 4 | 5 | **6** | 7 |
|---|---|---|---|---|---|---|---|---|
| 最小 $b$ | 8100 | 5600 | 3100 | 600 | 71 | 18 | **0** | 0 |
| 成本 $5a+3b$ (s) | 24300 | 16805 | 9310 | 1815 | 233 | 79 | **30** | 35 |

最小值在 $(a,b)=(6,0)$ 取得，为 $30$ s。$\blacksquare$

> **口径注意（勿误引）**：$30$ s 是**必要约束的松弛解**——它是三个证书（打靶 $a+b\ge5$、面积 $1000^2a+20^2b\ge1800^2$、边界弧 $67.4980a+1.2733b\ge360$）下 $5a+3b$ 的最小值，**但 $(a,b)=(6,0)$ 未必可达**：三约束并未证明 6 个半径 1000 m 的圆盘真能覆盖 $\Omega$。据 §3.4 把 $m_{\rm cov}\ge7$ 作为**文献档**引入后，严格档取 $b=0\Rightarrow a\ge7\Rightarrow5a=35$ s；若只采信自证结论（$\max\{4,5,6\}=6$），则退回 $30$ s 松弛档。**表 1 同时列出两档**，引用时必须注明用的是哪一档。

> **追加口径（$35$ s 严格档的适用边界，勿越界引用）**：允许 $b>0$ 时三约束本身就给出 $(a,b)=(6,1)\Rightarrow5\cdot6+3\cdot1=\mathbf{33}\ \text{s}<35$ s（校验：$67.4980\cdot6+1.2733=406.3\ge360$；$a+b=7\ge5$；面积 $10^6\cdot6+400\cdot1=6.0004\times10^6\ge3.24\times10^6$）。因此 $35$ s 的正确读法是**"只用 `no_signal` 点取证（$b=0$）"这一档位的下界**（本文策略的判空正是只用 `no_signal`，从不靠 `/clear` 未发现取证），而**不是**任意合法策略的逐局下界。若要把 $20$ m 盘也计入覆盖证据，需要"$6$ 个 $1000$ m 盘 $+$ 若干 $20$ m 盘不能覆盖 $\Omega$"这一更强命题——本队**未证明**；常见写法"缺口面积 $\ge1.23\times10^4$ m²"只是**上界构造值**（同心环构型下的缺口面积），**不能**当作下界使用。

> **为什么 $n=16$ 时判空项为 0**：若实例中已有 16 个源，策略清除满 16 个后即可断定"总数不超过 16"，**不需要**再证明其余 4 个频道为空；而 $n\le15$ 时"再多一个源"仍合法，判空不可省。故表 1 中 $n=16$ 一行的判空项为 0。这是本下界随 $n$ 变化的原因。

### 3.4 覆盖数 $m_{\rm cov}$ 的下界（三个严格证书 + 一个数值旁证）

**【定理，面积法】** $m_{\rm cov}\ge3.24\Rightarrow m_{\rm cov}\ge4$（引理 3 (ii) 面积约束 $b=0$ 的特例）。

**【定理，打靶（packing）证书】** $m_{\rm cov}\ge5$。

**证明.** $\Omega$ 内接正五边形（半径 1800 m）的相邻弦长 $2\cdot1800\sin36^\circ=2116.0$ m $>2000$ m，故 5 个顶点两两距离 $>2000$ m（脚本另给出一般搜索的最小两两距离 $2010.2$ m 的构型）。一个半径 1000 m 的圆盘至多覆盖其中 1 个点，故任何覆盖 $\Omega$ 的半径 1000 圆盘族至少需要 5 个。$\blacksquare$

**【定理，边界弧证书】** $m_{\rm cov}\ge6$。

**证明.** 覆盖 $\Omega$ 必覆盖其边界 $\partial\Omega$。半径 1000 m 的圆盘、盘心距 $O$ 为 $d$（$d\in[800,2800]$）时，它与 $\partial\Omega$ 的交是角半宽 $\arccos\frac{d^2+1800^2-1000^2}{2\cdot1800\cdot d}$ 的弧，故覆盖的弧长 $\text{arc}(d,1000)=2\arccos(\cdot)\le67.4980^\circ$（脚本 1 m 扫描 + 0.01 m 细化，在 $d^*=1496.66$ m 处取得）。于是 $a$ 个半径 1000 盘至多覆盖 $67.4980^\circ a$ 的边界弧，要覆盖 $360^\circ$ 需 $a\ge\lceil360/67.4980\rceil=6$。$\blacksquare$

> **注意（口径）**：自证严格下界为 $\max\{4,5,6\}=6$（面积 / 打靶 / 边界弧）。再引入**文献档** $m_{\rm cov}\ge7$（下条）后，判空严格档取 $35$ s；判空成本一律由引理 3 (iii) 的成本最小化给出，而不是简单的 $3m_{\rm cov}$ 或 $5m_{\rm cov}$。

**【文献档，$m_{\rm cov}\ge7$】** 用半径 $1000$ m 的圆盘覆盖 $\Omega$ 至少要 **$7$** 个。

**依据.**（i）文献经典结论：$6$ 个半径 $1$ 的圆盘最多覆盖半径 $\approx1.798907$ 的圆域（Fejes Tóth 型最薄覆盖问题，具体条目见 §9 文献支撑）；而本题 $1800/1000=1.8>1.798907$，余量仅 $1.1$ m，故 $6$ 个盘不能覆盖。（ii）本队独立复核（`.review/q3q4_bound_audit_probe.py` **S3 段**，$25$ m 网格、对称族 $1$ 中心 $+$ 环点与纯环点全扫）：$k=6$ 时最优最大覆盖半径 $\approx1046.4$ m $>1000$ m（**不能覆盖**），$k=7$ 时 $\approx900.0$ m（可覆盖，与 $c(1550)=900.04$ m 一致）——与文献方向一致。**标注**：该档依赖外部经典值，本队**未独立证明** $1.798907$ 这一临界值，故在表 1 中与自证的 $30$ s 松弛档并列，引用时须注明出处档位。

**【数值旁证（非严格档）】** k-中心优化（粗网格 100 m 搜索 + 细网格 20 m 核验，离散误差约 $\pm10$ m）给出

- $k=6$：找到的最优最大覆盖半径 $\approx1035.2$ m $>1000$ m（不能覆盖）；
- $k=7$：找到的最优最大覆盖半径 $\approx923.9$ m $\le1000$ m（可覆盖，余量 76 m）。

**这一档的可靠性说明**：$k=6$ 是**极紧**情形——经典临界值 $1.798907$ 给出上限 $\approx1798.9$ m $<1800$ m，余量仅 $1.1$ m（$0.06\%$）；而本脚本的 20 m 网格值与独立优化器的值（本例随机重启 Nelder-Mead 得 $1136.6$ m）**都只是非最优可行解**。故 $m_{\rm cov}\ge7$ 的正当来源是上面的**文献档**，本段的网格值只作方向性旁证。

> **口径**：$m_{\rm cov}$ 的三类证据并列——**自证严格** $\ge6$（面积/打靶/边界弧）、**文献档** $\ge7$（经典临界值 $1.798907$，未独立证明）、**数值旁证**（网格/优化器，$k=6$ 不可覆盖）。判空严格档取文献档的 $35$ s，松弛档取自证三约束的 $30$ s。

### 3.5 引理 4（行程下界）

**【定理】** $D\ \ge\ L^*(O;G)-\Pi(n)$，$\Pi(n)=20+40(n-1)$。

**证明.** 设第 $i$ 个源的成功清除发生在位置 $q_i$（$\lVert q_i-G_i\rVert\le20$，题面附录 2(8)），$q_i$ 互不相同。把这些动作按时间排序，并记这 $n$ 个位置按访问次序为 $q_{\pi(1)},\dots,q_{\pi(n)}$。则该次运行的轨迹是从 $O$ 出发、依次经过这些点（可能还经过其他点）的折线，故

$$D\ \ge\ \lVert q_{\pi(1)}-O\rVert+\sum_{i=1}^{n-1}\lVert q_{\pi(i+1)}-q_{\pi(i)}\rVert
\ \ge\ \bigl(\lVert G_{\pi(1)}-O\rVert-20\bigr)+\sum_{i=1}^{n-1}\bigl(\lVert G_{\pi(i+1)}-G_{\pi(i)}\rVert-40\bigr)$$

（第二个不等号用三角不等式：$\lVert q_{a}-q_{b}\rVert\ge\lVert G_a-G_b\rVert-40$）。对任意排列 $\pi$ 成立，故对 $\pi$ 取下确界得 $D\ge L^*(O;G)-20-40(n-1)$。$\blacksquare$

### 3.6 引理 5（对抗构型与 $L^*$ 的闭式）

**【定理，单环构型】** 把 $n$ 个源等角放在半径 1800 m 的圆周上，则

$$L^*_{\rm ring}(n)=1800+(n-1)\cdot3600\sin\frac{\pi}{n}.$$

**证明.** 设源点在圆周上。（i）首跳：$O$ 到任一点的距离都是 1800 m，故首跳 $\ge1800$；（ii）其余 $n-1$ 条边：圆周上任意两个不同点的距离 $\ge2\cdot1800\sin(\pi/n)$（相邻点弦长最小），故每条边 $\ge2\cdot1800\sin(\pi/n)$；（iii）按环序（相邻点依次访问）行进同时取到两个下界。故最小值即该闭式。$\blacksquare$

脚本用 Held-Karp 位掩码 DP 精确复核，两者一致到 $10^{-12}$ m（表 2 第 3 列）。

**【定理，两层构型的显式实例】** 取 $k$ 个源等角置于半径 1800 m 圆周、$n-k$ 个源等角置于半径 $\rho$ 的内环（偏移角给定），则该实例的 $L^*$ 可由 Held-Karp 精确计算，与源位置信息无关地给出一个合法实例的下界。**该构型族上的最大化搜索是数值的**（A 段）；它只影响下界的**强度**，不影响有效性——表中每个 $L^*$ 都是某个具体实例的精确值。

数值结果（表 2）：两层构型比单环构型长 $12.0\%\!-\!32.3\%$，$n$ 越大差距越大；例如 $n=16$：单环 $12334.9$ m，两层（$8+8$，内环 $\rho=900$ m，相位 $22.5^\circ$）$16318.9$ m。

### 3.7 主定理

**【定理 1（问题三平均定位清除时间下界）】** 设策略在**所有**合法实例上都达到清除比例 = 1。则对任意 $n\in[10,16]$，存在合法实例使

$$\frac{T}{n}\ \ge\ \frac{1}{n}\Bigl[\underbrace{\frac{L^*(G)-20-40(n-1)}{5}}_{\text{行程}}+\underbrace{5n}_{\text{清除}}+\underbrace{5n}_{\text{定位}}+\underbrace{\mathbb 1_{n\le15}\,(20-n)\cdot c_{\rm det}}_{\text{判空}}\Bigr],\qquad c_{\rm det}\ \ge\ 35\ \text{s}\ \text{（严格档）}\ \ \text{或}\ \ c_{\rm det}\ \ge\ 30\ \text{s}\ \text{（松弛档）}.\tag{4}$$

其中 $L^*(G)$ 按表 2 的对抗构型取**直接最大化**的精确值（`.review/q3q4_Lstar_max.py`），$c_{\rm det}$ 两档（严格 $35$ s / 松弛 $30$ s）并列于表 1。**定位项的 $5n$ 是引理 2 的 $\exists$-实例档**：见下面的口径说明；其与行程项可在同一实例上求和，由 §3.2 末的**两情形（max）论证**给出。

**证明.** 由定理 0，$T=D/5+5M+S+5C_{\rm ok}+3C_{\rm fail}$，忽略 $S\ge0$；
- $D/5\ge[L^*-20-40(n-1)]/5$（引理 4）；
- $5C_{\rm ok}\ge5n$（引理 1）；
- $5M+3C_{\rm fail}$ 至少含引理 2 的 $5n$（每源的定位动作）与引理 3 的 $\mathbb 1_{n\le15}(20-n)\,c_{\rm det}$（每个空频道的判空动作；$n=16$ 时该论证不适用，项为 0）；两类动作是**不同频道的不同动作**，不重复计数（源所在频道 $\ne$ 空频道）；
- 相加除以 $n$ 即得。$\blacksquare$

> **口径说明（定位项为何取 $5n$）**：定位项的 $5n$ 来自引理 2 的 **$\exists$-实例档**——对某个 $|P_k|=0$ 的频道，存在实例使其额外成本 $\ge24297$ s $\gg5$ s；而 $|P_k|\ge1$（情形一）本身是 per-run 的 $\ge5$ s。**各频道的对抗放置能否合并到同一实例**：引理 2 的构造对每个待判频道各自固定其余源，故在“每个频道分别取对抗实例”的意义下可加。
> **per-run 口径下没有只依赖 $n$ 的正下界**：若某源频道被唯一一次盲清直接命中，该频道除成功清除外无任何额外动作（$3$ s 光学定位已含在 $5$ s 内）。per-run 只能说 $5M+3C_{\rm fail}\ge5(n-z)$，$z$ 为无测量的源频道数，$z\ge0$ 且不受策略控制。**因此本文表 1 只给 $\exists$-实例档 $5n$。**

> **注意**：下界对"行程 / 清除 / 定位 / 判空"的求和是**加法**的，因为它们是**不同动作**上的成本；而 §3.5 的行程下界与 §4 两类覆盖行程下界之间应取 **max**（同一段轨迹不能重复计），本文已把被支配的覆盖行程放进 §4.3 说明。

---

## 4 数值结果

### 4.1 下界表

**表 1　问题三平均定位清除时间下界（s，及 s/源；行程项用表 2 的精确 $L^*$；判空项按引理 3 的成本最小化）**

| $n$ | 行程下界 | 清除 | 定位 | 判空（严格 $c_{\rm det}=35$ s） | 总 $T$ | **s/源（严格档）** | 判空（松弛 $c_{\rm det}=30$ s） | s/源（松弛档） |
|---|---|---|---|---|---|---|---|---|
| 10 | 2570.2 | 50 | 50 | 350 | 3020 | **302.0** | 300 | 297.0 |
| 11 | 2690.1 | 55 | 55 | 315 | 3115 | **283.2** | 270 | 279.1 |
| 12 | 2825.7 | 60 | 60 | 280 | 3226 | **268.8** | 240 | 265.5 |
| 13 | 2861.1 | 65 | 65 | 245 | 3236 | **248.9** | 210 | 246.2 |
| 14 | 2986.2 | 70 | 70 | 210 | 3336 | **238.3** | 180 | 236.2 |
| 15 | 3032.7 | 75 | 75 | 175 | 3358 | **223.8** | 150 | 222.2 |
| 16 | 3139.8 | 80 | 80 | 0 | 3300 | **206.2** | 0 | 206.2 |

**最坏 n（按下界 s/源）= 10：$\ge302.0$ s/源（严格档）/ $\ge297.0$ s/源（松弛档）；$n$ 在 $[10,16]$ 均匀取值时均值 $\approx253.0$（严格）/ $250.3$（松弛）s/源。**

> **表注（定位项的口径）**：本表“定位”列取 $5n$，即引理 2 的 **$\exists$-实例档**（存在实例使 $|P_k|=0$ 的频道额外成本 $\ge24297$ s）。**没有**只依赖 $n$ 的 per-run 正下界：per-run 只给出 $5M+3C_{\rm fail}\ge5(n-z)$（$z$ 为无测量的源频道数，不受策略控制）。与行程项的可加性见 §3.2 末的两情形论证。
>
> **表注（行程列的口径）**：行程列 $=[L^*-20-40(n-1)]/5$，其中 $L^*$ 取表 2 **直接最大化**列的值（`.review/q3q4_Lstar_max.py`）。
>
> **表注（判空档的口径）**：严格档 $35$ s 依赖 $m_{\rm cov}\ge7$，而 $m_{\rm cov}\ge7$ 来自经典 6 圆覆盖临界值 $1.798907$（**外部文献档，本队未独立证明**）；松弛档 $30$ s 只用自己的三个证书（面积/打靶/边界弧）。**引用时必须写明档位。**

一行读法（$n=10$）：总虚拟时间 $T\ge3020$ s，其中行程 $2570$ s、清除 $50$ s、定位 $50$ s、判空 $350$ s（$10$ 个空频道 $\times35$ s）。

### 4.2 表 1 的输入：对抗构型的最短开路 $L^*$（脚本 A 段）

**表 2　对抗构型与 $L^*$（Held-Karp 精确，单位 m；在同一两层构型族内按 $\rho$ 步长 $50$ m（$n\ge14$ 为 $100$ m）与相位 $4$ 档（$n\ge14$ 为 $2$ 档）的网格直接最大化）**

| $n$ | 单环闭式 $L^*$ | Held-Karp 复核 | **两层构型（直接 max $L^*$）** | **$L^*$** | 提升（vs 单环） |
|---|---|---|---|---|---|
| 10 | 11812.15 | 11812.15 | 7+3, $\rho=600$ | **13231.2** | 12.0% |
| 11 | 11942.37 | 11942.37 | 7+4, $\rho=650$ | **13870.3** | 16.1% |
| 12 | 12049.23 | 12049.23 | 8+4, $\rho=750$ | **14588.6** | 21.1% |
| 13 | 12138.44 | 12138.44 | 8+5, $\rho=750$ | **14805.4** | 22.0% |
| 14 | 12213.98 | 12213.98 | 7+7, $\rho=800$ | **15470.8** | 26.7% |
| 15 | 12278.75 | 12278.75 | 10+5, $\rho=900$ | **15743.6** | 28.2% |
| 16 | 12334.88 | 12334.88 | 8+8, $\rho=900$（相位 $22.5^\circ$） | **16318.9** | 32.3% |

（构型为两层等角环 + 相位偏移，参数由 `.review/q3q4_Lstar_max.py` 输出的 $(k,m,\rho,\phi)$ 唯一确定，可精确复原；$L^*$ 是**同一两层族内、在表题所述的有限网格上直接最大化 $L^*$** 的结果，故仍是某个**具体合法实例**的精确值 ⇒ 下界有效（网格越细只会得到**不更小**的 $L^*$，而 $L^*$ 在下界分子上，故只会让下界**更强**）。**网格加密的实测收敛（$n=13$）**：$50$ m/$4$ 档 $14805.37$ $\to$ $25$ m/$12$ 档 $14841.30$ $\to$ $10$ m/$36$ 档 $\mathbf{14845.15}$（相邻两档只差 $3.9$ m，已收敛）——即本表值偏**保守**约 $40$ m（折合 $+0.61$ s/源）；因方向是让下界更强，表内一律保留**同一粗网格**口径，以便各行之间可比。；"该族是 $L^*$ 的最大构型"仍未证明，真极值只会更大，故只会让下界更强。）

### 4.3 被支配的另外两类行程下界（脚本 A3 段）

- **盲搜**（策略完全不定位，靠轨迹的 20 m 邻域覆盖 $\Omega$）：管状面积上界给出 $D\ge(\pi1800^2-\pi20^2)/(2\cdot20)\approx2.54\times10^5$ m $=50887.5$ s（精确值），远大于表中行程，故"不定位"不可行——这正是引理 2 情形二（$|P_k|=0$ 盲清）不构成更弱下界的原因。
- **判空覆盖行程**（轨迹的 1000 m 邻域须覆盖 $\Omega$）：$D\ge(\pi1800^2-\pi1000^2)/(2\cdot1000)\approx3518.6$ m $=703.7$ s，被源间行程支配（表 1 行程 $\ge2570$ s），故未在 (4) 式中单列。

---

## 5 与实测对照及差额分析

**表 3　下界 vs 本地模拟器实测**

| 项目 | 问题三 |
|---|---|
| 实测（对照策略，`results/plan_mixed_20260912_120553.csv`，100 局全清） | 均值 **488.5** s/源（最小 346.8 / 最大 705.3） |
| 本文下界（最坏 $n=10$，严格档 $c_{\rm det}=35$ s） | $\ge302.0$ s/源 |
| 本文下界（同 $n$，松弛档 $c_{\rm det}=30$ s） | $\ge297.0$ s/源 |
| 实测 / 下界（**最坏实例 ÷ 随机均值，仅存档，勿作接近度**） | $\approx1.62$（严格）/ $1.64$（松弛） |

差额的去向（这些**不是**可证下界的一部分，而是工程实现成本，逐项量化见表 4 后的归因表）：

1. **覆盖巡视行程**：7 点构型（中心 + 半径 $1300$ m 环 6 点）环上开链 $7800$ m $\approx1560$ s，占总虚拟时间约 $45\%$；下界只要求"访问源的 20 m 邻域"，不要求覆盖巡视，故不含此项。
2. **单源多次检测**：正文清源链典型 4 次检测 + 1 次清除（$\approx24$ s/源），下界只计 1 次测量（5 s）。
3. **判空采样密度**：正文按 7 点构型对未决频道逐点测量（每点 $\approx6$ s/频道），下界只计每个空频道的**成本最小化解**（$6$ 次测量 $=30$ s；严格档 $7$ 次 $=35$ s）。
4. **清除链的复测与兜底**：保证性所需，非下界所需。

### 5.1 策略对照与"实例级下界"新口径

**先说口径问题（重要）**：上表的比值是「**随机实例平均 ÷ 最坏实例下界**」——分子是 100 局随机实例的平均，分母是 $n=10$ **对抗构型**的最坏档，两者不可比；$L^*$ 用直接最大化值后，$n=10$ 档几乎与实测相等（$277.8/297.0=0.94$），**这个数字没有任何"已接近最优"的含义**。要回答"还能压缩多少"，必须用下面两个口径之一：

- **同实例口径**：用该局**真值源位置**算 $L^*(G)$（式 (6)）——这才是"离地板多远"的刻度，且它与对抗构型的取值**无关**（故 $L^*$ 的取值不影响它）；
- **同 $n$ 口径**：把实测均值与**同一典型 $n$** 的下界比。$n$ 在 $[10,16]$ 上均值约 $13$，对应下界 $248.9$ s/源（严格档）$\Rightarrow 277.8/248.9=\mathbf{1.12}$。

$$\mathrm{LB}_{\rm inst}=\frac{L^*(G)-20-40(n-1)}{5}+5n+5n+\mathbb 1_{n\le15}(20-n)\,c_{\rm det}.\tag{6}$$

（$L^*$ 用 Held-Karp 精确；$n>14$ 时改用 MST——MST 是严格下界、偏保守。计算脚本 `.review/q3q4_attrib.py`，它同时给出**逐项时间归因**与账目恒等式 $T=D/5+5M+S+5C_{\rm ok}+3C_{\rm fail}$ 的逐动作核对，残差 $\le10^{-2}$ s。）

**表 4　两套口径下的实测对照（问题三）**

| 项目 | 对照策略（贪心顺序） | **本文策略** |
|---|---|---|
| 实测（100 局，全部 $100/100$ 全清、零中止） | $488.5$ s/源 | **$277.8$** s/源 |
| 虚拟时间（每局） | $6119.6$ s | $3466.5$ s |
| 实测 / 最坏档下界（严格档 $302.0$）**仅存档，勿作接近度** | $\approx1.62$ | $\approx\mathbf{0.92}$ |
| 实测 / **同 $n$ 口径**（$n\!\approx\!13$，$248.9$） | $\approx1.96$ | $\approx\mathbf{1.12}$ |
| 实测 / **实例级下界**（式 (6)） | $\approx3.17$（30 局） | 需按当前算法重算（逐局归因需运行日志） |

> **本文策略 = 在“中心 + 6 环点 @1300 m”构型上接入第一/二问结论**（本文档 §5.1 以下同）：覆盖顺序由一次性开链 TSP 给定、源按最小插入代价并入剩余路线、清源链按运行期 $R^*\le16$ m 证书条件化。**口径提醒**："实测 / 最坏档"一列已失去比较意义（$n=10$ 档与均值本就不同口径，甚至出现 $<1$）；**引用"还能压多少"时一律用同 $n$ 口径（$1.12$）**，并在正文注明：它**未含**"巡检 + 逐点判空"这类下界不覆盖的工程成本，故真实的可压缩量比这两个数更小。构型参数与对照明细见 `main/过程记录/问题三_版本与对比.md`。

**差额去向（可测的组件效应，同 30 局配对）**：谨慎贪心观测点 $+9.44$ s/源（$se\,1.30$，$28/30$ 局更慢）；自适应按频道判空 $-0.07$（与"无信息测量跳过"效果重叠）；垂直偏移与工程件合关 $+0.6$（$30$ 局未触发）；构型换"中心 $+$ 8 环点 @1000 m" $+0.6$（噪声内）——**总时间由移动主导**，可测的机制收益只有观测点选择一项。逐项时间归因（按恒等式 $T=D/5+5M+S+5C_{\rm ok}+3C_{\rm fail}$ 逐动作核对，残差 $\le10^{-2}$ s，脚本 `.review/q3q4_attrib.py`）与分量方差分解登记在 `main/过程记录/问题三_版本与对比.md`。

**结论**：下界 (4) 式的行程项只覆盖"访问源的 $20$ m 邻域"，**完全不含搜索/判空所必需的巡检行程**（对照策略占 $41.5\%$），因此"实测/下界"**不可能**接近 $1$。真正可压缩的是四项——① 巡检构型（点数与半径）、② 源间行程（贪心 $\to$ 覆盖骨架 $+$ 最小插入代价）、③ 空频道判空的逐点测量（正比于点数）、④ 清源链复测次数；本文策略对四项分别改动后，**同 $n$ 口径由 $1.96$ 降到 $1.12$**——残余差额主要是"巡检行程 $+$ 判空逐点测量"这两项**下界根本不含**的工程成本（故 $1.12$ 只是**上界意义**的"可压缩量"，真实可压缩量更小）。

**与项目既有"地板分析"的关系**：`README.md` 第 8 节（地板分析段）给出问题三地板 $\approx231\text{–}287$ s/源（清源行程 TSP 下界 $164$ + 检测 $61$ + 清除 $6$）。本文实测均值 $277.8$ s/源**落在该区间内**，量级合理，但该估计的两个分量都**未给出可证形式**；本文把它严格化：'清源行程下界'实为 §3.5 的 $L^*-\Pi$（表 2 直接最大化的两层构型），'检测'实为引理 2 的 $5$ s/源加上引理 3 的判空成本（严格 $35$ s／松弛 $30$ s 每空频道）。**最坏 $n=10$ 档 $302.0$ s/源仍高于该区间上端**，故该"地板"在**最坏实例**意义上确实偏松。

---

## 6 紧性与适用边界（诚实交代）

| 结论 | 地位 | 说明 |
|---|---|---|
| 定理 0、引理 1、引理 4、引理 5、定理 1 的不等式结构 | **定理** | 证明在 §3，只用到题面与附录 1/2 |
| 引理 2（$\ge5n$） | **定理（$\exists$-实例口径；per-run 只给 $5(n-z)$）** | 按 $\lvert P_k\rvert\ge1$ / $\lvert P_k\rvert=0$ 分类；对抗实例的构造见 §3.2 |
| 引理 3（判空 $\ge35$ s 严格 / $\ge30$ s 松弛，每空频道） | **定理（分两档）** | 精确伪装集（空频道无成功清除故无分歧）+ 打靶/面积/边界弧三约束成本最小化（$30$ s，自证）；严格档 $35$ s 用 $m_{\rm cov}\ge7$，其依据是**经典 6 圆覆盖临界值 $1.798907$（未独立证明）** |
| 表 2 的每个 $L^*$ | **定理（对具体实例）** | Held-Karp 精确值；实例参数 $(k,m,\rho,\phi)$ 由脚本输出可精确复原 |
| "表 2 的 $L^*$ 是两层族内最大" | **数值最大化（直接最大化 $L^*$）** | 两层族内全扫取最大；证据 `.review/q3q4_Lstar_max.py` |
| "两层构型是 $L^*$ 的全局最大构型" | **未证明** | 只在两层构型族内做了数值最大化。若存在更长构型，下界只会更高，本文结论仍成立 |
| $m_{\rm cov}\ge5$ | **定理** | 打靶证书（内接正五边形相邻弦 $2116.0$ m $>2000$ m，手工可核验） |
| $m_{\rm cov}\ge6$ | **定理** | 边界弧证书：半径 1000 盘最多覆盖 $\partial\Omega$ 的 $67.4980^\circ$ |
| $m_{\rm cov}\ge7$ | **文献档** | 经典 6 圆覆盖临界半径 $1.798907<1.8$（余量 $1.1$ m）；本队独立复核（对称族全扫）：$k=6$ 最优最大覆盖半径 $\approx1046.4$ m $>1000$ m、$k=7$ 为 $900.0$ m。**该临界值未独立证明**，故与 $30$ s 松弛档并列 |
| $n$ 均匀取值的下界均值 $253.0$（严格）/ $250.3$（松弛）s/源 | **估计** | 各 $n$ 下界的算术平均，官方 $n$ 分布未知；与 §8 的**随机实例期望**是两回事 |
| 最坏角间隙类结论（《问题四.md》§5.5 与《问题四_公式.md》式 (5)） | **数值证书（须用边界稳健网格）** | 20 m **对齐**网格会**偏乐观**：同一构型在错位 20 m/对齐 5 m/边界极坐标网格上可差 $8^\circ$；证据 `.review/q4_gap_grid_robust.py` |

**下界不可达的原因（重要）**：(4) 式是若干**独立必要动作**的成本之和，它把"一次测量同时用于定位与判空""顺路清除"等都排除在外，因此是**真下界但通常不可达**。同时它**不含**"覆盖巡视 + 逐点判空"的行程（那是判空的**必要**结构成本，但本下界未计入行程项），故"实测/下界"的比值**不能**读作"离最优还有多少"。它的用途是：① 给出任何策略都无法突破的最坏实例地板（$n=10$ 时 $302.0$ s/源）；② 证明某些"看起来很省"的方案（如跳过判空、盲搜）不可行；③ 为《问题三.md》§9 的理论分析提供严格支撑。

---

## 7 复现

```bash
# 需要 numpy 的解释器
PV=C:/Users/15350/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe
$PV .review/q3q4_lower_bound.py        # A/C/D/E 段（E 段含已声明的缺陷）
$PV .review/q3q4_bound_audit_probe.py  # 证书独立复算（S1–S5）
$PV .review/q3q4_Lstar_max.py          # 表 1/表 2 的全部数值（直接最大化 L*）
$PV .review/q4_gap_grid_robust.py      # Q4 角间隙的网格稳健性（《问题四.md》§5.5 用）
```

- 脚本（表 1、表 2 与 §5 三档比值的**唯一来源**）：`.review/q3q4_Lstar_max.py`
- 脚本（证书独立复算）：`.review/q3q4_bound_audit_probe.py`（S1 弧证书与 $(a,b)$；S2 $\alpha^*$；S3 $m_{\rm cov}$；S4 判空证据集可删性；S5 引理 2 可加性余量）
- **§8 的严格弱界**：`.review/q3q4_expect_bound.py`（给出 §8 公式 (5) 的期望弱界；既有 `q3q4_lower_bound.py` 的 E 段带漏项、不改）
- **脚本存档**：`.review/q3q4_lower_bound_v1.py` / `q3q4_lower_bound_v1.log`、`.review/q3q4_lower_bound.py` / `q3q4_lower_bound.log`——均为只读证据，说明见 `main/过程记录/问题三_版本与对比.md` §9
- 运行时间：`q3q4_Lstar_max.py` 约 15 min（$n=10..16$ 的两层族全扫 + Held-Karp）；其余脚本各 $<3$ min

---

## 8 随机实例（期望）口径的补充界

**口径声明（重要）**：本文第 3–4 节的主定理形式是"**存在**合法实例使 $T/n\ge X$"，即**最坏实例下界**。它约束的是任何策略在**最难的那个实例**上的表现；若赛题最终按**随机实例的平均**口径评分，最坏界**不直接**约束平均值，二者不可混用。本节给出随机口径的补充界，随机模型取本地模拟器所用者（官方未公开）：$n\sim U\{10,\dots,16\}$，源在半径 1800 m 圆域内均匀分布。

**【定理，严格弱界】** 轨迹必须到达最远的源，故 $L^*(O;G)\ge\max_i\lVert G_i\rVert$。由 $\lVert G_i\rVert=1800\sqrt{U_i}$（$U_i$ i.i.d. $U[0,1]$）与 $\mathbb E[\max_i\sqrt{U_i}]=\int_0^1(1-t^{2n})\,dt=\frac{2n}{2n+1}$，

$$\mathbb E\bigl[L^*\bigr]\ \ge\ \mathbb E\Bigl[\max_i\lVert G_i\rVert\Bigr]=\frac{3600\,n}{2n+1},$$

再代入引理 4 的行程折扣 $D\ge L^*-\Pi(n)$、$\Pi(n)=40n-20$，即 $\frac{D}{5}\ge\frac{1}{5}\mathbb E[\max_i\lVert G_i\rVert]-\frac{\Pi(n)}{5}$（$\Pi(n)/5=8n-4$ s），于是

$$\boxed{\ \mathbb E[T/n]\ \ge\ \frac1n\Bigl[\frac15\mathbb E\Bigl[\max_i\lVert G_i\rVert\Bigr]-\frac{\Pi(n)}{5}+5n+5n+\mathbb 1_{n\le15}(20-n)c_{\rm det}\Bigr]\ }.\tag{5}$$

> **计算说明（勿漏项）**：本式必须含 $-\Pi(n)/5$ 的行程折扣（只写 $+10n$ 会高估期望弱界）。本节的数值由 **`.review/q3q4_expect_bound.py`** 给出；数值与出处对照见 `main/过程记录/问题三_版本与对比.md` §5.3。

数值（判空两档并列；本节的界**与对抗构型 $L^*$ 无关**）：

| $n$ | 10 | 11 | 12 | 13 | 14 | 15 | 16 |
|---|---|---|---|---|---|---|---|
| $\mathbb E[\max_i\lVert G_i\rVert]$ (m) | 1714.3 | 1721.7 | 1728.0 | 1733.3 | 1737.9 | 1741.9 | 1745.5 |
| $\Pi(n)/5$ (s) | 76 | 84 | 92 | 100 | 108 | 116 | 124 |
| 下界 (s/源)，**严格档 $c_{\rm det}=35$** | **71.69** | **62.30** | **54.47** | **47.82** | **42.11** | **37.16** | **24.07** |
| 下界 (s/源)，松弛档 $c_{\rm det}=30$ | 66.69 | 58.21 | 51.13 | 45.13 | 39.97 | 35.49 | 24.07 |

$n$ 均匀取值时该严格弱界均值 $\approx\mathbf{48.52}$ s/源（严格档）/ $\mathbf{45.81}$ s/源（松弛档）。它只用到"必须到最远源 + 行程折扣"，**远弱于最坏档，但完全严格且不依赖分布细节**。（$\mathbb E[\max_i\lVert G_i\rVert]=3600n/(2n+1)$ 已由 $3\times10^5$ 组 Monte Carlo 核验：$n=10$ 时解析 $1714.29$ vs MC $1714.31$。）**注意**：$n=16$ 行的判空项为 $0$（清满 16 个即知结束），与表 1 同口径；本节的 $24.07$ 与表 1 的 $206.2$ 差在行程，不是同一量。

**【数值估计（非下界，仅供参照）】** Monte-Carlo（每 $n$ 120 局）给出 MST（由"开路 $\ge$ MST"知 $\mathbb E[\text{MST}]\le\mathbb E[L^*]$）与 NN+2-opt 上界：

| $n$ | 10 | 11 | 12 | 13 | 14 | 15 | 16 |
|---|---|---|---|---|---|---|---|
| $\mathbb E[\text{MST}]$ 估计 (m) | 6747 | 6975 | 7474 | 7775 | 8083 | 8455 | 8763 |
| $\mathbb E[\text{NN+2opt}]$ 估计 (m) | 7980 | 8279 | 8726 | 9046 | 9405 | 9812 | 10132 |

即典型实例的行程约 $1349\text{–}1753$ s（MST 方向，$135\text{–}110$ s/源）到 $1596\text{–}2026$ s（NN+2-opt 方向，$160\text{–}127$ s/源），远小于最坏档的 $2407\text{–}2990$ s（$241\text{–}187$ s/源）——**最坏实例与典型实例差距很大**，这正是两份口径必须分开引用的原因。真值位于两列之间（MST 列是下界方向、NN+2-opt 列是上界方向）。

---

## 9 文献支撑

本文的下界只依赖题面与附录 1/2，**不依赖任何文献**；但下列外部结论用于把某些档位从"自证"提到"更严"，或用于说明该档位在文献中的对应物。完整条目与核对状态见 `参考论文/README.md`（R23–R38）与《问题三.md》§9 参考文献；**卷期页码一律标注【待核对】**（检索时 Semantic Scholar 429、DBLP 拦截、arXiv 断连，仅 OpenAlex/Crossref 可用，未能逐条核准）。

| 用于 | 条目 | 说明 |
|---|---|---|
| §3.4 $m_{\rm cov}\ge7$ | [28] Fejes Tóth 2005 / Melissen 2000 / Gáspár–Tarnai–Hincz 2021 | 6 个半径 1 的圆盘最多覆盖半径 $\approx1.798907$ 的圆域；$1800/1000=1.8$ 超过该临界值 ⇒ 严格档 $c_{\rm det}=35$ s。**该临界值本队未独立证明**，故单列"文献档" |
| §3.5/§3.6 $L^*$ 与 §4.2 表 2 | [26] Held–Karp 1962；[27] Christofides 1976 / Arora 1998 | 精确 DP 给出 $L^*$ 的**可算下界**；近似算法给出上界侧，二者夹逼即为"下界—上界"框架 |
| §3.3 判空必须"覆盖 $\Omega$"的结构性来源 | [22] Ntafos 1992（限视距 watchman route）；[23] Guibas 等 1997/1999；[24] Gerkey–Thrun–Gordon 2006 | 保证性巡视在**有限视距**下必须付出绕行/多次访问；点数的组合下界 [25] Klein–Suri 2014 |
| §5.1 可压缩的四项与后续改进 | [29] Dumitrescu–Mitchell 2003 等（TSPN）；[31] Bertsimas–van Ryzin 1991 / 在线 TSP | "巡检 + 清源"合并成一条开链 = TSP with neighborhoods；"何时中断巡视去清源"有在线竞争比 |
| §7 复现 | 本队自产脚本 | 见 §7 的四条命令；**文献只用于外部档位，数值一律来自脚本** |

---

## 附录　与正文的符号对应

| 本文 | 《问题三.md》正文 | 说明 |
|---|---|---|
| $\Omega$ | $\Omega=B(O,1800)$ | 一致 |
| $L^*(O;G)$ | — | 正文未用；新引入，仅用于下界 |
| $m_{\rm cov}$ | $c(\mathcal P)\le1000$ 的覆盖判据 | 本文是"判空所需的最少证据点数"，正文是"采样构型的覆盖半径"，两者互补 |
| $P_k,Q_k$ | $S_k$（候选域） | 正文用候选域，本文用"动作位置集"，互为对偶 |
| $n$ | $n$ | 一致 |
| $T/n$ | 平均定位清除时间 | 一致（`core.py:221` `avg_clear_time = vt/len(cleared)`） |
