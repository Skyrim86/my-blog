---
# 分层项目的文档页（regular page）：scripts/new-content.sh doc 自动生成
title: "问题四　含定向干扰源的搜索—定位—清除策略与算法"
date: 2026-09-13
draft: false
weight: 1
# 公式本身在构建期已渲染（见 layouts/_markup/render-passthrough.html），这里的 math 只决定
# 本页要不要加载 KaTeX 样式。默认 true（本类文档通常含公式）；纯文字文档改成 false，省约 23KB。
math: true
description: ""
summary: ""
---
# 问题四　含定向干扰源的搜索—定位—清除策略与算法

本章依据赛题正文及其**附录 1（干扰源特征）**、**附录 2（测向机原理）**、**附录 4（测试说明）**建立模型；策略已在**本地模拟器**（按附件 1/2 实现的等价环境）上完成批量演练，所有数值由 `本地测试器/simulator/batch_plan.py` 与 `.review/q4_exclusion.py` 复算。

本章只叙述**策略思想、建模过程、算法设计与实验**；全部公式、符号定义与判据/命题的**陈述**见《问题四_公式.md》，短证明见《问题四_小证明.md》，问题级下界的完整大证明见《问题四_证明_下界.md》，验证交底见`main/过程记录/问题四_证明笔记.md`。

---

## 1 问题重述

在问题三的基础上，源中**混入未知数量、未知朝向的定向源**：定向源的辐射只覆盖"定向方向两侧各 $90^\circ$"（含边界），即一个**闭半平面**。其余条件（源数 $10\sim16$、频道 $1\sim20$ 互不重复、有效接收半径 $1000\sim1500$ m、机器狗自原点出发、初始频道 $1$、确保全部清除）与问题三相同；同样需给出检测与清除策略算法、演练并完成三次正式测试、按**表 1** 填报。

---

## 2 模型假设

1. **平面假设**（同问题三）。
2. **有界误差假设**：示向度误差 $\alpha_i\in[\theta_i\pm1^\circ]$，$\delta=1^\circ$。
3. **定向源的接收模型**：设源的定向方向为 $\varphi$（未知，接口不返回），则点 $p$ 能听到该源，当且仅当"距离 $\le R_{\rm eff}$"**且**"$p$ 落在源的覆盖闭半平面内"（式 (1)，见《问题四_公式.md》）。覆盖角内信号均匀辐射、场强仅随距离衰减、与角度无关（附录 1），故该式中的距离条件与朝向条件**可分离**。
4. **`no_signal` 的三义**：`no_signal` 意味着"① 该频道无未清源，**或** ② 距离 $>R_{\rm eff}$，**或** ③ 该点不在源的覆盖半平面内"，三者不可区分（式 (2)，见《问题四_公式.md》）。
5. **朝向未知的处理口径**：本章采用**最坏情况**（对每个位置要求"**所有**朝向都能被排除"），即给出**保证性**的排除结论。若改用"朝向边缘化"（假设朝向均匀随机）可排除更多区域、需要更少采样点，但**失去保证性**；本章不采用，并在 §5.7 说明差异。
6. **定向源的示向度仍指向源**：附录 2 关于示向度的定义不区分源的类型，故一旦听到，所得方位角即为指向源的真实方位（$\pm1^\circ$）。
7. **每频道至多一源**；同一地点误差固定。

---

## 3 符号说明

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $G_k$ | 频道 $k$ 的源位置（若该频道无源则无定义） | 未知 |
| $\varphi_k$ | 频道 $k$ 的定向方向（朝向）；全向源覆盖 $360^\circ$ | 未知，接口不返回 |
| $u_{\varphi_k}=(\cos\varphi_k,\sin\varphi_k)$ | 定向方向的单位向量（覆盖半平面的外法向） | 无量纲 |
| $H_k$ | 覆盖闭半平面：以 $u_{\varphi_k}$ 为外法向、过 $G_k$ 的闭半平面 | — |
| $C_k$ | 覆盖区 $B(G_k,R_{\rm eff})\cap H_k$（$H_k$ 被有效接收半径截断） | — |
| $R_{\rm eff}$ | 有效接收半径 | $1000\sim1500$ m（保证性推理取 $1000$） |
| $S_k$ | 频道 $k$ 上已取得 `no_signal` 的**观测点集** | 随运行增长 |
| $Q(G)$ | $S_k\cap B(G,1000)$：按最坏有效半径仍"本应听到" $G$ 的观测点 | — |
| $\theta_{\rm gap}(G)$ | 方位角集 $\{\angle(p-G):p\in Q(G)\}$ 的**最大角间隙** | $[0^\circ,360^\circ)$ |
| $s$ | 内层六边形（三角）格的格距 | 取 $1000$ m（要求 $\le1000$ m；$20$ m 对齐网格的基准取 $900$ m，见表 3） |
| $R_{\rm lat}$ | 内层格点的**环上纬度半径**（保留 $\lVert p\rVert\le R_{\rm lat}$） | 取 $1800$ m（基准 $1650$ m） |
| $R_{\rm out}$ | 域外包围环半径（决策变量） | 甜点区 $1900\sim2000$ m；本文策略取 $1900$ m |
| $n_{\rm out}$ | 域外包围环点数（决策变量） | 取 $12$（相位 $15^\circ$） |
| $\mathcal P_4$ | 问题四采样构型 $\mathcal P_4=\text{内层格}\cup\text{外环}$ | 共 $25$ 点 |
| $\theta$ | 示向度（测得方位角），误差 $\in[\theta\pm1^\circ]$ | $[0^\circ,360^\circ)$ |
| $\delta$ | 示向度误差界 | $1^\circ$ |
| $\Omega$ | 目标圆域 $B(O,1800)$ | 半径 $1800$ m |
| $n$ | 干扰源总数 | $10\sim16$（未知） |
| $P_{95}$ | 平均定位清除时间的 $95\%$ 分位数 | s |

> **记号说明**：问题三用 $S_k$ 表示**候选域**（源可能位置集），本章用 $S_k$ 表示**观测点集**——两者互补（观测点集越大，候选域越小）。为避免歧义，本章 $S_k$ **一律指观测点集**。

---

## 4 问题分析

定向源带来的变化**只在"负信息"的解释上**：

1. **`no_signal` 的强度被削弱。** 问题三里一次 `no_signal` 直接排除一个 $1000$ m 圆盘。定向源下，落在这个圆盘里的源只要"背对"观测点就会给出 `no_signal`，因此**单个 `no_signal` 不排除任何位置**（引理 1 见《问题四_小证明.md》§1）。排除必须靠**多个观测点从不同方向"包围"**该位置（定理 3 见《问题四_小证明.md》§2）。
2. **采样密度必须提高。** 由 §5.4，内层格距需从 $\sqrt3\cdot1000$ m 收紧到 $\le1000$ m。
3. **采样范围必须外扩。** 位于圆域边界、朝向域外的源，其覆盖半平面几乎不含域内点，**任何域内观测都无法排除它**（§5.5 实测域内采样盲区 $46.3\%$）。必须把采样点布置到圆域外，且**不是越远越好**——外环半径有**甜点区** $R_{\rm out}\in[1900,2000]$ m，本文取其下界 $1900$ m（更远反而重现盲区，更近则稳健角间隙不足）。
4. **定位与清除不变。** 由假设 6 与附录 2，示向度与朝向无关；`/clear` 只与距离（$\le20$ m）有关。故**一旦听到**，问题三的交会定位与清除链（$R^*\le16$ m 证书直接清除，否则按几何级兜底）完全适用。**问题四的额外难度全部在检测与排除**，成本增量来自采样密度与范围。

**核心主线（三步）**：① `no_signal` 的包围语义（引理 1）$\Rightarrow$ ② 排除判据与采样构型（定理 3 + 表 2/表 3）$\Rightarrow$ ③ 定位清除复用问题三的结论。三步的形式化陈述见《问题四_公式.md》，证明见《问题四_小证明.md》与《问题四_证明_下界.md》。

---

## 5 模型的建立

### 5.1 定向源的接收模型

由式 (1)（见《问题四_公式.md》），固定频道 $k$、固定观测点 $p$，若得到 `no_signal`，则必有"源不在 $G$ 处，**或** $\lVert p-G\rVert>R_{\rm eff}$，**或** $p$ 落在源的覆盖半平面外"三者之一成立，即式 (2)。观测点集 $S_k$ 就是所有给出 `no_signal` 的点。**注意 $S_k$ 不含给出 `direction` 的点**——给出示向度的点说明该处能听到源。

### 5.2 `no_signal` 的包围语义（引理 1）

**命题（引理 1，陈述见《问题四_公式.md》§3.1）**：对任意位置 $G$ 与任意单个 `no_signal` 观测点 $p$，存在某个定向方向 $\varphi$ 使 $p$ 落在 $G$ 的覆盖半平面外，从而"源在 $G$ 且定向方向为 $\varphi$"与"在 $p$ 处 `no_signal`"**完全相容**。

**结论含义**：定向源下，对**未形成包围的有限**观测点集，"某频道在若干点 `no_signal`"**不足以**判定该频道为空；**单个 `no_signal` 不排除任何位置**，排除必须依赖**包围**（一旦凸包条件成立即可判空）。这正是问题四与问题三的关键差别（问题三里 `no_signal` 直接意味着"距离 $>1000$"）。

**完整证明见《问题四_小证明.md》§1**（含推论）。

### 5.3 排除判据（定理 3）

**命题（定理 3，陈述见《问题四_公式.md》§3.2）**：设 $S_k$ 为频道 $k$ 的 `no_signal` 观测点集，对候选位置 $G$ 记 $Q(G)=S_k\cap B(G,1000)$。则 $G$ 可被**保证排除**，当且仅当对每个定向方向 $\varphi$ 都存在 $p\in Q(G)$ 落在覆盖半平面内，当且仅当 $G\in\operatorname{conv}\bigl(Q(G)\bigr)$。

**可计算形式（最大角间隙判据）**：记 $\theta_{\rm gap}(G)$ 为方位角集 $\{\angle(p-G):p\in Q(G)\}$ 排序后的最大角间隙，则有 $\theta_{\rm gap}(G)\le180^\circ$（式 (4)，见《问题四_公式.md》）。实现只需把 $Q(G)$ 中各点的方位角排序求最大间隙，复杂度 $O(\lvert S_k\rvert\log\lvert S_k\rvert)$。

**等价性数值验证**：$300$ 组随机 $(S_k,G)$ 上，最大角间隙判据与"对 $720$ 个朝向逐一检查式 (1)"的判定**零不一致**（`.review/q4_exclusion.py` A 节）。

**两个不可省的定义要点**：① 定义域是 $Q(G)=S_k\cap B(G,1000)$，**距离截断不可省**（只有 $1000$ m 内的观测点才"本应听到"）；② 判据用的是**闭**凸包（$\theta_{\rm gap}=180^\circ$ 的中点型构型也满足，此时 $G$ 落在凸包边界上仍可排除）。这两点的依据与踩坑见`main/过程记录/问题四_证明笔记.md`§4、§7。

**完整证明见《问题四_小证明.md》§2**（分离超平面证明）。

### 5.4 内层采样：格距 $\le1000$ m 的充分性

设内层观测点为六边形（三角）格，格距 $s$。对**远离圆域边界**的 $G$（其 $1000$ m 邻域内的格点未被圆域裁剪）：

- **单调性归约**：令 $\tau=1000/s\ge1$，把构型按 $1/s$ 缩放到单位三角格，阈值 $\tau\ge1$ 时 $Q(h,\tau)\supseteq Q(h,1)$；**加入证据点只会缩小最大角间隙**，故最坏情形在 $\tau=1$（即 $s=1000$ m），此处 $\sup\theta_{\rm gap}=150.0^\circ<180^\circ$（**余量 $\ge30^\circ$**）；
- **两个特例**（均非最坏）：$G$ 落在格点上时最大角间隙 $60^\circ$（$6$ 个最近邻，方位角间隔 $60^\circ$）；$G$ 落在三格点中心时 $120^\circ$（$3$ 个顶点，方位角间隔 $120^\circ$）。$s=900$ m 时 $\sup\theta_{\rm gap}\approx127.77^\circ$。

故**只要 $s\le1000$ m，内层即保证排除**（$\theta_{\rm gap}\le150^\circ<180^\circ$；式 (5)，见《问题四_公式.md》）。本文策略取 $s=1000$ m（余量 $\ge30^\circ$）；表 3 的 $20$ m 对齐网格基准取 $s=900$ m（余量 $\approx52^\circ$）。这正是"格距由 $\sqrt3\cdot1000$ 收紧到 $1000$"的由来——**包围**比**覆盖**苛刻：覆盖只需"最近邻 $\le1000$"（$s\le\sqrt3\cdot1000$），包围还需邻居**方位角分散**。

**充分性论证见《问题四_小证明.md》§3**（单调性归约 + $150^\circ$ 上确界）；`.review/q4_exclusion.py` B 节只检验"格点 + 最近邻点对边中点"，**不覆盖 $150^\circ$ 的最坏点**，不可据其反推 $\theta_{\rm gap}\le120^\circ$。注意：$s\le1000$ m 只保证**远离边界**的 $G$ 被排除，它是内层的**充分**条件，与 §5.5 的"必须域外采样"（必要性）互补。

### 5.5 域外包围环：必要性与参数

**（1）只用域内采样必然有盲区。** 取内层六边形格（$s=950$ m、最大半径 $1740$ m，$13$ 点）作域内采样，在圆域内 $40$ m 网格上逐点判定最大角间隙判据（`.review/q4_exclusion.py` C 节）：

> $6361$ 个检验点中**不可排除 $2944$ 个 = $46.3\%$**；典型盲区点位于圆域边界（如 $(0,-1800)$）。

**机理**：位于边界、朝向域外的源，其覆盖半平面几乎不含域内点，故任何域内 `no_signal` 都无法包围它。**必须域外采样。**

**（2）外环半径存在"甜点区"。** 在域外增加一圈等角点，扫描 $R_{\rm out}$ 与 $n_{\rm out}$（$40$ m 网格上的盲区点占比）：

**表 2　外环参数与盲区（内层 $s=950$ m、最大半径 $1740$ m；表内为盲区占比）**

| $R_{\rm out}$/m | $n_{\rm out}=6$ | 9 | **12** | 15 | 18 | 24 |
|---|---|---|---|---|---|---|
| 1900 | 8.61% | 4.86% | **0.00%** | 0.00% | 0.00% | 0.00% |
| 2000 | 6.54% | 5.94% | **0.00%** | 0.00% | 0.00% | 0.00% |
| 2100 | 7.73% | 8.71% | 1.76% | 0.88% | 0.88% | 0.82% |
| 2200 | 13.71% | 13.74% | 4.78% | 3.02% | 3.08% | 2.45% |
| 2400 | 24.96% | 24.79% | 16.98% | 12.04% | 10.94% | 10.31% |

**结论（与直觉相反）**：外环**不是越远越好**。$R_{\rm out}\ge2100$ m 后，环点到边界源的距离超过 $1000$ m 的比例上升、能参与"包围"的点变少，**即使加密到 24 点仍留盲区**。**甜点区是 $1900\sim2000$ m**；环周长相应为 $11.9\sim12.6$ km。（踩坑机理与依据见`main/过程记录/问题四_证明笔记.md`§4.1、§7。）

**（3）最小无盲区构型（细网格复核）。** 在 $20$ m 网格（$25445$ 个检验点）上复核对若干 $(s,R_{\rm lat},R_{\rm out},n_{\rm out})$ 组合，得最小无盲区构型：

**表 3　问题四的采样构型（细网格 $20$ m 复核，零盲区）**

| 内层格距 $s$ | 内层范围 $R_{\rm lat}$ | 外环半径 $R_{\rm out}$ | 外环点数 $n_{\rm out}$ | 总采样点 | 外环周长 | 检验点盲区 |
|---|---|---|---|---|---|---|
| $900$ m | $1650$ m | $1900$ m | $12$ | **$25$** | $11\,938$ m | **$0$** |

（$25$ 点 = 内层 $13$ 点 + 外环 $12$ 点。外环周长 $11.9$ km $\Rightarrow$ 环上行程约 $2388$ s。）

**（4）角间隙余量与"时间最优"外环.** 判据 $G\in\operatorname{conv}(S_k\cap B(G,1000))$ 等价于一个**角间隙判据**：以 $G$ 为原点、把落在 $B(G,1000)$ 内的采样点方向排序，最大角间隙须 $<180^\circ$（恰好 $180^\circ$ 仍合法：两个反向证据点使任意含 $G$ 的闭半平面必含其一）。据此独立复算：

1. 表 3 的 $25$ 点构型**零盲区**，其最坏角间隙在 $20$ m **对齐**网格上为 $171.74^\circ$，出现在 $G=(1740,-460)$（$|G|=1800$ m，在程证据点 $4$ 个）。§5.4 的"$\theta_{\rm gap}\le150^\circ$（余量 $\ge30^\circ$）"是对**内层格**（$s=1000$ 为单位格长）的推论，**不是整份构型的最坏值**，引用时须区分。
2. **余量口径（敏感程度依构型而异，勿一并套用）**：最坏角间隙对网格步长是否敏感，取决于**构型**。在"内层 $900/\le1650$ $+$ 外环 $1930/12/15^\circ$"这一族上，$20$ m **对齐**网格给 $165.58^\circ$ 而边界稳健网格给 $173.86^\circ$（**偏乐观 $8.28^\circ$**，即该族的"余量"是网格假象）；而在**本文采用的构型**（内层 $1000/\le1800$ $+$ 外环 $1900/12/15^\circ$）上，四种网格（对齐 $20$ m／错位 $20$ m／对齐 $5$ m／边界极坐标）给出的最坏角间隙**全部为 $171.80^\circ$**（差 $0.00^\circ$，见第 $4$ 条），故 $8.20^\circ$ 的余量本身是**网格稳健**的；表 3 构型的两口径为 $171.74^\circ$ vs $171.80^\circ$（差 $0.06^\circ$）。引用余量时一律取"对齐 $5$ m"与"边界极坐标 $5$ m$\times0.25^\circ$（$r\in[1200,1800]$）"两网格的**较大者**（脚本 `.review/q4_gap_1900_vs_1930.py`，与 `.review/q4_gap_grid_robust.py` S7/S9 段同判据）。
3. **外环参数**取 $(R_{\rm out},n_{\rm out},\text{相位})=(1900,12,15^\circ)$：点数不变、四种网格仍零盲区，采样点开链 $18\,181\to17\,692$ m（$-98$ s/局）；且 $\rho=1880$ m 时稳健最坏角间隙升到 $176.25^\circ$、$\rho=1860$ m 时 $180.80^\circ>180^\circ$ **出现盲点**，故 $1900$ m 是甜点区 $[1900,2000]$ m 内可取的最小半径（越近行程越短），实测见 §7.2。
4. **内层格**取 $(s,R_{\rm lat})=(1000,1800)$、外环 $(1900,12,15^\circ)$：点数不变 $25$，且四种网格（对齐 $20$ m／错位 $20$ m／对齐 $5$ m／边界极坐标）上最坏角间隙**稳定在 $171.80^\circ$**（余量 $8.20^\circ$，网格稳健）——**时间与稳健性同时改善**（实测见 §7.2）。脚本见 `.review/q4_gap_1900_vs_1930.py`。
5. 另一族"同心环"构型（中心 $+$ 内环 $+$ 外环）可把点数降到 $21$（判空测量由 $150$ s/空频道降到 $126$ s/空频道），但其最坏角间隙在**对齐 $5$ m 网格上出现 $56$ 个盲点（最坏 $181.99^\circ$）**——**真的存在盲区，故不采用**。

**时间最优的构型选择原则**（与问题三 §5.1(3) 同）：空频道的判空成本是"点数 $\times6$ s"、巡检成本是"采样点开链"，两者都随构型收紧而下降，故应取**零盲区 $+$ 角间隙余量达标**前提下点数与行程最小的构型，而不是仅"点数最少"的构型。扫描与证书见 `.review/q4_tour_scan.py`。

**（5）$25$ 点的"逐点不可删"证书.** 判空成本正比于点数，故"能否减点"直接决定 Q4 的可压缩空间。`.review/q4_trim_scan.py` 给出两项独立证据：

1. **外环点数**（内层格不动）：$n_{\rm out}=11$ 即出现 $0.41\%$ 盲区（最坏角间隙 $242.37^\circ$）、$10\to2.04\%$、$8\to11.18\%$、$6\to25.36\%$，只有 $n_{\rm out}=12$ 零盲区。即 $12$ 个域外点是**必需**的（引理 3 的"$7$ 点覆盖边界弧"只是**必要条件**，不构成整域的包围证书）。
2. **贪心删点**：从 $25$ 点构型逐步删除"删后 $40$ m 网格仍零盲区且角间隙 $\le$ 阈值"中成本最低者——在阈值 $180^\circ$／$175^\circ$／$165^\circ$ 下**删点轨迹均为空**，即**任何一个点都不能单独删除**（两种等点数构型都一样，见 `.review/q3q4_bound_audit_probe.py` S4 段）。

**结论**：$25$ 点是"**逐点不可删**"的最小结构型，而空频道必须在每个采样点各测一次，故 Q4 的判空成本（$25\times6$ s $\times$ 空频道数 $\approx1050$ s/局）**在保证性口径下是结构性不可压缩的**——这解释了为什么 Q4 的实测值（$518.7$ s/源）距问题级下界（$317.0$）仍远：差额主要是下界 (4) 式**根本不含**的"巡检行程 $+$ 逐点判空测量"。

### 5.6 定位与清除：与问题三完全通用

- **定位**：由假设 6，`direction` 给出的示向度仍指向源。故一旦听到，直接用问题三 §5.3 的两视线交会（丢弃交会角 $\le2\delta$ 的视线对）与无框半平面交。
- **清除**：`/clear` 成功仅与距离（$\le20$ m）有关、**与覆盖角无关**，故问题三 §5.4 的清除链（$R^{*}\le16$ m 证书 $\to$ 中心清除；否则 $c_1$ 复测 $+$ 垂直视线偏移 $100$ m 复测，二级半径 $R^{*}_2\le2.1383$ m、$\lVert c_2-G\rVert\le1.9029$ m，见《问题三_小证明.md》§2）**原样适用**；实测中本文策略最常走的是**可行域格点清除**这一兜底分支（P4 消融：关掉它 $+66.6$ s/源，见表 7）。
- **唯一差异**：定向源下"在 $c_1$ 复测"时可能因源背对 $c_1$ 而得到 `no_signal`。此时该次复测不产生视线，应改为**在 $c_1$ 附近绕一圈取一个能听到的点**（算法步骤 8 的兜底）；数值上这一情形仅占少数，且可由射线兜底清除保证收敛。

### 5.7 朝向口径的取舍

| 口径 | 判据 | 采样需求 | 性质 |
|---|---|---|---|
| **最坏情况（本章采用）** | $G\in\operatorname{conv}(S_k\cap B(G,1000))$ | 内层 $s\le1000$ + 外环 | **保证性**：对所有朝向都被排除 |
| 朝向边缘化 | 按朝向先验加权后排除 | 可显著减少点（外环可省） | 仅统计意义，**不保证** |

本章取最坏情况，因为题面要求"**确保**全部清除"；论文中已明确写出该口径，二者可排除的区域不同。

---

## 6 算法设计与复杂度

### 6.1 主算法

```
算法 Q4：含定向源的搜索—定位—清除
输入 : Ω、频道集 K、采样构型 P4 = 内层六边形格(s=1000, R≤1800) ∪ 外环(R=1900, 12 点, 相位 15°)
输出 : 清除记录、虚拟时间

 1  构造巡回路径 tour ← NN(P4) + 2-opt，起点 = 原点
 2  noSig[k] ← ∅（频道 k 的 no_signal 观测点集）；cleared ← ∅
 3  FOR p IN tour:
 4      FOR k IN 尚未定论 的频道:
 5          (res, svd) ← /measure(p, k)
 6          IF res = near:      /clear(p,k); cleared ← cleared ∪ {k}
 7          ELIF res = direction: dets[k] ← dets[k] ∪ {(p,svd)}
 8          ELSE:               noSig[k] ← noSig[k] ∪ {p}     # 负信息记账
 9  巡回结束：对每个 k
10      IF dets[k] ≠ ∅:            转入清源流程（问题三 §5.4）
11      ELIF 用最大角间隙判据判定 "Ω 内所有 G 均可排除":  k 判空    # 包围判据
12      ELSE:                      加密采样（内层减半格距 / 外环加点）后重判
13  WHILE 待清频道: 调用问题三的证书条件化清除链 + 兜底
14  /exit
```

### 6.2 复杂度

| 环节 | 复杂度 | 说明 |
|---|---|---|
| 巡回规划 | $O(m^3)$ | $m=25$，秒级 |
| 逐点检测 | 上界 $25\times20=500$ 次；自适应后平均 $\approx$ 见 §7 | 与问题三同一机制 |
| 逐频道判空 | $O(\lvert S_k\rvert\log\lvert S_k\rvert)$ / 每个检验点 | 实际只对网格检验点抽样判定 |
| **总虚拟时间** | 由**移动距离**主导（25 点巡回） | 见 §7 |

---

## 7 数值实验与验证

### 7.1 正式测试结果（表 1）

> **数据来源说明**：与问题三相同——表 1 应填报**官方模拟器**三次正式测试的结果；本机仅有自研本地模拟器（协议一致、案例编码格式不同、无联网登录），故下表先用本地模拟器按"问题四、含定向源"配置跑出的 3 局结果占位，**保留官方结果替换位**。下表**当前为本地自研模拟器占位，待官方模拟器正式测试后替换**。取数文件与口径见 `本地测试器/simulator/results/README.md`（§2 表格溯源、§3 取数口径）。

**表 1　问题四正式测试结果（本地模拟器，待官方正式测试后替换）**

| 测试 | 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|---|
| 测试 1 | P4-C1F7CD5B | 11 | $709.8$ s | $1.9$ s |
| 测试 2 | P4-6FB2A063 | 16 | $489.5$ s | $1.5$ s |
| 测试 3 | P4-5A368932 | 11 | $780.2$ s | $2.3$ s |

三局的**被清除源个数比例均为 $100\%$**（$11/11$、$16/16$、$11/11$）。程序运行时间远低于 $1200$ s 上限。

### 7.2 批量演练统计

用与正式测试完全相同的程序，在本地模拟器上跑 **100 局**（种子 $1\sim100$）。**口径声明：本节全部统计量都是本地等价模拟器上的数值实验现象（非客观概率、非官方测试结果）；偏差用样本标准差与标准误报告，复现依赖同一随机种子。**取数文件与口径见 `本地测试器/simulator/results/README.md`（§2 表格溯源、§3 取数口径）；本节统一采用**按种子单一口径**（每个种子取一次有效运行）。

**表 4　本文策略批量演练（100 局，本地模拟器；按种子单一口径）**

| 指标 | 值 |
|---|---|
| **全清局数（按种子单一口径，推荐）** | **$100/100$**（$100$ 个种子各取一次有效运行，全部 $100\%$ 清除、零中止） |
| 被清除源个数比例 | $100.0\%$ |
| **平均定位清除时间** | **$518.7$ s** |
| 标准差 / 标准误 | $93.4$ s / $9.34$ s |
| 变异系数 / $P_{95}$ | $18.0\%$ / $668.8$ s |
| 中位 / 最小 / 最大 | $504.6$ s / $342.0$ s / $738.0$ s |
| 虚拟时间 | $6421.3$ s |
| 动作条数（30 局） | $288.0$ |
| 程序运行时间（真实） | 远低于 $1200$ s 上限（真实时间不影响评分，评分量为虚拟时间；见取数文件与 `本地测试器/simulator/results/README.md`） |

> **口径说明**：$100/100$ 是"**每个种子取一次有效运行**"（按种子单一口径）下的结论。本地自研模拟器的 HTTP 服务在每个响应后都发送 `Connection: close`（已实测确认），故每个请求占用一个新的本机临时端口；$100$ 局批量并发下偶发 `WinError 10048` 端口耗尽中止——该中止属**测试环境、非策略失败**，中止种子以低请求速率（$30$ ms 间隔）单独复跑均 $100\%$ 清除（脚本 `.review/retry_aborted.py`，结果存 `results/retry_seeds_result.json`）。为压制请求速率，正式复算对客户端加了 $12\sim30$ ms 的请求间隔，**只增加真实时间、不改变虚拟时间**（评分量）。取数文件 `results/final_q4/batch_20260913_134053.csv`（`--dog βdog`，100 局零中止、零漏源）；动作条数由 `.review/dog_action_count.py` 直接驱动机器狗、统计客户端逻辑动作数（$30$ 局均值，含 enter/exit）；口径见 `本地测试器/simulator/results/README.md`（§2 表格溯源、§3 取数口径）。

**与保底基线（`dog.py`）的对照**（基线取 `batch_20260912_113329.csv`，同 100 局、批次口径；其均值按**全清 $98$ 局**统计）：

**表 5　与保底基线的对照（同 100 局、批次口径）**

| 方案 | 平均定位清除时间/s | 虚拟时间/s | 清除比例 | 动作数 |
|---|---|---|---|---|
| 保底基线 `dog.py`（同 100 局，批次口径） | $2783.2$ | $34728.1$ | $98/100$ 全清（`seed 32`、`53` 各漏 $1$ 源，属**真漏源**、非环境中止；故按种子并集口径仍为 $98/100$） | 约 $2000$ |
| **本文策略（按种子单一口径）** | **$518.7$** | **$6421.3$** | **$100/100$ 全清**（零中止） | **$288.0$** |
| **提升** | **$5.4\times$** | **$5.4\times$** | 且不再漏源 | 约 $6.9\times$ |

**与问题三对照**：**本文策略**（$518.7$ s）约为**问题三本文策略**（$277.8$ s）的 $\mathbf{1.87}$ 倍。其增量来自采样点数由 $7$ 增到 $25$（外环与加密内层）以及**逐点判空成本由 $7\times6=42$ s/空频道升到 $25\times6=150$ s/空频道**，**与理论预期一致**（§4 第 4 点、§7.4 末段）。

**改进的来源**：时间由移动主导（巡检行程约占 $>50\%$，见 §7.5 的组件消融）；**逐项时间归因**（按恒等式 $T=D/5+5M+S+5C_{\rm ok}+3C_{\rm fail}$ 逐动作核对，残差 $\le10^{-2}$ s）与**分量方差分解**登记在 `main/过程记录/问题四_版本与对比.md`，写作层只保留结论：样点构型（$25$ 点开链）与逐点判空测量是**结构性成本**，"可行域格点清除"与"单线补线/沿视线二分"是可压缩的两项（§7.5）。

**可压缩空间的边界**：问题四的虚拟时间由**结构性的巡检**（$25$ 点构型的开链 $17\,692$ m $=3538$ s，占 $>50\%$）与**逐点判空测量**（$|P|\times6$ s/空频道 $=150$ s/空频道，是问题级下界 $50$ s 的 $3$ 倍）主导，而这两项都由"排除判据必须在**每个**采样点取证"这一硬约束决定——**运行期剪枝对判空无效**（域内每个 $G$ 都需要在程证据点，故每个采样点都必须测）。因此在保持"零盲区 + 角间隙余量"的前提下，问题四的可行改进空间主要只有**行程排序**、**外环参数**与**内层格参数**三项；要再压缩必须减少采样点数，而同心环族把点数降到 $21$ 时在 $5$ m 网格上**出现 $56$ 个盲点**，故不采用。

**表 7　组件消融（P4，同 30 局；消融档由 `Q4V_*` 环境变量产生）**

| 配置 | 平均定位清除时间/s | 同种子配对差 | 清除比例 |
|---|---|---|---|
| **本文策略（部署档：内层 $1000$、外环 $1900$、工程件全开）** | $\mathbf{498.4}$ | — | $100\%$（$30/30$） |
| 关"可行域格点清除" | $565.0$ | $+66.6$（$se\,8.17$，$30/30$ 局更慢） | $100\%$ |
| 关"单线补线 $+$ 沿视线二分" | $517.4$ | $+19.0$（$se\,9.69$） | **$13/30$ 全清（$4$ 局漏源）** |
| 关"测量/清除去重缓存 $+$ 完备性护栏" | $499.2$ | $+0.80$（$se\,0.19$，$14/30$ 局更慢） | $100\%$ |
| 路线口径换为"固定覆盖骨架 $+$ 最小插入代价" | $497.1$ | $-1.29$（$se\,1.89$，噪声内） | $100\%$ |
| 外环半径换为 $1930$ m（其余不变） | $509.9$ | $+11.5$（$se\,3.57$） | $100\%$ |

**结论**：① 最大单项是**可行域格点清除**（$+66.6$ s/源）——定向源常出现"真值就在几米外、但该点落在覆盖角外 $\Rightarrow$ 测出 `no_signal`"，用 $28$ m 点阵铺满可行域逐点 `/clear`（失败仅 $3$ s）比沿射线长距离绕行便宜得多；② **单线补线 $+$ 沿视线二分是保证性组件**（关闭后 $4/30$ 局漏源），不只影响速度——这与"巡检结束时只有 $1$ 条视线的频道绝不可判空"是同一条不变量；③ 去重缓存是稳定的小收益（$+0.8$ s/源）；④ 两种路线口径在 $30$ 局上等价（噪声内，$100$ 局 $-2.6\pm1.4$），故按归因干净取"统一目标集 $+$ 在线开链"；⑤ 外环 $1900$ 优于 $1930$（$+11.5$ s/源）。

| **本文策略均值/s** | $666.0$ | $587.9$ | $524.5$ | $493.0$ | $461.2$ | $412.0$ | $399.4$ |
| 本文策略标准差/s | $85.2$ | $87.0$ | $45.0$ | $47.4$ | $36.0$ | $45.6$ | $42.7$ |

（口径与《问题三.md》表 6、表 7 完全一致，取数见 `results/final_q4/batch_20260913_134053.csv` 与 `results/README.md` §2、§3。$P_{95}$ 用线性插值分位数。**平均值随 $N$ 单调下降**（本文策略由 $N=10$ 的 $666.0$ s 降到 $N=16$ 的 $399.4$ s，降幅 $40\%$，比问题三更陡——因为问题四的固定巡检成本占比更大），故引用平均值必须写明 $N$。问题四的变异系数（$18.0\%$）明显高于问题三（$12.6\%$），反映定向源"背向导致补测与兜底次数不同"带来的额外波动。）

### 7.3 排除判据的验证

见 §5.3：最大角间隙判据与"逐方向检查"在 $300$ 组随机构型上**零不一致**（`.review/q4_exclusion.py` A 节；证明见《问题四_小证明.md》§2、§4）。

### 7.4 盲区与外环参数

见 §5.5 表 2、表 3：只用域内采样在圆域边界留下 $46.3\%$ 盲区；加外环后 $R_{\rm out}\in[1900,2000]$ m 且 $n_{\rm out}\ge12$ 时盲区为 $0$；$R_{\rm out}\ge2100$ m 反而重现盲区。理论上，保证判空的域外证据点在纯测量路线上至少需 $7$ 个、角间隔 $\le58.11^\circ$（问题级下界，见《问题四_证明_下界.md》引理 3）。

**为什么问题四 / 问题三 $\approx1.87$ 倍（同代比较，单位 s/源）**：本文策略 $518.7$ s/源 是问题三本文策略 $277.8$ s/源 的 $1.87$ 倍。增量几乎全部来自由**判空判据**决定的两项结构性成本，其余环节两问相同；把两问的最终口径逐项对齐即可看出增高在哪里：

**表 9　问题四与问题三的逐项对齐（同代比较，s/源）**

| 成本项 | 问题三本文策略 | 本文策略 | 倍数 | 成因 |
|---|---|---|---|---|
| **逐点判空测量** | 约 $23$（$7$ 点 $\times6$ s $\times$ 约 $7$ 个空频道 $\div13$ 源） | 约 $81$（$25$ 点 $\times6$ s $\times$ 约 $7$ 个空频道 $\div13$ 源） | $3.5\times$ | 定向源下"单个 `no_signal` 不排除任何位置"，排除必须在**每个**采样点取证（引理 3）；点数 $7\to25$ |
| **巡检行程** | 开链 $7800$ m $=1560$ s/局 | 开链 $17\,692$ m $=3538$ s/局 | $2.3\times$ | 内层格 $+$ 外环共 $25$ 点，且必须**域外采样** |
| 检测 $+$ 切换 $+$ 清除 | — | — | $\approx1.1\times$ | 定向源的"多角度确认"只体现在这里，确实很小 |
| **合计** | $277.8$ | $518.7$ | $1.87$ | 前两项即解释了几乎全部增量 |

即：**问题四的额外难度不在"多测几次"，而在"负信息不再可加"**——空频道必须走遍全部 $25$ 个采样点才能判空（这是判空证明的要求，运行期剪枝无效），于是点数直接乘在判空成本上。这也解释了为什么问题四的可行改进空间只有行程排序与外环/内层格参数三项：**要再压缩就必须减点，而 §5.5(5) 已证明 $25$ 点逐点不可删**。实测比值 $1.87$ 才是与本构型自洽的结论。

### 7.5 验证结论

1. **保证性**：内层格距 $s=1000\le1000$ m 使远离边界的 $G$ 被包围；外环（$R_{\rm out}=1900$ m、$12$ 点、相位 $15^\circ$）消除边界盲区（四种网格零盲区、最坏角间隙 $171.80^\circ$、余量 $8.20^\circ$）。故"全部观测点 `no_signal` 且判据成立 $\Rightarrow$ 该频道无源"在**所有朝向**下成立（代码另有"判无源前必须测遍全部 $25$ 个巡检点"的完备性护栏）。
2. **效率**：按种子单一口径，平均定位清除时间 $518.7$ s，比保底基线（批次口径 $2783.2$ s）快 $5.4$ 倍，动作条数减少约 $6.9$ 倍；且**消除了基线在定向源上的漏检**（基线 $98/100$ 全清、$2$ 个种子各漏 $1$ 源，本策略 $100$ 个种子全部 $100\%$ 清除）。
3. **成本结构符合预期**：本文策略（$518.7$ s/源）相对问题三本文策略（$277.8$ s/源）的 $1.87$ 倍增量，**几乎全部来自两项结构性成本**——逐点判空测量（$42\to150$ s/空频道，$3.5$ 倍）与巡检开链（$1560\to3538$ s/局，$2.3$ 倍）；"多角度确认"本身只贡献约 $1.1$ 倍。定位与清除环节两问共用同一套链路（见 §7.4 末段的逐项对齐表）。
4. **时间稳定性与可复现性**：本文策略的变异系数 $18.0\%$、$P_{95}=668.8$ s、最坏 $738.0$ s（表 4），波动高于问题三（$12.6\%$），原因是定向源"背向"导致补测与兜底次数因局而异——表 7 的消融也显示"关掉单线补线/沿视线二分"会出现 $4/30$ 局漏源，说明该分支的触发与否直接改变单局走势。以**同一配置**重跑种子 $1\sim30$（P4）与 100 局结果的同一批种子**逐位一致**，30 局全部 $100\%$ 清除、零中止（`results/final_q4/batch_20260913_134053.csv` 与 `results/abl_*/`）。

---

## 8 结论

1. **定向源改变了"负信息"的强度**：`no_signal` 有三种不可区分的原因，**单个 `no_signal` 不排除任何位置**（引理 1，证明见《问题四_小证明.md》§1）；排除必须依赖**包围**。
2. **排除判据（定理 3）**：$G$ 可被保证排除 $\iff G\in\operatorname{conv}(S_k\cap B(G,1000))$，可计算形式为**最大角间隙 $\le180^\circ$**（与逐方向检查等价，数值零不一致；证明见《问题四_小证明.md》§2、§4）。
3. **采样必须"变密 + 外扩"**：内层六边形格格距 $\le1000$ m（本文策略取 $1000$ m）；仅用域内采样在圆域边界留下 $46.3\%$ 盲区，**必须域外采样**；外环半径的**甜点区为 $1900\sim2000$ m**（更远反而重现盲区）。
4. **推荐构型**：内层 $s=1000$ m、$R_{\rm lat}=1800$ m（$13$ 点）+ 外环 $R_{\rm out}=1900$ m、$12$ 点（相位 $15^\circ$），共 **$25$ 点**，四种网格（对齐 $20$ m／错位 $20$ m／对齐 $5$ m／边界极坐标）上均零盲区、最坏角间隙稳定 $171.80^\circ$（余量 $8.20^\circ$）；$\rho\le1860$ m 时稳健最坏角间隙 $180.80^\circ>180^\circ$ 出现盲点，故 $1900$ m 是甜点区内可取的最小半径（越近行程越短，实测 $1900$ 比 $1930$ 省 $11.5$ s/源）。这也是**本文算法（`dogs/βdog.py`）的默认构型**，其实测 $518.7$ s/源即本章最终口径。
5. **定位与清除完全复用问题三**：示向度与朝向无关、`/clear` 与朝向无关，故证书条件化的清除链（$R^*\le16$ m 证书直接清除，否则逐级兜底）原样适用；**问题四的额外难度全部在检测与排除**。
6. **实测**：按种子单一口径，本文策略平均定位清除时间 $518.7$ s（$P_{95}=668.8$ s、变异系数 $18.0\%$），比保底基线（批次口径 $2783.2$ s）快 $5.4$ 倍，$100$ 个种子全部 $100\%$ 清除，并消除了基线在定向源上的漏检。平均值随源数 $N$ 单调下降（$N=10$ 时 $666.0$ s、$N=16$ 时 $399.4$ s，表 8），引用时须写明对应的 $N$。
7. **问题级下界**（算法无关，详见《问题四_证明_下界.md》）：最坏 $n=10$ 时 $\ge317.0$ s/源的保证性下界；其中"定向源抬高下界"的可证部分几乎全部来自**判空成本**（每空频道 $35\to50$ s，$35$ s 为文献档），外环的严格绕行下界 $1207.6$ s 被源间行程支配。**口径**：实测均值与"最坏 $n=10$ 档"不可比（同 $n$ 口径 $\approx2.0$、同实例口径 $\approx3.2$，由 `.review/q3q4_attrib.py` 实测档重算），且下界**不含**"巡检 $+$ 逐点判空"的行程，故比值不表示可压缩空间的真实大小。

---

## 9 理论依据与文献支撑

**（1）分离超平面定理.** 定理 3 的核心是"点不属于闭凸集 $\iff$ 存在分离超平面"，这是凸分析的经典结论（文献 [1] 第 11 章）。它把"对所有朝向都存在探测点"这一**连续量词**的判定，化归为一个**凸包/最大角间隙**的有限计算。

**（2）定向视场下的覆盖与包围.** "单点覆盖不足以判定、需要多方向覆盖"在传感器网络与计算几何中有对应命题：定向传感器（摄像机/定向天线）的覆盖判定属"带视场的覆盖/守卫"问题，其保证性判据必须考虑**视场方向**；本章定理 3 给出的是"闭半平面视场 + 距离截断"下的**精确**形式（可由分离超平面自证，不依赖文献）。与本问题结构最接近的是**限视距 watchman route**与"有限视场追逃"（文献 [22]、[24]）：二者都说明"视距有限 ⇒ 保证性巡视必须绕行/多次访问"，这正是本章 §5.5 的采样构型与《问题四_证明_下界.md》引理 3/4 的文献对应物；"最少观察点数"形式的组合下界见 [25]。

**（3）搜索论.** 把 `no_signal` 解释为"对（位置, 朝向）联合假设的部分削弱"，与最优搜索论中"负信息即假设剪除"的框架一致（文献 [21]）；本章取**最坏朝向**（而非边缘化），对应鲁棒/悲观推断（文献 [3]）。**注意**：通道级的"负信息"**不能**用分组测试加速——题面 API 一次只能测一个频道（无合并检测），故 [37] 的思路不适用。

**（4）问题级下界的文献支撑与后续改进方向.** 下界四部分的文献对应物：行程项 = 最短开路精确 DP（[26]）与近似侧 [27]；判空项 = "负信息必须覆盖整个候选域"（[21]）与"外部证据点必须绕边界一圈"（[22]、[25]）；保证性巡视的结构成本 = 限视距巡视/追逃（[22]–[24]）。**后续改进方向**：① "巡检采样点与源清除点统一开链规划"（TSPN，[29]）——本文的"统一目标集 $+$ 在线开路"已是其**在线**版本，进一步可求已知全部源位置时的**离线精确最短开路**作为性能参照；② 纯方位定位的最优观测几何（D-最优，交会角 $\approx90^\circ$）用于选第二/第三视线（[30]）；③ "何时中断巡视去清源"的在线决策与竞争比（[31]）；④ 覆盖巡视路线的分解/生成树构造（[32]–[34]）与带界信息路径规划（[35]）。

---

## 参考文献

沿用问题一至问题三的编号 [1]–[37]（完整条目见《问题三.md》§参考文献与 `参考论文/README.md`）。本章直接用到 [1]（分离超平面）、[3]（有界误差/鲁棒推断）、[21]（最优搜索：负信息剪除）、[22]（限视距 watchman route）、[24]（有限视场追逃）、[25]（捕获界）、[26,27]（$L^*$ 的精确下界与近似上界）、[29]（TSPN：巡检与清源统一的开链规划）、[30]（纯方位定位的最优观测几何）、[31]（何时中断巡视的在线决策）、[32]–[35]（覆盖巡视与信息路径规划）、[37]（分组测试**不适用**的说明）。

> **注**：[20] Lin–Kernighan（TSP 启发式）、[21] Stone（最优搜索论）。[16]–[37] 的书目信息本机无法逐条联网核实（Semantic Scholar 429、DBLP 拦截、arXiv 断连，仅 OpenAlex/Crossref 可用），已在 `参考论文/README.md` 统一标注【待核对】；本章全部结论均可由附录 B 代码与 `.review/q4_exclusion.py`、`.review/q4_gap_grid_robust.py` 复现，**不依赖**文献。

---

## 附录 A　核心算法伪代码

```
函数 EXCLUDABLE(S_k, G):                     # 最大角间隙判据
    Q ← { p ∈ S_k : ‖p−G‖ ≤ 1000 }
    if |Q| < 2: return False
    A ← sort({ atan2(p−G) : p ∈ Q })
    gap ← max( A[i+1]−A[i] )，含首尾环绕
    return gap ≤ 180°

函数 Q4_CONFIG():                            # 本文策略构型（§8.4）
    内层 ← 六边形格(s=1000, 保留 ‖p‖ ≤ 1800)
    外环 ← { 1900·(cos(2πi/12 + 15°), sin(2πi/12 + 15°)) : i = 0..11 }
    return 内层 ∪ 外环                       # 共 25 点；表 3 的 20 m 基准为 (900,1650,1900,12,0°)

函数 Q4_RUN():
    未访问 ← Q4_CONFIG()；已并入 ← ∅
    while 有目标 且 预算未用尽:
        目标 ← 未访问 ∪ { k : R*(k) ≤ 1500 且 (R*(k) ≤ 400 或已有良态双线) }   # 统一目标集
        next ← 对目标作开链 TSP（最近邻 + 2-opt + Or-opt，起点=当前位置）的第一个节点
        if next 是覆盖点 p:
            for k in 待定频道（R*(k) > 120 或尚无视线）:
                if 最乐观后验收缩 < 30 m 或 p 距可行域 > 1500 m: 跳过        # 无信息测量
                res ← /measure(p,k)
                if res=near: /clear(p,k)
                elif res=direction: dets[k].add((p,svd)); noSig 记账
            未访问 ← 未访问 \ {p}
        else: CLEAR_CHAIN(next)                                          # 问题三 §5.4
    对每个无视线且"已测遍全部 25 个巡检点"的频道 k:
        if 对所有网格检验点 G, EXCLUDABLE(noSig[k], G): k 判空              # 完备性护栏
    while 仍有"有视线但未清除"的频道（按可行域中心就近排序）: CLEAR_CHAIN(k)
```

## 附录 B　Python 参考实现

> 本附录给出**问题四正式算法的完整可运行源码**（与支撑材料 `code/q4_planner.py`、`本地测试器/simulator/dogs/βdog.py` 逐字一致；仅把等宽字体缺字形的数学符号改写为 ASCII，逻辑与数值不变）。它可直接作为机器狗客户端接入本地/官方模拟器：`python q4_planner.py --host 127.0.0.1 --port 2026`。

```python
# -*- coding: utf-8 -*-
# 论文附录源码：问题四正式算法（`本地测试器/simulator/dogs/βdog.py`）的逐字副本。
# 改写说明：仅把等宽字体缺字形或易错渲染的数学符号改写为 ASCII 等价写法
# （小于等于、大于等于、蕴含、远小于、交、根号、负号、乘号、中点、范数、箭头、
#   破折号、正负、平方、节号、比例、等价号），**逻辑与数值逐字一致**
# （已用 AST 比对 + 同种子 3 局复跑逐位核对）。
"""
机器狗客户端 -- 问题四（含定向源）：可检性覆盖 + 信念主动定位 + 联合路径规划

算法要点（本文件即论文问题四的正式算法实现，附录 B 为其逐字副本）
 1. **覆盖构型（保证"可检"）**：内层六边形格（格距 s=1000 m、保留 ||p||<=1800 m，13 点）
    ∪ 域外环（R_out=1900 m、12 点、相位 15°），共 25 点。可检性判据：对任意 G，落在
    B(G,1000) 内的采样点方向的最大相邻夹角 <=180° <=> G ∈ conv(S_k ∩ B(G,1000))
    （闭半平面视场下"任意朝向必被检出"的充要条件）。本文构型在四种网格（对齐 20 m／
    错位 20 m／对齐 5 m／边界极坐标）上最坏角间隙稳定 171.80°（余量 8.20°）、零盲区；
    25 点为"逐点不可删"的最小结构型；R_out <=1860 m 时最坏角间隙 180.80°>180° 出现盲点。
 2. **信念（有界不确定性）**：每次测向给出 +/-1° 楔形约束（定向源下 no_signal **不排除
    任何位置**）；可行域 = 场地圆盘 ∩ 圆盘(S_i,1500) ∩ 楔形(S_i, θ_i, +/-1°)，用凸多边形
    半平面精确裁剪（保守），最小包围圆（Welzl）给出圆心 c 与半径 R*。
 3. **联合路径规划**：统一目标集 = 未访问的巡检点 ∪ 已可清除的源；每步对目标集作开链
    TSP（最近邻 + 2-opt + Or-opt，起点 = 当前位置）并取最近的目标前进，于是"发现即顺路
    清除"是算法的自然结果（`Q4V_ROUTE=1` 可切换为"固定覆盖骨架 + 源最小插入代价"口径）。
 4. **测量策略**：每个巡检点只测仍待定的频道；可行域最近点 >1500 m、或最乐观后验收缩
    <30 m 则跳过该次测量；外环点只测"完全未检出"的频道；同一 (频道, 位置) 的结果确定
    => 去重缓存复用（题目明示"同一地点重复检测误差不变"）。
 5. **清除链（与问题三通用）**：R* <= 16 m 时在可行域中心 /clear（真值必在区域内，
    **必然命中**）；否则依次走 可行域格点清除(28 m 点阵, R*<=170 m) -> 单线补线(前进 a +
    侧偏 w) -> 沿视线二分 -> 追踪式逼近 -> 局部 14 m 网格。判无源前另有"必须测遍全部 25 个
    巡检点"的完备性护栏。
 6. **量化**：外环半径取甜点区下界 1900 m 比 1930 m 省 11.5 s/源；组件消融显示最大单项是
    "可行域格点清除"（+66.6 s/源），"单线补线 + 沿视线二分"是**保证性组件**（关掉后
    4/30 局漏源）。

所有开关都可由环境变量 `Q4V_*` 关闭以做消融（论文 Sec 7.5 的消融档即由它们产生）。

实测（本地模拟器，100 局随机案例、种子 1-100）：平均定位清除时间 518.7 s/源
（sd 93.4、CV 18.0%、中位 504.6、P95 668.8，极差 342-738），100/100 全清、零漏源。
"""
import http.client
import json
import math
import os
import random
import time

# ==================== 常量 ====================
ARENA_R = 1800.0
SPEED = 5.0
MAX_VT = 360000.0
MAX_CH = 20

R_MIN = 1000.0            # 有效接收半径下界（可检性设计的保证值）
R_MAX = 1500.0            # 上界：一次 direction 必意味着距离 <=1500
NEAR_R = 5.0
CLEAR_R = 20.0
DELTA = 1.0               # 示向度误差上界（°）
MIN_CROSS = 15.0          # 良态交会最小夹角（°）
SAFE_RANGE = 950.0        # 观测点距可行域最远 <=SAFE_RANGE < R_MIN -> 该处必有信号

# 覆盖构型：内层六边形格（格距 1000 m、半径 1800 m）
# + 域外环（ρ=1900 m、12 点、相位 15°），共 25 点。4 m/0.4° 细采样核验：最坏方位角
# 间隙 171.8° <180°（余量 8.2°）-> 任意位置、任意朝向的定向源必被检出。
# 25 点已证为极小（任一单点删除都会在边界处出现 >180° 间隙）；ρ=1900 相比 1930
# 缩短了开链路径（17692 m vs 18181 m）。
HEX_S, HEX_RMAX = 1000.0, 1800.0
COVER_OUTER_R = float(os.environ.get("Q4V_OUTR", "1900"))
COVER_OUTER_K = 12
COVER_OUTER_PHASE = math.pi / 12.0
COVER_WORST_GAP = 171.798        # 稳健口径（四网格一致）：见 .review/q4_gap_1900_vs_1930.py

# 开关：Q4V_ROUTE=1 -> 「固定覆盖骨架 + 源最小插入代价 + 每步重排」路线（与默认的在线开路口径并列）
Q4V = {
    "ROUTE": os.environ.get("Q4V_ROUTE", "0") != "0",
    "DEDUP": os.environ.get("Q4V_DEDUP", "1") != "0",
    "GUARD": os.environ.get("Q4V_GUARD", "1") != "0",
    "PERP": os.environ.get("Q4V_PERP", "1") != "0",
    "BRACKET": os.environ.get("Q4V_BRACKET", "1") != "0",
    "REG": os.environ.get("Q4V_REG", "1") != "0",
}

VIEW_MAX = 4              # 单频道主动观测次数上限
GRID_SPACING = 14.0
GRID_RMAX = 42.0
BOX = 2600.0

# 是否启用外环（贴边朝外定向源的检出保证）。True=保证全场可检、100% 全清；
# False=仅内层格（更快，但会漏掉贴边朝外的定向源，清除比例约 0.9）。
# 可用环境变量 DOG2_OUTER=0 关闭以便对比评测。
USE_OUTER_SWEEP = os.environ.get("DOG2_OUTER", "1") != "0"

# 贝叶斯提前停止（可选加速档，默认关闭）：走完内层格后用后验 P(仍有未检出源) 判断
# 是否值得走外环。BAYES_Q = 内层构型对单个源的检出率（保守取 0.90）；BAYES_TAU = 停止阈值。
# 注意：这是"用很小的漏检风险换时间"，会牺牲"确保全部清除"，仅在明确接受该口径时开启。
BAYES_STOP = os.environ.get("DOG2_BAYES", "0") == "1"
BAYES_Q = float(os.environ.get("DOG2_BAYES_Q", "0.90"))
BAYES_TAU = float(os.environ.get("DOG2_BAYES_TAU", "0.10"))

# 调参入口
TUNE = {
    "TIGHT": 16.0,        # R* <=TIGHT：可保证一次清除（< CLEAR_R=20）
    "NEAR_FIX": float(os.environ.get("DOG2_NEARFIX", "400")),  # R* <=NEAR_FIX：直奔可行域中心复测
    "LAMBDA": float(os.environ.get("DOG2_LAMBDA", "2.6")),   # 谨慎贪心选点的移动代价权重
    "SKIP_FAR": 1500.0,   # 可行域最近点仍远于此值 -> 测也只会 no_signal，跳过
    "INS_R": float(os.environ.get("DOG2_INS_R", "1500")),    # R* <=INS_R：把该源并入路线
    "STOP_R": float(os.environ.get("DOG2_STOP_R", "120")),   # R* <=STOP_R：不再在监测点重复观测
    "REDUNDANT": 1.0,     # 预留（旧字段）
    "MIN_SHRINK": float(os.environ.get("DOG2_MINSHRINK", "30")),  # 预测收缩 < 此值(m) 则不测
}

# 「可行域格点清除」：定向源常出现「真值就在可行域内、但该点落在覆盖角外 -> 测出
# no_signal」的情形（清除半径 20 m 与朝向无关）。因此只要可行域 R* 不太大，就用
# 覆盖半径 <20 m 的方格点阵铺满可行域，逐点 /clear；由 poly 包含 真值 保证必命中。
# 单点 /clear 失败仅 3 s，远小于沿射线二分/追踪 homing 的绕行代价。
REG_CLEAR_R = float(os.environ.get("DOG2_REG_R", "170"))   # R* <= 此值才铺格点
REG_SPACING = float(os.environ.get("DOG2_REG_S", "28"))    # 方格点阵间距（覆盖半径 <20）
REG_MAX_PTS = int(os.environ.get("DOG2_REG_PTS", "48"))    # 格点上限
REG_EDGE = float(os.environ.get("DOG2_REG_E", "20"))       # 保留距可行域 <= 此值的格点
REG_FULL_ONLY = os.environ.get("DOG2_REG_FULL", "0") == "1"  # 只做能完整覆盖的情形
REG_INLOOP_R = float(os.environ.get("DOG2_REG_IN", "35"))   # 主循环内直接格点清除的 R* 上限


# ==================== 基础几何 ====================
def ring_points(radius, n, phase=0.0):
    return [(radius * math.cos(phase + 2 * math.pi * i / n),
             radius * math.sin(phase + 2 * math.pi * i / n)) for i in range(n)]


def dist(a, b):
    return math.hypot(a[0] - b[0], a[1] - b[1])


def angdiff(a, b):
    d = abs(a - b) % 360.0
    return min(d, 360.0 - d)


def circ180(a, b):
    """两条视线方向的无向夹角（0..90）。"""
    d = abs(a - b) % 180.0
    return min(d, 180.0 - d)


def uvec(deg):
    r = math.radians(deg)
    return (math.cos(r), math.sin(r))


def perp(deg):
    r = math.radians(deg)
    return (-math.sin(r), math.cos(r))


def bearing(p, q):
    return math.degrees(math.atan2(q[1] - p[1], q[0] - p[0]))


def clamp(p, r=ARENA_R + 120.0):
    d = math.hypot(p[0], p[1])
    return (p[0], p[1]) if d <= r else (p[0] * r / d, p[1] * r / d)


def best_pair_angle(rays):
    best = 0.0
    for i in range(len(rays)):
        for j in range(i + 1, len(rays)):
            best = max(best, circ180(rays[i][2], rays[j][2]))
    return best


def hex_lattice(s, rmax):
    """六边形（三角）晶格，格距 s，取半径 <= rmax 的点（含中心）。"""
    pts = []
    ny = int(rmax / (s * math.sqrt(3) / 2)) + 2
    nx = int(rmax / s) + 2
    for j in range(-ny, ny + 1):
        y = j * s * math.sqrt(3) / 2
        for i in range(-nx, nx + 1):
            x = i * s + (j % 2) * s / 2
            if math.hypot(x, y) <= rmax:
                pts.append((x, y))
    return pts


COVER_INNER_PTS = hex_lattice(HEX_S, HEX_RMAX)          # 内层：13 点（含中心）
COVER_OUTER_PTS = ring_points(COVER_OUTER_R, COVER_OUTER_K, COVER_OUTER_PHASE)
N_INNER = len(COVER_INNER_PTS)
COVER_POINTS = COVER_INNER_PTS + COVER_OUTER_PTS      # 25 点


# ==================== 凸区域：半平面/圆裁剪 + 最小包围圆 ====================
def _wedge_halfplanes(S, theta, half=DELTA):
    a1 = math.radians(theta - half)
    a2 = math.radians(theta + half)
    return [(S, (-math.sin(a1), math.cos(a1))), (S, (math.sin(a2), -math.cos(a2)))]


def _clip_hp(poly, q, n):
    """Sutherland-Hodgman：保留 n*(p-q) >= 0 的一侧。"""
    if not poly:
        return poly
    out = []
    m = len(poly)
    for i in range(m):
        a, b = poly[i], poly[(i + 1) % m]
        da = n[0] * (a[0] - q[0]) + n[1] * (a[1] - q[1])
        db = n[0] * (b[0] - q[0]) + n[1] * (b[1] - q[1])
        if da >= -1e-9:
            out.append(a)
        if (da > 1e-9 > db) or (db > 1e-9 > da):
            t = da / (da - db)
            out.append((a[0] + t * (b[0] - a[0]), a[1] + t * (b[1] - a[1])))
    return out


DISK_N = 120


def _disk_halfplanes(c, r):
    """用外接正 DISK_N 边形（含圆，保守）近似圆盘约束。"""
    R = r / math.cos(math.pi / DISK_N)
    out = []
    for k in range(DISK_N):
        ph = 2.0 * math.pi * k / DISK_N
        nx, ny = math.cos(ph), math.sin(ph)
        out.append(((c[0] + R * nx, c[1] + R * ny), (-nx, -ny)))
    return out


def _circum(a, b, c):
    ax, ay = a
    bx, by = b
    cx, cy = c
    d = 2.0 * (ax * (by - cy) + bx * (cy - ay) + cx * (ay - by))
    if abs(d) < 1e-12:
        best = (a[0], a[1], 0.0)
        for p, q in ((a, b), (b, c), (a, c)):
            r = dist(p, q) / 2.0
            if r > best[2]:
                best = ((p[0] + q[0]) / 2.0, (p[1] + q[1]) / 2.0, r)
        return best
    ux = ((ax * ax + ay * ay) * (by - cy) + (bx * bx + by * by) * (cy - ay)
          + (cx * cx + cy * cy) * (ay - by)) / d
    uy = ((ax * ax + ay * ay) * (cx - bx) + (bx * bx + by * by) * (ax - cx)
          + (cx * cx + cy * cy) * (bx - ax)) / d
    return (ux, uy, math.hypot(ax - ux, ay - uy))


def mec(points, seed=20260912):
    """Welzl 最小包围圆：返回 (cx, cy, R)。"""
    pts = list(points)
    if not pts:
        return None
    if len(pts) == 1:
        return (pts[0][0], pts[0][1], 0.0)
    random.Random(seed).shuffle(pts)

    def inside(p, c):
        return math.hypot(p[0] - c[0], p[1] - c[1]) <= c[2] + 1e-7

    c = None
    for i, p in enumerate(pts):
        if c is not None and inside(p, c):
            continue
        c = (p[0], p[1], 0.0)
        for j in range(i):
            q = pts[j]
            if inside(q, c):
                continue
            c = ((p[0] + q[0]) / 2.0, (p[1] + q[1]) / 2.0, dist(p, q) / 2.0)
            for k in range(j):
                r = pts[k]
                if inside(r, c):
                    continue
                c = _circum(p, q, r)
    return c


def region(rays):
    """由测向约束构造可行域。返回 dict(poly, cons, c, R) 或 None。"""
    if not rays:
        return None
    cons = list(_disk_halfplanes((0.0, 0.0), ARENA_R))
    for (x, y, th) in rays:
        cons.extend(_wedge_halfplanes((x, y), th))
        cons.extend(_disk_halfplanes((x, y), R_MAX + 0.5))
    poly = [(-BOX, -BOX), (BOX, -BOX), (BOX, BOX), (-BOX, BOX)]
    for (q, n) in cons:
        poly = _clip_hp(poly, q, n)
        if len(poly) < 3:
            return None
    c = mec(poly)
    if c is None:
        return None
    return {"poly": poly, "cons": cons, "c": (c[0], c[1]), "R": c[2]}


def _region_after(reg, S, theta):
    poly = reg["poly"]
    for (q, n) in _wedge_halfplanes(S, theta):
        poly = _clip_hp(poly, q, n)
        if len(poly) < 3:
            return None
    cc = mec(poly)
    if cc is None:
        return None
    return ((cc[0], cc[1]), cc[2])


def _spread(reg, k=10):
    poly = reg["poly"]
    pts = list(poly)
    m = len(poly)
    for i in range(m):
        a, b = poly[i], poly[(i + 1) % m]
        pts.append(((a[0] + b[0]) / 2.0, (a[1] + b[1]) / 2.0))
    if len(pts) <= k:
        return pts
    st = len(pts) / float(k)
    return [pts[int(i * st)] for i in range(k)]


def _poly_min_dist(p, poly):
    inside = True
    m = len(poly)
    sgn = 0
    for i in range(m):
        a, b = poly[i], poly[(i + 1) % m]
        cr = (b[0] - a[0]) * (p[1] - a[1]) - (b[1] - a[1]) * (p[0] - a[0])
        if cr > 1e-9:
            if sgn < 0:
                inside = False
                break
            sgn = 1
        elif cr < -1e-9:
            if sgn > 0:
                inside = False
                break
            sgn = -1
    if inside:
        return 0.0
    best = float("inf")
    for i in range(m):
        a, b = poly[i], poly[(i + 1) % m]
        dx, dy = b[0] - a[0], b[1] - a[1]
        L2 = dx * dx + dy * dy
        if L2 < 1e-18:
            d = dist(p, a)
        else:
            t = max(0.0, min(1.0, ((p[0] - a[0]) * dx + (p[1] - a[1]) * dy) / L2))
            d = math.hypot(p[0] - a[0] - t * dx, p[1] - a[1] - t * dy)
        if d < best:
            best = d
    return best


def predicted_R(reg, p):
    cx, cy = reg["c"]
    if math.hypot(p[0] - cx, p[1] - cy) < 1e-9:
        return None
    nr = _region_after(reg, p, bearing(p, (cx, cy)))
    return None if nr is None else nr[1]


def choose_view(reg, pos, avoid=None):
    """谨慎贪心选点：最小化「最坏情形后验 R*」+ λ*移动代价。"""
    cx, cy = reg["c"]
    R = reg["R"]
    samples = _spread(reg, 10)
    reach = max(0.0, SAFE_RANGE ** 2 - R * R) ** 0.5
    cands = []
    for L in (max(45.0, 1.6 * R), R + 200.0, 0.55 * reach, 0.85 * reach):
        L = min(L, reach)
        if L < 45.0:
            continue
        for k in range(12):
            ph = 2.0 * math.pi * k / 12.0
            p = (cx + L * math.cos(ph), cy + L * math.sin(ph))
            if p[0] * p[0] + p[1] * p[1] <= ARENA_R * ARENA_R:
                cands.append(p)
    dp = dist(pos, (cx, cy))
    if dp > 1.0:
        ux, uy = (cx - pos[0]) / dp, (cy - pos[1]) / dp
        for L in (max(60.0, dp - R - 150.0), dp * 0.55):
            p = (cx - L * ux, cy - L * uy)
            if p[0] * p[0] + p[1] * p[1] <= ARENA_R * ARENA_R:
                cands.append(p)
    cands.append((cx, cy))
    uniq = []
    for p in cands:
        p = clamp(p)
        if p[0] * p[0] + p[1] * p[1] > ARENA_R * ARENA_R:
            continue
        if avoid is not None and dist(p, avoid) < 25.0:
            continue
        if any(dist(p, u) < 20.0 for u in uniq):
            continue
        uniq.append(p)
    best = None
    for p in uniq:
        if max(dist(p, q) for q in samples) > SAFE_RANGE:
            continue
        worst = 0.0
        for q in samples:
            nr = _region_after(reg, p, bearing(p, q))
            if nr is None:
                continue
            if nr[1] > worst:
                worst = nr[1]
        travel = dist(pos, p) + dist(p, (cx, cy))
        score = worst + TUNE["LAMBDA"] * travel
        if best is None or score < best[0]:
            best = (score, p, worst)
    return None if best is None else best[1]


# ==================== 开链 TSP：最近邻 + 2-opt + Or-opt ====================
def _path_cost(order, nodes, pos_of, start):
    if not order:
        return 0.0
    c = dist(start, pos_of(nodes[order[0]]))
    for a, b in zip(order, order[1:]):
        c += dist(pos_of(nodes[a]), pos_of(nodes[b]))
    return c


def plan_order(start, nodes, pos_of):
    n = len(nodes)
    if n == 0:
        return []
    if n == 1:
        return [0]
    left = list(range(n))
    order, cur = [], start
    while left:
        k = min(left, key=lambda i: dist(pos_of(nodes[i]), cur))
        order.append(k)
        cur = pos_of(nodes[k])
        left.remove(k)
    best = _path_cost(order, nodes, pos_of, start)
    improved = True
    while improved:
        improved = False
        for i in range(n - 1):
            for j in range(i + 1, n):
                cand = order[:i] + order[i:j + 1][::-1] + order[j + 1:]
                c = _path_cost(cand, nodes, pos_of, start)
                if c < best - 1e-9:
                    order, best, improved = cand, c, True
        for seg_len in (1, 2, 3):
            for i in range(0, n - seg_len + 1):
                seg = order[i:i + seg_len]
                rest = order[:i] + order[i + seg_len:]
                for k in range(len(rest) + 1):
                    if k == i:
                        continue
                    cand = rest[:k] + seg + rest[k:]
                    c = _path_cost(cand, nodes, pos_of, start)
                    if c < best - 1e-9:
                        order, best, improved = cand, c, True
    return order


def tsp_open(targets, start, rounds=4):
    """在线开链规划：起点固定 start，返回 (有序目标点, 总长)。

    用距离矩阵做「最近邻构造 + 开链 2-opt + 单点 Or-opt」，O(n^2) 一轮，便于每一步重算。
    统一目标集（监测点 ∪ 待清源）每步重排，取最近的目标
    前进，使「发现即顺路清除」成为算法的自然结果。
    """
    n = len(targets)
    if n == 0:
        return [], 0.0
    P = [start] + list(targets)
    D = [[math.hypot(P[i][0] - P[j][0], P[i][1] - P[j][1]) for j in range(n + 1)]
         for i in range(n + 1)]
    order, left, cur = [], list(range(n)), 0
    while left:
        nxt = min(left, key=lambda j: D[cur][j + 1])
        order.append(nxt)
        left.remove(nxt)
        cur = nxt + 1

    def total(seq):
        s, prev = 0.0, 0
        for x in seq:
            s += D[prev][x + 1]
            prev = x + 1
        return s

    best = total(order)
    improved = True
    while improved and rounds > 0:
        rounds -= 1
        improved = False
        for i in range(n - 1):
            for j in range(i + 1, n):
                a = order[i - 1] + 1 if i > 0 else 0
                b, cc = order[i] + 1, order[j] + 1
                d = order[j + 1] + 1 if j + 1 < n else -1
                old = D[a][b] + (D[cc][d] if d >= 0 else 0.0)
                new = D[a][cc] + (D[b][d] if d >= 0 else 0.0)
                if new < old - 1e-9:
                    order[i:j + 1] = reversed(order[i:j + 1])
                    best += new - old
                    improved = True
        for i in range(n):                     # 单点 Or-opt
            node = order[i]
            rest = order[:i] + order[i + 1:]
            for j in range(n):
                cand = rest[:j] + [node] + rest[j:]
                v = total(cand)
                if v < best - 1e-9:
                    order, best, improved = cand, v, True
                    break
            if improved:
                break
    return order, best                      # 返回下标序列，避免坐标重复造成歧义


# ==================== DogClient ====================
class DogClient(object):
    """问题4：含定向干扰源的可检性覆盖 + 信念主动定位 + 联合路径规划。"""

    def __init__(self, base_url="http://127.0.0.1:2026", robot_id="2026001",
                 note=None, stop_flag=None, max_attempts=6):
        self.base = base_url.rstrip("/")
        self.robot_id = robot_id
        self.note = note or (lambda *a, **k: None)
        self.stop_flag = stop_flag
        self.max_attempts = max_attempts
        self._counter = 0

        self.t_start = None
        self.remaining_real = None
        self.deadline = None

        self.pos = (0.0, 0.0)
        self.channel = 1
        self.vt = 0.0
        self.entered = False
        self.cleared = set()
        self.results = []

        self.rays = {}            # ch -> [(x, y, svd)]
        self.nosig = {}           # ch -> [(x, y)]
        self.no_source = set()
        self._reg = {}            # ch -> region 缓存
        self._clear_failed = set()
        self._grid_done = set()
        self._acq_n = {}
        self._visited_all = False
        self._conn = None
        # 测量/清除去重缓存：电磁环境对同一 (频道, 位置) 确定性不变（题目明示
        # 「同一地点重复检测误差不变」），故同一 (ch, 四舍五入位置) 的测量结果可直接复用；
        # 同一位置的失败清除同样无需重试。这消除了沿射线推进被 clamp 到同一点时
        # 连续重复 /measure（每次 6 s）的浪费。
        self._meas_cache = {}
        self._clear_cache = set()
        # 每个频道实际测过的巡检点集合：只有测遍全部 25 点才可判定「无源」，
        # 避免因预算中断而在未测点处漏掉朝向刚好避开已测点的定向源。
        self._cover_hits = {}

    # ---------- 通信（长连接复用，失败重连） ----------
    @staticmethod
    def _split(base):
        body = base.split("://", 1)[-1]
        hostport = body.split("/", 1)[0]
        host, _, port = hostport.partition(":")
        return host, int(port or 80)

    def _post(self, path, payload):
        data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        last = None
        for attempt in range(self.max_attempts):
            if self.stop_flag is not None and self.stop_flag.is_set():
                raise KeyboardInterrupt("stopped")
            try:
                if self._conn is None:
                    host, port = self._split(self.base)
                    self._conn = http.client.HTTPConnection(host, port, timeout=10)
                self._conn.request("POST", path, body=data,
                                   headers={"Content-Type": "application/json"})
                r = self._conn.getresponse()
                raw = r.read()
                body = json.loads(raw.decode("utf-8"))
                if getattr(r, "will_close", False):
                    self._close()
                if r.status != 200 or not body.get("accepted"):
                    last = RuntimeError("HTTP %d: %s" % (r.status, body))
                    self._close()
                else:
                    return body
            except KeyboardInterrupt:
                raise
            except Exception as e:
                last = e
                self._close()
            time.sleep(0.2 * (attempt + 1))
        raise RuntimeError("%s 连续失败: %s" % (path, last))

    def _close(self):
        try:
            if self._conn is not None:
                self._conn.close()
        except Exception:
            pass
        self._conn = None

    def _rid(self, tag):
        self._counter += 1
        return "%s-%d" % (tag, self._counter)

    def _payload(self, rid):
        return {"arena_id": "default", "robot_id": self.robot_id, "request_id": rid}

    def _budget_ok(self):
        if self.vt > MAX_VT - 3000:
            return False
        if self.deadline is not None and time.monotonic() > self.deadline:
            return False
        return True

    # ---------- 动作 ----------
    def enter(self):
        r = self._post("/enter", self._payload(self._rid("enter")))
        self.entered = True
        self.t_start = time.monotonic()
        self.remaining_real = r.get("remaining_real_duration_s", 1200)
        mx = r.get("max_real_duration_s", 1200)
        if self.remaining_real is not None and self.remaining_real < mx - 5:
            self.deadline = self.t_start + max(0, self.remaining_real - 15)
        self.vt = r["virtual_time_s"]
        self.note("进入：remaining_real_duration_s=%s" % self.remaining_real)

    def measure(self, x, y, ch):
        x, y, ch = round(x, 3), round(y, 3), int(ch)
        if ch in self.cleared:               # 已清除频道不可能再现信号
            return "no_signal", None
        key = (ch, x, y)
        if Q4V["DEDUP"] and key in self._meas_cache:   # 同点同频道结果确定 -> 直接复用，省 6 s
            return self._meas_cache[key]
        p = self._payload(self._rid("measure"))
        p["position"] = {"x": x, "y": y}
        p["channel"] = ch
        r = self._post("/measure", p)
        self.pos, self.channel, self.vt = (x, y), ch, r["virtual_time_s"]
        self._reg.pop(ch, None)
        res, svd = r.get("measure_result"), r.get("svd_deg")
        if res == "no_signal":
            self.nosig.setdefault(ch, []).append((x, y))
        elif res == "direction" and svd is not None:
            self.rays.setdefault(ch, []).append((x, y, float(svd)))
        elif res == "near":
            self.note("频道%d 近距离信号 @ (%.1f, %.1f)" % (ch, x, y))
        self._meas_cache[key] = (res, svd)
        return res, svd

    def clear(self, x, y, ch):
        x, y, ch = round(x, 3), round(y, 3), int(ch)
        key = (ch, x, y)
        if Q4V["DEDUP"] and key in self._clear_cache:  # 同点同频道已失败过 -> 不再重试
            return "no_target_in_range"
        p = self._payload(self._rid("clear"))
        p["position"] = {"x": x, "y": y}
        p["channel"] = ch
        r = self._post("/clear", p)
        self.pos, self.vt = (x, y), r["virtual_time_s"]
        res = r.get("clear_result")
        if res == "success":
            self.cleared.add(ch)
            self.results.append((ch, x, y))
            self._reg.pop(ch, None)
            self.note("频道%d 已清除 @ (%.1f, %.1f)" % (ch, x, y))
        else:
            self._clear_cache.add(key)
        return res

    def exit_(self):
        try:
            r = self._post("/exit", self._payload(self._rid("exit")))
            reason = r.get("exit_reason")
        finally:
            self._close()
        self.entered = False
        return reason

    # ==================== 信念 ====================
    def _region(self, ch):
        if ch in self._reg:
            return self._reg[ch]
        reg = region(self.rays.get(ch, []))
        self._reg[ch] = reg
        return reg

    def _routable(self, ch):
        reg = self._region(ch)
        return reg is not None and reg["R"] <= TUNE["STOP_R"]

    def _pending(self):
        out = []
        for ch in range(1, MAX_CH + 1):
            if ch in self.cleared or ch in self.no_source:
                continue
            if not self._routable(ch):
                out.append(ch)
        return out

    def _score(self, ch):
        I = 0.35 if self.rays.get(ch) else 1.0
        C = 0.0 if ch == self.channel else 1.0
        return I / (C + 0.05)

    # ==================== 监测点扫描 ====================
    def _scan_point(self, p, only_unseen=False):
        pend = self._pending()
        if only_unseen:
            pend = [ch for ch in pend if not self.rays.get(ch)]
        if not pend:
            return
        pend.sort(key=self._score, reverse=True)
        self.note("监测点(%.0f,%.0f)：%d 个待定频道%s"
                  % (p[0], p[1], len(pend), "（仅未检出）" if only_unseen else ""))
        for ch in pend:
            if not self._budget_ok():
                return
            if ch in self.cleared:
                continue
            reg = self._region(ch)
            if reg is not None:
                if _poly_min_dist(p, reg["poly"]) > TUNE["SKIP_FAR"]:
                    continue
                pr = predicted_R(reg, p)
                # 只有「最乐观情形下可行域收缩仍不足」才跳过该点测量：
                # predicted_R 假定新视线恰好指向当前可行域中心，是最佳情形；连最佳情形
                # 都缩不动 MIN_SHRINK，实测更不会有用。（旧写法 pr > R 恒不成立，等于没跳。）
                if pr is not None and pr > reg["R"] - TUNE["MIN_SHRINK"]:
                    continue
            res, svd = self.measure(p[0], p[1], ch)
            self._cover_hits.setdefault(ch, set()).add((round(p[0], 1), round(p[1], 1)))
            if res == "near":
                self.clear(p[0], p[1], ch)

    # ==================== 主动定位与保证清除 ====================
    def _perp_ray(self, ch):
        """补第二条视线：从已有视线的**测点**出发，沿「射线前进 a + 侧偏 w」取点。

        理论依据：源必在射线上且距锚点 <= 其有效
        半径 R，取 p = S1 + a*û + w*n̂ 时 |p-G|^2 = (d-a)^2 + w^2，在 d∈[0,R] 上有界
        (R-a)^2+w^2；代入 R 的下界 1000 m，当 a=500、|w|<=250 时 |p-G| <= 587 m <<1000 m，
        即**必在接收半径内**、且交会角良态。候选按「离当前位置由近到远」排序先试近的。
        """
        rays = self.rays.get(ch, [])
        if not rays:
            return False
        # 候选池分两族，各取最近若干再合并--避免"离当前位置最近的全落在盲区一侧"：
        #   锚点族 p = S1 + a*û + w*n̂（u 指向源，保证 |p-G| <= (R-a)^2+w^2 的界，必在接收半径内）
        #   中心族 在可行域中心 c 附近沿射线/横向短距探针（就近、便宜）
        a_cands, c_cands = [], []
        for (x, y, th) in list(rays)[-2:]:
            S = (x, y)
            u = uvec(th)
            n = perp(th)
            for (a, w) in ((500.0, 250.0), (500.0, -250.0), (300.0, 250.0),
                           (300.0, -250.0), (150.0, 150.0), (150.0, -150.0)):
                p = clamp((S[0] + u[0] * a + n[0] * w, S[1] + u[1] * a + n[1] * w))
                a_cands.append((dist(p, self.pos), p))
        reg = self._region(ch)
        # 中心族只在**已有良态双线**（可行域中心才有意义）时使用；单线时可行域是一个大
        # 楔形、中心无信息，用它探针会白跑（实测某些定向源因此在中心族上浪费 ~200 s）。
        if reg is not None and len(rays) >= 2 and best_pair_angle(rays) >= MIN_CROSS:
            cu = uvec(rays[-1][2])
            cn = perp(rays[-1][2])
            cxx, cyy = reg["c"]
            for (a, w) in ((120.0, 0.0), (-80.0, 0.0), (100.0, 150.0), (100.0, -150.0),
                           (200.0, 150.0), (200.0, -150.0), (250.0, 0.0),
                           (0.0, 150.0), (0.0, -150.0)):
                p = clamp((cxx + cu[0] * a + cn[0] * w, cyy + cu[1] * a + cn[1] * w))
                c_cands.append((dist(p, self.pos), p))
        a_cands.sort(key=lambda t: t[0])
        c_cands.sort(key=lambda t: t[0])
        # 中心族（若有，近且便宜）优先；锚点族按离当前位置由近到远试，共上限 6 次。
        cands = c_cands[:4] + a_cands
        tried = 0
        seen = []
        for (_d, p) in cands:
            if tried >= 6:                     # 单次补线上限，避免为顽固源到处跑
                break
            if any(dist(p, q) < 60.0 for q in seen):
                continue
            seen.append(p)
            tried += 1
            if ch in self.cleared or not self._budget_ok():
                return ch in self.cleared
            res, svd2 = self.measure(p[0], p[1], ch)
            if res == "near":
                self.clear(p[0], p[1], ch)
                return ch in self.cleared
            if res == "direction" and svd2 is not None:
                if best_pair_angle(self.rays.get(ch, [])) >= MIN_CROSS:
                    return True
        return False

    def _ray_bracket_clear(self, ch, max_d=1500.0, step=100.0):
        """沿最新视线向源推进，用「有信号->无信号」的翻转点二分定出源距，再清除。

        定向源在覆盖角边界处常只拿到单条视线；沿该视线朝源走，进入覆盖角内即有信号，
        越过源后立即落入盲区变 no_signal，故翻转点就是源的距离。对 R 小于距离的情形
        同样成立（信号只会出现在源附近的一段）。
        """
        rays = self.rays.get(ch, [])
        if not rays:
            return False
        x0, y0, svd = rays[-1]
        ux, uy = uvec(svd)
        first = last_sig = None
        hi = None
        # 采样距离表：近段 40 m 细步（源可能就在锚点附近，避免粗步直接越过盲区），
        # 远段 100 m 粗步。命中速度与稳健性折中。
        ds, d = [], 0.0
        while d < 400.0 - 1e-9:
            d += 40.0
            ds.append(d)
        while d < max_d - 1e-9:
            d = min(max_d, d + step)
            ds.append(d)
        for d in ds:
            if not self._budget_ok():
                return ch in self.cleared
            p = clamp((x0 + d * ux, y0 + d * uy))
            res, _sv = self.measure(p[0], p[1], ch)
            if res == "near":
                self.clear(p[0], p[1], ch)
                return ch in self.cleared
            if res == "direction":
                if first is None:
                    first = d
                last_sig = d
            elif first is not None:
                hi = d
                break
        if first is None:
            return False
        if hi is None:
            hi = max_d
        lo = last_sig if last_sig is not None else first
        for _ in range(7):
            mid = 0.5 * (lo + hi)
            if not self._budget_ok():
                break
            p = clamp((x0 + mid * ux, y0 + mid * uy))
            res, _sv = self.measure(p[0], p[1], ch)
            if res == "near":
                self.clear(p[0], p[1], ch)
                return ch in self.cleared
            if res == "direction":
                lo = mid
            else:
                hi = mid
        for d2 in (lo, 0.5 * (lo + hi), hi):
            p = clamp((x0 + d2 * ux, y0 + d2 * uy))
            if self.clear(p[0], p[1], ch) == "success":
                return True
        return False

    def _resolve(self, ch):
        """主动观测 -> 收缩可行域 -> 保证清除；单线定向源先法向补线、再沿视线二分定位。"""
        if ch in self.cleared or not self._budget_ok():
            return
        for _ in range(VIEW_MAX):
            if ch in self.cleared or not self._budget_ok():
                return
            reg = self._region(ch)
            if reg is None:
                break
            if reg["R"] <= TUNE["TIGHT"]:
                break
            rays = self.rays.get(ch, [])
            if len(rays) >= 2 and best_pair_angle(rays) >= MIN_CROSS:
                # 可行域已很小时直接铺格点清；否则先「到可行域中心量测」--量测便宜且
                # 信息量大，能把 R* 再压小，之后的格点清除点更少（先清后测会多铺很多点）。
                if Q4V["REG"] and reg["R"] <= REG_INLOOP_R and self._region_clear(ch):
                    return
                if reg["R"] <= TUNE["NEAR_FIX"]:
                    p = reg["c"]
                else:
                    p = choose_view(reg, self.pos, avoid=self.pos) or reg["c"]
            else:
                if self._acq_n.get(ch, 0) < 2:      # 每频道最多补线 2 次
                    self._acq_n[ch] = self._acq_n.get(ch, 0) + 1
                    if Q4V["PERP"] and self._perp_ray(ch):
                        continue
                break
            res, svd = self.measure(p[0], p[1], ch)
            if res == "near":
                self.clear(p[0], p[1], ch)
                return
        if ch in self.cleared:
            return
        # 保证清除：R*<=TIGHT 时真值必在可行域内、与 c 距离 <CLEAR_R
        for _ in range(3):
            reg = self._region(ch)
            if reg is None:
                break
            if reg["R"] <= TUNE["TIGHT"]:
                if self.clear(reg["c"][0], reg["c"][1], ch) == "success":
                    return
            if not self._budget_ok():
                break
            c = reg["c"]
            res, svd = self.measure(c[0], c[1], ch)
            if res == "near":
                self.clear(c[0], c[1], ch)
                return
            if res != "direction":
                break
        if ch in self.cleared:
            return
        if Q4V["REG"] and self._region_clear(ch):   # 定向源「近在咫尺却测出 no_signal」的死结：
            return                        # poly 包含 真值 -> 格点覆盖清除必命中
        if Q4V["BRACKET"] and self._ray_bracket_clear(ch):
            return
        if self._cert_clear(ch):          # 补线/二分过程中射线增多，可能已可保证清除
            return
        if ch in self.cleared:
            return
        if self._home_clear(ch):
            return
        if self._cert_clear(ch):
            return
        reg = self._region(ch)
        if reg is not None and ch not in self._grid_done:
            self._grid_done.add(ch)          # 网格兜底每频道只做一次，避免反复烧时间
            self.note("频道%d 转局部网格兜底" % ch)
            if self._grid_clear(ch, reg["c"][0], reg["c"][1], rmax=GRID_RMAX):
                return
        self._clear_failed.add(ch)
        self.note("频道%d 本轮未能清除" % ch)

    def _region_clear(self, ch):
        """可行域格点清除（定向源的关键补强）。

        可行域多边形 poly 由各测向的 +/-1° 楔形与 <=1500 m 距离约束精确裁出，故真值
        必在 poly 内。用间距 REG_SPACING 的方格点阵（覆盖半径 sqrt2/2*间距 <20 m）铺满
        poly，逐点 /clear：/clear 只看距离、与朝向无关，失败仅 3 s，故只要铺满就必命中。
        专治「源就在 4 m 外、但该点落在定向覆盖角外 -> 测出 no_signal」而被迫沿射线
        二分/homing 长距离绕行的情形。
        """
        reg = self._region(ch)
        if reg is None or reg["R"] > REG_CLEAR_R:
            return False
        poly, c = reg["poly"], reg["c"]
        if self.clear(c[0], c[1], ch) == "success":
            return True
        xs = [p[0] for p in poly]
        ys = [p[1] for p in poly]
        x0, y0, x1, y1 = min(xs), min(ys), max(xs), max(ys)
        nx = int((x1 - x0) / REG_SPACING) + 2
        ny = int((y1 - y0) / REG_SPACING) + 2
        cand = []
        for j in range(ny):
            yy = y0 + j * REG_SPACING
            for i in range(nx):
                p = (x0 + i * REG_SPACING, yy)
                if _poly_min_dist(p, poly) <= REG_EDGE:
                    cand.append(p)
        cand.sort(key=lambda p: dist(p, self.pos))   # 近的优先，省移动
        if REG_FULL_ONLY and len(cand) > REG_MAX_PTS:
            return False                             # 铺不满 -> 不做半吊子覆盖
        for p in cand[:REG_MAX_PTS]:
            if ch in self.cleared:
                return True
            if not self._budget_ok():
                return False
            if self.clear(p[0], p[1], ch) == "success":
                return True
        return False

    def _cert_clear(self, ch):
        """证书清除：可行域最小包围圆半径 R* <= TIGHT 时，真值必在区域内、与 c 距离
        <= R* < 20 m，故在 c 处 /clear 必然命中（20 m 清除半径与朝向无关）。"""
        reg = self._region(ch)
        if reg is None or reg["R"] > TUNE["TIGHT"]:
            return False
        return self.clear(reg["c"][0], reg["c"][1], ch) == "success"

    def _home_clear(self, ch):
        """追踪式 homing：用最新视线方向逐步逼近，步骤递减。"""
        ls = self.rays.get(ch, [])
        if not ls:
            return False
        p = self.pos
        last_good = p
        for step in (200.0, 140.0, 90.0, 60.0, 40.0, 25.0, 15.0):
            if ch in self.cleared or not self._budget_ok():
                return ch in self.cleared
            res, svd = self.measure(p[0], p[1], ch)
            if res == "near":
                self.clear(p[0], p[1], ch)
                return ch in self.cleared
            if res != "direction" or svd is None:
                mid = ((p[0] + last_good[0]) / 2.0, (p[1] + last_good[1]) / 2.0)
                for q in (last_good, mid):
                    if self.clear(q[0], q[1], ch) == "success":
                        return True
                return ch in self.cleared
            last_good = p
            p = (p[0] + step * math.cos(math.radians(svd)),
                 p[1] + step * math.sin(math.radians(svd)))
        return self.clear(p[0], p[1], ch) == "success"

    def _grid_clear(self, ch, cx, cy, rmax=GRID_RMAX, spacing=GRID_SPACING):
        if self.clear(cx, cy, ch) == "success":
            return True
        n = int(rmax / spacing)
        for ring in range(1, n + 1):
            r = ring * spacing
            pts = []
            for i in range(-ring, ring + 1):
                pts.append((cx + i * spacing, cy + r))
                pts.append((cx + i * spacing, cy - r))
            for j in range(-ring + 1, ring):
                pts.append((cx + r, cy + j * spacing))
                pts.append((cx - r, cy + j * spacing))
            for (x, y) in pts:
                if ch in self.cleared:
                    return True
                if not self._budget_ok():
                    return False
                if self.clear(x, y, ch) == "success":
                    return True
        return False

    # ==================== 联合路径规划 ====================
    @staticmethod
    def _pos_of(node, est):
        if node[0] == "pt":
            return COVER_POINTS[node[1]]
        return est.get(node[1]) or (0.0, 0.0)

    def _insert(self, route, node, est):
        pos_of = lambda n: self._pos_of(n, est)
        if not route:
            route.append(node)
            return
        base = dist((0.0, 0.0), pos_of(route[0]))
        for a, b in zip(route, route[1:]):
            base += dist(pos_of(a), pos_of(b))
        best_i, best_c = len(route), None
        for i in range(len(route) + 1):
            if i == 0:
                c = (base - dist((0.0, 0.0), pos_of(route[0]))
                     + dist((0.0, 0.0), pos_of(node)) + dist(pos_of(node), pos_of(route[0])))
            elif i == len(route):
                c = base + dist(pos_of(route[-1]), pos_of(node))
            else:
                c = (base - dist(pos_of(route[i - 1]), pos_of(route[i]))
                     + dist(pos_of(route[i - 1]), pos_of(node))
                     + dist(pos_of(node), pos_of(route[i])))
            if best_c is None or c < best_c:
                best_i, best_c = i, c
        route.insert(best_i, node)

    def _bayes_p_remain(self):
        """后验「仍有未检出源」的概率（总数先验 10..16 + 单源检出率 q 的二项似然）。

        D = 已检出（有视线或已清除）的源数；P(S|D) 正比 P(S)*C(S,D)q^D(1-q)^(S-D)。
        返回 (1 - P(S=D|D), D)，即还存在未检出源的估计概率。
        """
        D = len([ch for ch in range(1, MAX_CH + 1)
                 if self.rays.get(ch) or ch in self.cleared])
        q = BAYES_Q
        posts = []
        for S in range(10, 17):
            if S < D:
                continue
            lp = math.log(math.comb(S, D)) + D * math.log(q)
            if S - D > 0:
                lp += (S - D) * math.log(1.0 - q)
            posts.append((S, lp))
        if not posts:
            return 1.0, D
        mx = max(lp for _, lp in posts)
        w = [(S, math.exp(lp - mx)) for S, lp in posts]
        tot = sum(v for _, v in w)
        p0 = sum(v for S, v in w if S == D) / tot
        return 1.0 - p0, D

    def _run_route(self):
        """统一目标集 + 每步在线开路：每步对目标集重排后取最近者前进。

        目标集 = 未访问的监测点 ∪ 已可清除的源（可行域中心）。每一步对目标集做一次
        开链 TSP（最近邻 + 开链 2-opt + Or-opt），取最近的目标前进；于是「发现即顺路
        清除」是算法的自然结果，避免"先跑完巡检再逐个清源"的双程浪费。
        监测点只对仍待定的频道测量；贴边/无信号频道靠监测点路线检出，走完判无源。

        BAYES_STOP=True 时先把内层格走完，再用后验 P(仍有未检出源) 决定是否值得走
        外环（见 `_bayes_p_remain`）--这是"用统计换时间"的档位，默认关闭以保证全清。
        """
        self.note("监测点构型：内层六边格(%.0f,<=%.0f) %d 点 + 外环(%.0f,%d,相位%.0f°)"
                  % (HEX_S, HEX_RMAX, N_INNER, COVER_OUTER_R, COVER_OUTER_K,
                     math.degrees(COVER_OUTER_PHASE)))
        if Q4V["ROUTE"]:
            return self._run_route_insert()
        if USE_OUTER_SWEEP:
            if BAYES_STOP:
                cover_left = list(range(N_INNER))
                outer_left = list(range(N_INNER, len(COVER_POINTS)))
            else:
                cover_left = list(range(len(COVER_POINTS)))
                outer_left = []
        else:
            cover_left = list(range(N_INNER))
            outer_left = []
        guard = 0
        while self._budget_ok() and guard < 1200:
            guard += 1
            if not cover_left and outer_left:          # 内层走完 -> 决定是否走外环
                p_rem, D = self._bayes_p_remain()
                if p_rem <= BAYES_TAU:
                    self.note("贝叶斯停止：已检出 %d 个，P(仍有未检出源)=%.3f <= %.2f，跳过外环"
                              % (D, p_rem, BAYES_TAU))
                    outer_left = []
                else:
                    self.note("贝叶斯续扫：已检出 %d 个，P(仍有未检出源)=%.3f > %.2f"
                              % (D, p_rem, BAYES_TAU))
                    cover_left += outer_left
                    outer_left = []
            targets, kinds = [], []
            for k in cover_left:
                targets.append(COVER_POINTS[k])
                kinds.append(("cover", k))
            for ch in range(1, MAX_CH + 1):
                if (ch in self.cleared or ch in self.no_source
                        or ch in self._clear_failed):
                    continue
                reg = self._region(ch)
                if reg is None or reg["R"] > TUNE["INS_R"]:
                    continue
                rays = self.rays.get(ch, [])
                if not (reg["R"] <= TUNE["NEAR_FIX"]
                        or (len(rays) >= 2 and best_pair_angle(rays) >= MIN_CROSS)):
                    continue          # 单线/近平行：继续在监测点扫描，免费取第二视线
                targets.append(reg["c"])
                kinds.append(("src", ch))
            if not targets:
                break
            ordered, _ = tsp_open(targets, self.pos)
            i = ordered[0]
            kind, val = kinds[i]
            try:
                if kind == "cover":
                    cover_left.remove(val)
                    self._scan_point(COVER_POINTS[val], only_unseen=(val >= N_INNER))
                else:
                    if val in self.cleared:
                        continue
                    self._resolve(val)
            except Exception as e:      # 单步网络/异常不应中断整条巡检
                self.note("步骤异常(ch=%s,%s)：%s" % (val, kind, e))
        self._visited_all = not cover_left

    def _run_route_insert(self):
        """固定覆盖骨架 + 源最小插入代价 + 每步重排（`Q4V_ROUTE=1`）。

        * 覆盖顺序 = 出发点 (0,0) 对全部巡检点**一次性**求开链 TSP（避免逐步重排的抖动）；
        * 频道可行域够小（R* <= INS_R，且 R* <= NEAR_FIX 或已有良态双线）即按**最小插入代价**
          并入**剩余**路线，估计位置随观测更新；
        * 每步用 plan_order 对整条剩余路线做 2-opt/Or-opt 重排，取第一个节点执行；
        * 外环点（下标 >= N_INNER）只测「完全未检出」的频道--内层格已经取到视线的频道
          在边界外环上再测一次几乎无新信息，省下一次测量。
        """
        cover_order = plan_order((0.0, 0.0), list(range(len(COVER_POINTS))),
                                 lambda i: COVER_POINTS[i])
        route = [("pt", i) for i in cover_order]
        est, in_route = {}, set()
        guard = 0
        while route and self._budget_ok() and guard < 1200:
            guard += 1
            for ch in range(1, MAX_CH + 1):
                if (ch in self.cleared or ch in self.no_source
                        or ch in self._clear_failed):
                    continue
                reg = self._region(ch)
                if reg is None or reg["R"] > TUNE["INS_R"]:
                    continue
                rays = self.rays.get(ch, [])
                if not (reg["R"] <= TUNE["NEAR_FIX"]
                        or (len(rays) >= 2 and best_pair_angle(rays) >= MIN_CROSS)):
                    continue          # 单线/近平行：继续在监测点扫描，免费取第二视线
                est[ch] = reg["c"]
                if ch not in in_route:
                    in_route.add(ch)
                    self._insert(route, ("src", ch), est)
            if not route:
                break
            pos_of = lambda n: self._pos_of(n, est)
            route = [route[i] for i in plan_order(self.pos, route, pos_of)]
            node = route.pop(0)
            if node[0] == "pt":
                self._scan_point(COVER_POINTS[node[1]], only_unseen=(node[1] >= N_INNER))
            else:
                ch = node[1]
                in_route.discard(ch)
                if ch in self.cleared:
                    continue
                reg = self._region(ch)
                if reg is not None:
                    est[ch] = reg["c"]      # 用本频道当前的可行域中心（勿用残留值）
                try:
                    self._resolve(ch)
                except Exception as e:
                    self.note("频道%d 清除异常：%s" % (ch, e))
        self._visited_all = all(n[0] != "pt" for n in route)

    # ==================== 主流程 ====================
    def run(self):
        t0 = time.monotonic()
        self.note("问题4策略：内层六边格 + 域外环 可检性覆盖（最坏角间隙 %.1f° <180°）"
                  % COVER_WORST_GAP)
        self.enter()
        try:
            try:
                self._run_route()
            except Exception as e:            # 路线阶段出错也要保证收尾清除不丢
                self.note("路线阶段异常：%s" % e)
            if self._visited_all:
                for ch in range(1, MAX_CH + 1):
                    if (ch not in self.cleared and ch not in self.no_source
                            and not self.rays.get(ch)):
                        # 必须确实测遍全部巡检点才可判无源（保证覆盖定理成立的前提）
                        if (Q4V["GUARD"] and len(self._cover_hits.get(ch, ()))
                                < len(COVER_POINTS)):
                            self.note("频道%d 未测遍全部巡检点，暂不判无源" % ch)
                            continue
                        self.no_source.add(ch)
                        self.note("频道%d 全程无信号，判定无干扰源" % ch)
            # 收尾清除按「就近」顺序（不是频道号顺序）：以可行域中心估计为节点做开链
            # 规划，每清完一个重排一次，避免在场地两端来回空跑。
            tried = {}
            while self._budget_ok():
                pend = []
                for ch in set(self.rays) - self.cleared:
                    reg = self._region(ch)
                    if reg is None or tried.get(ch):
                        continue
                    pend.append((reg["c"], ch))
                if not pend:
                    break
                order, _ = tsp_open([c for c, _ in pend], self.pos)
                ch = pend[order[0]][1]
                tried[ch] = True
                try:
                    self._resolve(ch)
                except Exception as e:
                    self.note("频道%d 清除异常：%s" % (ch, e))
            stuck = [ch for ch in self.rays if ch not in self.cleared]
            if stuck:
                self.note("以下频道未清除: %s" % stuck)
            det = len(set(self.rays) | self.cleared)
            self.note("完备性核验：检出 %d，清除 %d，判无源 %d"
                      % (det, len(self.cleared), len(self.no_source)))
        except KeyboardInterrupt:
            raise
        except Exception as e:
            self.note("运行异常：%s" % e)
        finally:
            det = len(set(self.rays) | self.cleared)
            self.note("完备性核验：检出 %d，清除 %d，判无源 %d"
                      % (det, len(self.cleared), len(self.no_source)))
            try:
                reason = self.exit_()
            except Exception:
                reason = "exit_error"
            dt = time.monotonic() - t0
            n = len(self.cleared)
            self.note("结束(%s)：清除 %d 个，虚拟时间 %.1f s，程序运行 %.1f s，平均 %.1f s"
                      % (reason, n, self.vt, dt, (self.vt / n) if n else 0.0))
        return {"cleared": sorted(self.cleared), "virtual_time": self.vt,
                "program_time": time.monotonic() - t0}


# ==================== 独立运行 ====================
def main():
    import argparse
    ap = argparse.ArgumentParser(description="问题4：含定向干扰源的可检性覆盖 + 信念定位")
    ap.add_argument("--host", default="127.0.0.1")
    ap.add_argument("--port", type=int, default=2026)
    ap.add_argument("--robot", default="2026001")
    args = ap.parse_args()
    dog = DogClient("http://%s:%d" % (args.host, args.port), args.robot,
                    note=lambda s, **k: print("[q4]", s))
    print(json.dumps(dog.run(), ensure_ascii=False))


if __name__ == "__main__":
    main()
```

**调用示例（复现盲区与外环参数）：**

```python
# 需 numpy；见 .review/q4_exclusion.py
#   A 节：判据等价性（300 组随机构型，零不一致）
#   C 节：仅域内采样(s=950, R_lat=1740, 13 点) -> 6361 个检验点中 2944 个不可排除（46.3%）
#   D 节：外环扫描 -> R_out=1900/2000 且 n_out>=12 时盲区 0；R_out>=2100 重现盲区
#   20 m 网格复核：s=900, R_lat=1650, R_out=1900, n_out=12 -> 共 25 点，零盲区
```
