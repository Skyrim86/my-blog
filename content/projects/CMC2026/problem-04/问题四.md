---
# 分层项目的文档页（regular page）：scripts/new-content.sh doc 自动生成
title: "问题四　含定向干扰源的搜索—定位—清除策略与算法"
date: 2026-09-12
draft: false
weight: 1
# 公式本身在构建期已渲染（见 layouts/_markup/render-passthrough.html），这里的 math 只决定
# 本页要不要加载 KaTeX 样式。默认 true（本类文档通常含公式）；纯文字文档改成 false，省约 23KB。
math: true
description: ""
summary: ""
---
# 问题四　含定向干扰源的搜索—定位—清除策略与算法

本章依据赛题正文及其**附录 1（干扰源特征）**、**附录 2（测向机原理）**、**附录 4（测试说明）**建立模型；策略已在**本地模拟器**（按附件 1/2 实现的等价环境）上完成批量演练，所有数值由 `本地测试器/simulator/batch_plan.py` 与 `.review/q4_exclusion.py` 复算。

本章只叙述**策略思想、建模过程、算法设计与实验**；全部公式、符号定义与判据/命题的**陈述**见《问题四_公式.md》，短证明见《问题四_小证明.md》，问题级下界的完整大证明见《问题四_证明_下界.md》，验证交底见《问题四_证明笔记.md》。

---

## 1 问题重述

在问题三的基础上，源中**混入未知数量、未知朝向的定向源**：定向源的辐射只覆盖"定向方向两侧各 $90^\circ$"（含边界），即一个**闭半平面**。其余条件（源数 $10\sim16$、频道 $1\sim20$ 互不重复、有效接收半径 $1000\sim1500$ m、机器狗自原点出发、初始频道 $1$、确保全部清除）与问题三相同；同样需给出检测与清除策略算法、演练并完成三次正式测试、按**表 1** 填报。

---

## 2 模型假设

1. **平面假设**（同问题三）。
2. **有界误差假设**：示向度误差 $\alpha_i\in[\theta_i\pm1^\circ]$，$\delta=1^\circ$。
3. **定向源的接收模型**：设源的定向方向为 $\varphi$（未知，接口不返回），则点 $p$ 能听到该源，当且仅当"距离 $\le R_{\rm eff}$"**且**"$p$ 落在源的覆盖闭半平面内"（式 (1)，见《问题四_公式.md》）。覆盖角内信号均匀辐射、场强仅随距离衰减、与角度无关（附录 1），故该式中的距离条件与朝向条件**可分离**。
4. **`no_signal` 的三义**：`no_signal` 意味着"① 该频道无未清源，**或** ② 距离 $>R_{\rm eff}$，**或** ③ 该点不在源的覆盖半平面内"，三者不可区分（式 (2)，见《问题四_公式.md》）。
5. **朝向未知的处理口径**：本章采用**最坏情况**（对每个位置要求"**所有**朝向都能被排除"），即给出**保证性**的排除结论。若改用"朝向边缘化"（假设朝向均匀随机）可排除更多区域、需要更少采样点，但**失去保证性**；本章不采用，并在 §5.7 说明差异。
6. **定向源的示向度仍指向源**：附录 2 关于示向度的定义不区分源的类型，故一旦听到，所得方位角即为指向源的真实方位（$\pm1^\circ$）。
7. **每频道至多一源**；同一地点误差固定。

---

## 3 符号说明

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $G_k$ | 频道 $k$ 的源位置（若该频道无源则无定义） | 未知 |
| $\varphi_k$ | 频道 $k$ 的定向方向（朝向）；全向源覆盖 $360^\circ$ | 未知，接口不返回 |
| $u_{\varphi_k}=(\cos\varphi_k,\sin\varphi_k)$ | 定向方向的单位向量（覆盖半平面的外法向） | 无量纲 |
| $H_k$ | 覆盖闭半平面：以 $u_{\varphi_k}$ 为外法向、过 $G_k$ 的闭半平面 | — |
| $C_k$ | 覆盖区 $B(G_k,R_{\rm eff})\cap H_k$（$H_k$ 被有效接收半径截断） | — |
| $R_{\rm eff}$ | 有效接收半径 | $1000\sim1500$ m（保证性推理取 $1000$） |
| $S_k$ | 频道 $k$ 上已取得 `no_signal` 的**观测点集** | 随运行增长 |
| $Q(G)$ | $S_k\cap B(G,1000)$：按最坏有效半径仍"本应听到" $G$ 的观测点 | — |
| $\theta_{\rm gap}(G)$ | 方位角集 $\{\angle(p-G):p\in Q(G)\}$ 的**最大角间隙** | $[0^\circ,360^\circ)$ |
| $s$ | 内层六边形（三角）格的格距 | 取 $900$ m（要求 $\le1000$ m） |
| $R_{\rm lat}$ | 内层格点的**环上纬度半径**（保留 $\lVert p\rVert\le R_{\rm lat}$） | 取 $1650$ m |
| $R_{\rm out}$ | 域外包围环半径（决策变量） | 甜点区 $1900\sim2000$ m |
| $n_{\rm out}$ | 域外包围环点数（决策变量） | $\ge12$ |
| $\mathcal P_4$ | 问题四采样构型 $\mathcal P_4=\text{内层格}\cup\text{外环}$ | 共 $25$ 点 |
| $\theta$ | 示向度（测得方位角），误差 $\in[\theta\pm1^\circ]$ | $[0^\circ,360^\circ)$ |
| $\delta$ | 示向度误差界 | $1^\circ$ |
| $\Omega$ | 目标圆域 $B(O,1800)$ | 半径 $1800$ m |
| $n$ | 干扰源总数 | $10\sim16$（未知） |

> **记号说明**：问题三用 $S_k$ 表示**候选域**（源可能位置集），本章用 $S_k$ 表示**观测点集**——两者互补（观测点集越大，候选域越小）。为避免歧义，本章 $S_k$ **一律指观测点集**。

---

## 4 问题分析

定向源带来的变化**只在"负信息"的解释上**：

1. **`no_signal` 的强度被削弱。** 问题三里一次 `no_signal` 直接排除一个 $1000$ m 圆盘。定向源下，落在这个圆盘里的源只要"背对"观测点就会给出 `no_signal`，因此**单个 `no_signal` 不排除任何位置**（引理 1 见《问题四_小证明.md》§1）。排除必须靠**多个观测点从不同方向"包围"**该位置（定理 3 见《问题四_小证明.md》§2）。
2. **采样密度必须提高。** 由 §5.4，内层格距需从 $\sqrt3\cdot1000$ m 收紧到 $\le1000$ m。
3. **采样范围必须外扩。** 位于圆域边界、朝向域外的源，其覆盖半平面几乎不含域内点，**任何域内观测都无法排除它**（§5.5 实测域内采样盲区 $46.3\%$）。必须把采样点布置到圆域外。
4. **定位与清除不变。** 由假设 6 与附录 2，示向度与朝向无关；`/clear` 只与距离（$\le20$ m）有关。故**一旦听到**，问题三的交会定位与两级清除链完全适用。**问题四的额外难度全部在检测与排除**，成本增量来自采样密度与范围。

**核心主线（三步）**：① `no_signal` 的包围语义（引理 1）$\Rightarrow$ ② 排除判据与采样构型（定理 3 + 表 2/表 3）$\Rightarrow$ ③ 定位清除复用问题三的结论。三步的形式化陈述见《问题四_公式.md》，证明见《问题四_小证明.md》与《问题四_证明_下界.md》。

---

## 5 模型的建立

### 5.1 定向源的接收模型

由式 (1)（见《问题四_公式.md》），固定频道 $k$、固定观测点 $p$，若得到 `no_signal`，则必有"源不在 $G$ 处，**或** $\lVert p-G\rVert>R_{\rm eff}$，**或** $p$ 落在源的覆盖半平面外"三者之一成立，即式 (2)。观测点集 $S_k$ 就是所有给出 `no_signal` 的点。**注意 $S_k$ 不含给出 `direction` 的点**——给出示向度的点说明该处能听到源。

### 5.2 `no_signal` 的包围语义（引理 1）

**命题（引理 1，陈述见《问题四_公式.md》§3.1）**：对任意位置 $G$ 与任意单个 `no_signal` 观测点 $p$，存在某个定向方向 $\varphi$ 使 $p$ 落在 $G$ 的覆盖半平面外，从而"源在 $G$ 且定向方向为 $\varphi$"与"在 $p$ 处 `no_signal`"**完全相容**。

**结论含义**：定向源下，对**未形成包围的有限**观测点集，"某频道在若干点 `no_signal`"**不足以**判定该频道为空；**单个 `no_signal` 不排除任何位置**，排除必须依赖**包围**（一旦凸包条件成立即可判空）。这正是问题四与问题三的关键差别（问题三里 `no_signal` 直接意味着"距离 $>1000$"）。

**完整证明见《问题四_小证明.md》§1**（含推论）。

### 5.3 排除判据（定理 3）

**命题（定理 3，陈述见《问题四_公式.md》§3.2）**：设 $S_k$ 为频道 $k$ 的 `no_signal` 观测点集，对候选位置 $G$ 记 $Q(G)=S_k\cap B(G,1000)$。则 $G$ 可被**保证排除**，当且仅当对每个定向方向 $\varphi$ 都存在 $p\in Q(G)$ 落在覆盖半平面内，当且仅当 $G\in\operatorname{conv}\bigl(Q(G)\bigr)$。

**可计算形式（最大角间隙判据）**：记 $\theta_{\rm gap}(G)$ 为方位角集 $\{\angle(p-G):p\in Q(G)\}$ 排序后的最大角间隙，则有 $\theta_{\rm gap}(G)\le180^\circ$（式 (4)，见《问题四_公式.md》）。实现只需把 $Q(G)$ 中各点的方位角排序求最大间隙，复杂度 $O(\lvert S_k\rvert\log\lvert S_k\rvert)$。

**等价性数值验证**：$300$ 组随机 $(S_k,G)$ 上，最大角间隙判据与"对 $720$ 个朝向逐一检查式 (1)"的判定**零不一致**（`.review/q4_exclusion.py` A 节）。

**两个不可省的定义要点**：① 定义域是 $Q(G)=S_k\cap B(G,1000)$，**距离截断不可省**（只有 $1000$ m 内的观测点才"本应听到"）；② 判据用的是**闭**凸包（$\theta_{\rm gap}=180^\circ$ 的中点型构型也满足，此时 $G$ 落在凸包边界上仍可排除）。这两点的依据与踩坑见《问题四_证明笔记.md》§4、§7。

**完整证明见《问题四_小证明.md》§2**（分离超平面证明）。

### 5.4 内层采样：格距 $\le1000$ m 的充分性

设内层观测点为六边形（三角）格，格距 $s$。对**远离圆域边界**的 $G$（其 $1000$ m 邻域内的格点未被圆域裁剪）：

- **单调性归约**：令 $\tau=1000/s\ge1$，把构型按 $1/s$ 缩放到单位三角格，阈值 $\tau\ge1$ 时 $Q(h,\tau)\supseteq Q(h,1)$；**加入证据点只会缩小最大角间隙**，故最坏情形在 $\tau=1$（即 $s=1000$ m），此处 $\sup\theta_{\rm gap}=150.0^\circ<180^\circ$（**余量 $\ge30^\circ$**）；
- **两个特例**（均非最坏）：$G$ 落在格点上时最大角间隙 $60^\circ$（$6$ 个最近邻，方位角间隔 $60^\circ$）；$G$ 落在三格点中心时 $120^\circ$（$3$ 个顶点，方位角间隔 $120^\circ$）。$s=900$ m 时 $\sup\theta_{\rm gap}\approx127.77^\circ$。

故**只要 $s\le1000$ m，内层即保证排除**（$\theta_{\rm gap}\le150^\circ<180^\circ$；式 (5)，见《问题四_公式.md》）。取 $s=900$ m 留 $100$ m 余量。这正是"格距由 $\sqrt3\cdot1000$ 收紧到 $1000$"的由来——**包围**比**覆盖**苛刻：覆盖只需"最近邻 $\le1000$"（$s\le\sqrt3\cdot1000$），包围还需邻居**方位角分散**。

**充分性论证见《问题四_小证明.md》§3**（单调性归约 + $150^\circ$ 上确界）；`.review/q4_exclusion.py` B 节只检验"格点 + 最近邻点对边中点"，**不覆盖 $150^\circ$ 的最坏点**，不可据其反推旧结论 $120^\circ$。注意：$s\le1000$ m 只保证**远离边界**的 $G$ 被排除，它是内层的**充分**条件，与 §5.5 的"必须域外采样"（必要性）互补。

### 5.5 域外包围环：必要性与参数

**（1）只用域内采样必然有盲区。** 取内层六边形格（$s=950$ m、最大半径 $1740$ m，$13$ 点）作域内采样，在圆域内 $40$ m 网格上逐点判定最大角间隙判据（`.review/q4_exclusion.py` C 节）：

> $6361$ 个检验点中**不可排除 $2944$ 个 = $46.3\%$**；典型盲区点位于圆域边界（如 $(0,-1800)$）。

**机理**：位于边界、朝向域外的源，其覆盖半平面几乎不含域内点，故任何域内 `no_signal` 都无法包围它。**必须域外采样。**

**（2）外环半径存在"甜点区"。** 在域外增加一圈等角点，扫描 $R_{\rm out}$ 与 $n_{\rm out}$（$40$ m 网格上的盲区点占比）：

**表 2　外环参数与盲区（内层 $s=950$ m、最大半径 $1740$ m；表内为盲区占比）**

| $R_{\rm out}$/m | $n_{\rm out}=6$ | 9 | **12** | 15 | 18 | 24 |
|---|---|---|---|---|---|---|
| 1900 | 8.61% | 4.86% | **0.00%** | 0.00% | 0.00% | 0.00% |
| 2000 | 6.54% | 5.94% | **0.00%** | 0.00% | 0.00% | 0.00% |
| 2100 | 7.73% | 8.71% | 1.76% | 0.88% | 0.88% | 0.82% |
| 2200 | 13.71% | 13.74% | 4.78% | 3.02% | 3.08% | 2.45% |
| 2400 | 24.96% | 24.79% | 16.98% | 12.04% | 10.94% | 10.31% |

**结论（与直觉相反）**：外环**不是越远越好**。$R_{\rm out}\ge2100$ m 后，环点到边界源的距离超过 $1000$ m 的比例上升、能参与"包围"的点变少，**即使加密到 24 点仍留盲区**。**甜点区是 $1900\sim2000$ m**；环周长相应为 $11.9\sim12.6$ km。（踩坑机理与依据见《问题四_证明笔记.md》§4.1、§7。）

**（3）最小无盲区构型（细网格复核）。** 在 $20$ m 网格（$25445$ 个检验点）上复核对若干 $(s,R_{\rm lat},R_{\rm out},n_{\rm out})$ 组合，得最小无盲区构型：

**表 3　问题四的采样构型（细网格 $20$ m 复核，零盲区）**

| 内层格距 $s$ | 内层范围 $R_{\rm lat}$ | 外环半径 $R_{\rm out}$ | 外环点数 $n_{\rm out}$ | 总采样点 | 外环周长 | 检验点盲区 |
|---|---|---|---|---|---|---|
| $900$ m | $1650$ m | $1900$ m | $12$ | **$25$** | $11\,938$ m | **$0$** |

（$25$ 点 = 内层 $13$ 点 + 外环 $12$ 点。外环周长 $11.9$ km $\Rightarrow$ 环上行程约 $2388$ s。）

**（4）角间隙余量与"时间最优"外环（2026-09-12 新增，v2 策略采用）.** 判据 $G\in\operatorname{conv}(S_k\cap B(G,1000))$ 等价于一个**角间隙判据**：以 $G$ 为原点、把落在 $B(G,1000)$ 内的采样点方向排序，最大角间隙须 $<180^\circ$（恰好 $180^\circ$ 仍合法：两个反向证据点使任意含 $G$ 的闭半平面必含其一）。据此在 $20$ m 网格上独立复算：

1. 表 3 的 $25$ 点构型**零盲区**（与正文一致），但其**最坏角间隙为 $171.74^\circ$**、出现在 $G=(1740,-460)$（$|G|=1800$ m，在程证据点 $4$ 个）——即按 $20$ m 网格的余量只有 $8.26^\circ$（**该网格偏乐观，见下条**）。§5.5 的"$\theta_{\rm gap}\le150^\circ$（余量 $\ge30^\circ$）"是对**内层格**（$s=1000$ 为单位格长）的推论，**不是整份构型的最坏值**，引用时须区分。
2. **网格口径更正（2026-09-12 第 5 版审计）**：上面 $171.74^\circ$ 是 $20$ m **对齐**网格（含 $0,\pm20\dots$）上的值，而**该口径偏乐观**。用四种网格复核同一个 v2 构型（`.review/q4_gap_grid_robust.py`）：对齐 $20$ m $165.58^\circ$、错位 $20$ m $172.18^\circ$、对齐 $5$ m $173.37^\circ$、边界极坐标网格（$5$ m$\times0.25^\circ$，$r\in[1200,1800]$）$173.86^\circ$——即 **v2 构型的真实余量只有 $\le6.14^\circ$**，而原版 $(1900,12,0^\circ)$ 在各网格上稳定在 $171.3^\circ\!-\!171.8^\circ$（余量 $8.2^\circ$）。因此"改外环把余量由 $8.26^\circ$ 提高到 $14.42^\circ$"**不成立**（那是网格假象）；$(1930,12,15^\circ)$ 的**真正收益是开链 $-77$ s**，余量反而略降。
3. **等点数改进（候选 A，已 A/B 实测并采纳）**：把**内层格**由 $(s,R_{\max})=(900,1650)$ 改为 $(1000,1800)$（外环仍 $1930/12/15^\circ$）：点数不变 $25$、开链 $18\,735\to18\,181$ m（$3747\to3636$ s，$-111$ s/局），且四种网格上最坏角间隙**稳定在 $165.34^\circ$**（余量 $14.66^\circ$）——**时间与稳健性同时改善**。**实测（同种子配对 100 局，`results/plan_q4v4a_20260912_212907.csv`）**：均值 $577.0$ vs $581.9$ s/源（配对 $-4.9$ s、$t=-0.84$、$63/100$ 局更优）、虚拟时间 $-57.7$ s/局 ⇒ 时间上**不劣（噪声内略优）**，**采纳的主要理由是稳健余量**（$\le6.14^\circ\to14.66^\circ$）。见 `.review/q4_gap_grid_robust.py` 与 §7.2。
4. 另一族"同心环"构型（中心 $+$ 内环 $+$ 外环）可把点数降到 $21$（判空测量由 $150$ s/空频道降到 $126$ s/空频道），但其最坏角间隙在 $20$ m 网格上为 $179.82^\circ$，**在 $5$ m 网格上出现 $56$ 个盲点（最坏 $181.99^\circ$）**——它不是"余量不足"，而是**真的存在盲区，故不采用**（这比早期"余量 $0.18^\circ$"的理由更硬）。

**时间最优的构型选择原则**（与问题三 §5.1(3) 同）：空频道的判空成本是"点数 $\times6$ s"、巡检成本是"采样点开链"，两者都随构型收紧而下降，故应取**零盲区 $+$ 角间隙余量达标**前提下点数与行程最小的构型，而不是仅"点数最少"的构型。扫描与证书见 `.review/q4_tour_scan.py`。

**（5）$25$ 点的"逐点不可删"证书（2026-09-12 新增）.** 判空成本正比于点数，故"能否减点"直接决定 Q4 的可压缩空间。`.review/q4_trim_scan.py` 给出两项独立证据：

1. **外环点数**（内层格不动）：$n_{\rm out}=11$ 即出现 $0.41\%$ 盲区（最坏角间隙 $242.37^\circ$）、$10\to2.04\%$、$8\to11.18\%$、$6\to25.36\%$，只有 $n_{\rm out}=12$ 零盲区。即 $12$ 个域外点是**必需**的（引理 3 的"$7$ 点覆盖边界弧"只是**必要条件**，不构成整域的包围证书）。
2. **贪心删点**：从 $25$ 点构型逐步删除"删后 $40$ m 网格仍零盲区且角间隙 $\le$ 阈值"中成本最低者——在阈值 $180^\circ$／$175^\circ$／$165^\circ$ 下**删点轨迹均为空**，即**任何一个点都不能单独删除**（两种构型——v3 现用与候选 A——都一样，见 `.review/q3q4_bound_audit_probe.py` S4 段）。

**结论**：$25$ 点是"**逐点不可删**"的最小结构型，而空频道必须在每个采样点各测一次，故 Q4 的判空成本（$25\times6$ s $\times$ 空频道数 $\approx1050$ s/局）**在保证性口径下是结构性不可压缩的**——这解释了为什么 Q4 的实测值（$579.0$ s/源）距问题级下界（$317.0$）仍远：差额主要是下界 (4) 式**根本不含**的"巡检行程 $+$ 逐点判空测量"。

### 5.6 定位与清除：与问题三完全通用

- **定位**：由假设 6，`direction` 给出的示向度仍指向源。故一旦听到，直接用问题三 §5.3 的两视线交会（丢弃交会角 $\le2\delta$ 的视线对）与无框半平面交。
- **清除**：`/clear` 成功仅与距离（$\le20$ m）有关、**与覆盖角无关**，故问题三 §5.4 的两级清除链（$c_1$ 复测 + 垂直视线偏移 $100$ m 复测，二级半径 $R^{*}_2\le2.1383$ m、$\lVert c_2-G\rVert\le1.9029$ m，见《问题三_小证明.md》§2）**原样适用**。
- **唯一差异**：定向源下"在 $c_1$ 复测"时可能因源背对 $c_1$ 而得到 `no_signal`。此时该次复测不产生视线，应改为**在 $c_1$ 附近绕一圈取一个能听到的点**（算法步骤 8 的兜底）；数值上这一情形仅占少数，且可由射线兜底清除保证收敛。

### 5.7 朝向口径的取舍

| 口径 | 判据 | 采样需求 | 性质 |
|---|---|---|---|
| **最坏情况（本章采用）** | $G\in\operatorname{conv}(S_k\cap B(G,1000))$ | 内层 $s\le1000$ + 外环 | **保证性**：对所有朝向都被排除 |
| 朝向边缘化 | 按朝向先验加权后排除 | 可显著减少点（外环可省） | 仅统计意义，**不保证** |

本章取最坏情况，因为题面要求"**确保**全部清除"；论文中已明确写出该口径，二者可排除的区域不同。

---

## 6 算法设计与复杂度

### 6.1 主算法

```
算法 Q4：含定向源的搜索—定位—清除
输入 : Ω、频道集 K、采样构型 P4 = 内层六边形格(s=900, R≤1650) ∪ 外环(R=1900, 12 点)
输出 : 清除记录、虚拟时间

 1  构造巡回路径 tour ← NN(P4) + 2-opt，起点 = 原点
 2  noSig[k] ← ∅（频道 k 的 no_signal 观测点集）；cleared ← ∅
 3  FOR p IN tour:
 4      FOR k IN 尚未定论 的频道:
 5          (res, svd) ← /measure(p, k)
 6          IF res = near:      /clear(p,k); cleared ← cleared ∪ {k}
 7          ELIF res = direction: dets[k] ← dets[k] ∪ {(p,svd)}
 8          ELSE:               noSig[k] ← noSig[k] ∪ {p}     # 负信息记账
 9  巡回结束：对每个 k
10      IF dets[k] ≠ ∅:            转入清源流程（问题三 §5.4）
11      ELIF 用最大角间隙判据判定 "Ω 内所有 G 均可排除":  k 判空    # 包围判据
12      ELSE:                      加密采样（内层减半格距 / 外环加点）后重判
13  WHILE 待清频道: 调用问题三的两级清除链 + 射线兜底
14  /exit
```

### 6.2 复杂度

| 环节 | 复杂度 | 说明 |
|---|---|---|
| 巡回规划 | $O(m^3)$ | $m=25$，秒级 |
| 逐点检测 | 上界 $25\times20=500$ 次；自适应后平均 $\approx$ 见 §7 | 与问题三同一机制 |
| 逐频道判空 | $O(\lvert S_k\rvert\log\lvert S_k\rvert)$ / 每个检验点 | 实际只对网格检验点抽样判定 |
| **总虚拟时间** | 由**移动距离**主导（25 点巡回） | 见 §7 |

---

## 7 数值实验与验证

### 7.1 正式测试结果（表 1）

> **数据来源说明**：与问题三相同——表 1 应填报**官方模拟器**三次正式测试的结果；本机仅有自研本地模拟器（协议一致、案例编码格式不同、无联网登录），故下表先用本地模拟器按"问题四、含定向源"配置跑出的 3 局结果占位，**保留官方结果替换位**。下表**当前为本地自研模拟器占位，待官方模拟器正式测试后替换**。取数文件与口径见 `本地测试器/simulator/results/README.md`（§2 表格溯源、§3 取数口径）。

**表 1　问题四正式测试结果（本地模拟器，待官方正式测试后替换）**

| 测试 | 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|---|
| 测试 1 | P4-C1F7CD5B | 11 | $709.8$ s | $1.9$ s |
| 测试 2 | P4-6FB2A063 | 16 | $489.5$ s | $1.5$ s |
| 测试 3 | P4-5A368932 | 11 | $780.2$ s | $2.3$ s |

三局的**被清除源个数比例均为 $100\%$**（$11/11$、$16/16$、$11/11$）。程序运行时间远低于 $1200$ s 上限。

### 7.2 批量演练统计

用与正式测试完全相同的程序，在本地模拟器上跑 **100 局**（种子 $1\sim100$）。取数文件与口径见 `本地测试器/simulator/results/README.md`（§2 表格溯源、§3 取数口径）；本节统一采用**按种子单一口径**（每个种子取一次有效运行）。

**表 4　问题四批量演练（100 局，本地模拟器；按种子单一口径）**

| 指标 | 值 |
|---|---|
| **全清局数（按种子单一口径，推荐）** | **$100/100$**（$100$ 个种子各取一次有效运行，全部 $100\%$ 清除） |
| 被清除源个数比例 | $100.0\%$ |
| **平均定位清除时间** | **$664.3$ s**（中位 $651.4$ s，范围 $470.1\sim1172.3$ s） |
| 平均定位清除时间（批次口径，`plan_mixed_20260912_120910.csv` 内 $97$ 局） | $664.0$ s（中位 $646.8$ s） |
| 虚拟时间 | $8264.2$ s |
| 程序运行时间（真实） | 批次内 $97$ 局均值 $2.72$ s、最大 $5.90$ s（**注**：单独复跑的 $25$ 局为压制本机请求速率加了 $30$ ms 间隔，其真实时间与批次内不可比，故此处只报批次内的真实时间） |
| 动作条数 | $355.0$ |

> **"$100/100$"是该口径下的结论，以及批次内 $3$ 个种子的中止说明（属测试环境而非策略）**：
> ① **批次内 $3$ 个中止种子**：表 4 引用的批次 `plan_mixed_20260912_120910.csv` 内 `seed 18/46/69` 因本机套接字耗尽（`WinError 10048`）中止，故该批次内全清数为 **$97/100$**（批次内 $97$ 局均值 $664.0$ s）；这 $3$ 个种子已单独复跑确认 $100\%$ 清除。
> ② **跨批次累计的问题种子**：截至该批次，**累计**有 $25$ 个种子曾在**不同批次**中因同一环境原因中止（其中 $22$ 个来自更早批次，**不是**本批次的 $3$ 个，二者仅重叠 $3$ 个）。把这 $25$ 个种子以低请求速率（$30$ ms 间隔）单独复跑，结果 **$25/25$ 全部 $100\%$ 清除**（复算脚本 `.review/retry_aborted.py`，结果存 `results/retry_seeds_result.json`）。**不要把"批次内的 $3$ 个中止"与"跨批次累计的 $25$ 个种子"混为一谈**。
> ③ **机理**：本地自研模拟器的 HTTP 服务在每个响应后都发送 `Connection: close`（已实测确认），故**每个请求占用一个新的本机临时端口**。Windows 的动态端口范围为 $16384$ 个、`TIME_WAIT` 约 $120$ s，即可持续请求速率上限约 $1.6\times10^4/120\approx136$ 次/秒；而 $100$ 局批量共约 $3.5\times10^4$ 个请求，多线程下瞬时速率超过该上限，故偶发 `WinError 10048`。
> ④ **对官方测试无影响**：单局约 $355$ 个动作，即使按官方 $1200$ s 真实时间上限，请求速率也只有约 $0.3$ 次/秒，远低于上述上限；且官方模拟器的连接策略与本机自研版无关。
> ⑤ 为消除该干扰，正式复算对客户端加了 $12\sim30$ ms 的请求间隔——**只增加真实时间、不改变虚拟时间**（评分量）。

**与保底基线（`dog.py`）的对照**（基线取 `batch_20260912_113329.csv`，同 100 局、批次口径；其均值按**全清 $98$ 局**统计）：

| 方案 | 平均定位清除时间/s | 虚拟时间/s | 清除比例 | 动作数 |
|---|---|---|---|---|
| 保底基线 `dog.py`（同 100 局，批次口径） | $2783.2$ | $34728.1$ | $98/100$ 全清（`seed 32`、`53` 各漏 $1$ 源，属**真漏源**、非环境中止；故按种子并集口径仍为 $98/100$） | 约 $2000$ |
| **本策略（按种子单一口径）** | **$664.3$** | **$8264.2$** | **$100/100$ 全清**（批次内 $3$ 个中止种子经单独复跑确认全清） | **$355.0$** |
| **提升** | **$4.2\times$** | **$4.2\times$** | 且不再漏源 | $5.6\times$ |

**与问题三对照**：问题四的平均定位清除时间（$664.3$ s）约为问题三（$488.5$ s）的 $1.36$ 倍——增量全部来自采样点数由 $7$ 增到 $25$（外环与加密内层），**与理论预期一致**（§4 第 4 点）。

**表 4b　优化策略（v2：统一目标集 + 条件化清源链 + 外环 1930@15°，2026-09-12）**

| 指标 | 基础策略（v1，表 4） | **优化策略（v2）** | 变化 |
|---|---|---|---|
| 全清局数 | $100/100$ | **$100/100$**（100 局零中止） | — |
| **平均定位清除时间** | $664.3$ s | **$579.0$ s** | $\mathbf{-12.8\%}$ |
| 虚拟时间 | $8264.2$ s | $7192.5$ s | $-13.0\%$ |
| 动作条数 | $355.0$ | $309.9$ | $-12.7\%$ |
| 外环参数 | $(1900,12,0^\circ)$，最坏角间隙 $171.74^\circ$（稳健网格 $171.80^\circ$） | $(1930,12,15^\circ)$，开链 $-77$ s；最坏角间隙按稳健网格为 $173.86^\circ$ | 见 §5.5(4)（**网格口径已更正**） |
| 清源顺序 | 贪心最近邻 | 统一目标集 + 在线开链前瞻 | 同问题三 §6 |
| 清源链 | 固定 $c_1+\pm100$ m 偏移 | 按运行期 $R^*\le18$ m 证书条件化 | 保证性不降 |

（表 4b 取数：`results/plan_q4v2_20260912_192424.csv`，`--cfg q4v2 --dog-module dog_plan_v2 --workers 1 --min-interval 0.02`，100 局全部 $100/100$ 全清、零中止；均值 $579.0$ s/源、中位 $562.2$ s、范围 $403.0\sim836.1$ s。）

**表 4c　优化策略第二轮（v3：接入第一/二问结论，本章最终口径）**

| 指标 | v2 | **v3（最终）** | 变化 |
|---|---|---|---|
| 全清局数 | $100/100$ | **$100/100$**（零中止） | — |
| **平均定位清除时间** | $579.0$ s | **$581.9$ s** | $+0.5\%$（在噪声内，见下） |
| 虚拟时间 | $7192.5$ s | $7219.6$ s | $+0.4\%$ |
| 动作条数 | $309.9$ | $321.9$ | $+3.9\%$ |
| 补第二视线 | 单层"沿射线前进 $+$ 侧偏" | **两层**（沿射线优先 $+$ 第二问透镜点 $(1000,38^\circ)$ 兜底） | 同问题三 §5.4bis(2) |
| 第三个测点偏移 | $100$ m | **$30$ m** | 同问题三 §5.4bis(4) |
| 实测 / 最坏档下界（$317.0$）**仅存档，勿作接近度** | $2.10$ | $\approx2.08$ | — |
| 实测 / 同 $n$ 口径（$n\!\approx\!13$，$257.0$） | $\approx2.25$ | $\approx2.26$ | — |

（表 4c 取数：`results/plan_q4v3_20260912_202108.csv`，`--cfg q4v3`，100 局零中止、$100/100$ 全清；均值 $581.9$ s/源、最小 $400.1$、最大 $1028.1$ s。）

**v3 在问题四上"持平"是预期结果，且原因可解释**：定向源可能在第二视线的第一层测点**背向**（`no_signal`），此时会继续试候选（最多 $8$ 个沿射线点 $+4$ 个第二问透镜点，而 v2 只试 $8$ 个）——动作由 $309.9$ 增到 $321.9$、虚拟时间 $+0.4\%$，换来的是**补测成功率与鲁棒性**（尤其官方模拟器 $R_{\min}=1000$ m、朝向未知时）。**问题四的时间大头是"结构性巡检 $+$ 逐点判空"，v3 的两项改动都不触及这两项，故不可能显著改变 Q4 的均值**——真正压缩 Q4 只能减点，而 §5.5(5) 已证明 $25$ 点**逐点不可删**。

**改进的来源（`.review/q3q4_attrib.py` 逐项归因，30 局平均，s/局）**：

| 成本项 | v1 | v2 | 说明 |
|---|---|---|---|
| 巡检行程 | $3824$ | $3026$ | 与清源路线合并 + 外环参数优化 |
| 源间/逼近行程 | $2021$ | $1743$ | 开链前瞻 |
| 清源链内行程 | $251$ | $543$ | 互有升降（条件化后个别源走兜底） |
| 检测（$5$ s $\times$ 次数） | $1573$ | $1407$ | 含空频道逐点判空 $150\to150$ s/空频道（点数未变） |
| 频道切换 | $309$ | $273$ | — |
| 清除动作 | $122$ | $97$ | — |
| **合计** | $8100$ | $7089$ | $-12.5\%$ |

**为什么问题四的改进幅度小于问题三**：问题四的虚拟时间由**结构性的巡检**（$25$ 点构型的开链 $18\,742$ m $=3748$ s，占 $>50\%$）与**逐点判空测量**（$|P|\times6$ s/空频道 $=150$ s/空频道，是问题级下界 $50$ s 的 $3$ 倍）主导，而这两项都由"排除判据必须在**每个**采样点取证"这一硬约束决定——**运行期剪枝对判空无效**（域内每个 $G$ 都需要在程证据点，故每个采样点都必须测）。因此在保持"零盲区 + 角间隙余量"的前提下，问题四的可行改进空间主要只有**行程排序**、**外环参数**与**内层格参数**三项；要再压缩必须减少采样点数，而收敛的同心环族把点数降到 $21$ 时在 $5$ m 网格上**出现 $56$ 个盲点**，故不采用。

**表 4d　v2 两项改动的消融（P4，同 30 局；脚本 `.review/q3q4_attrib.py`）**

| 配置 | 平均定位清除时间/s | 相对 v2 全量 |
|---|---|---|
| **v2 全量**（统一目标集 + 在线开链前瞻 + 外环 1930@15°） | **$565.3$** | — |
| v2 但清源顺序退回贪心（关开链前瞻） | $591.0$ | $+4.5\%$ |
| v2 但外环换回 1900（相位仍 15°） | $566.0$ | $+0.1\%$（噪声内） |

（外环参数的收益**主要在时间**：开链 $-77$ s；"余量 $8.26^\circ\to14.42^\circ$"是 $20$ m 对齐网格的假象——按稳健网格，$(1900,12,0^\circ)$ 为 $171.80^\circ$（余量 $8.20^\circ$）、$(1930,12,15^\circ)$ 为 $173.86^\circ$（余量 $6.14^\circ$），见 §5.5(4)；若要同时改善余量，应改**内层格**为候选 A，见 §5.5(4)3。）

### 7.3 排除判据的验证

见 §5.3：最大角间隙判据与"逐方向检查"在 $300$ 组随机构型上**零不一致**（`.review/q4_exclusion.py` A 节；证明见《问题四_小证明.md》§2、§4）。

### 7.4 盲区与外环参数

见 §5.5 表 2、表 3：只用域内采样在圆域边界留下 $46.3\%$ 盲区；加外环后 $R_{\rm out}\in[1900,2000]$ m 且 $n_{\rm out}\ge12$ 时盲区为 $0$；$R_{\rm out}\ge2100$ m 反而重现盲区。理论上，保证判空的域外证据点在纯测量路线上至少需 $7$ 个、角间隔 $\le58.11^\circ$（问题级下界，见《问题四_证明_下界.md》引理 3）。

### 7.5 验证结论

1. **保证性**：内层格距 $s=900\le1000$ m 使远离边界的 $G$ 被包围；外环（$R_{\rm out}=1900$ m、$12$ 点）消除边界盲区（$20$ m 网格零盲区）。故"全部观测点 `no_signal` 且判据成立 $\Rightarrow$ 该频道无源"在**所有朝向**下成立。
2. **效率**：按种子单一口径，平均定位清除时间 $664.3$ s，比保底基线（批次口径 $2783.2$ s）快 $4.2$ 倍，动作条数减少 $5.6$ 倍；且**消除了基线在定向源上的漏检**（基线 $98/100$ 全清、$2$ 个种子各漏 $1$ 源，本策略 $100$ 个种子全部 $100\%$ 清除——批次内 $3$ 个种子的环境中止已由单独复跑确认全清）。
3. **成本结构符合预期**：问题四相对问题三的增量（$1.36$ 倍）全部来自采样点数（$7\to25$），定位与清除环节完全相同。

---

## 8 结论

1. **定向源改变了"负信息"的强度**：`no_signal` 有三种不可区分的原因，**单个 `no_signal` 不排除任何位置**（引理 1，证明见《问题四_小证明.md》§1）；排除必须依赖**包围**。
2. **排除判据（定理 3）**：$G$ 可被保证排除 $\iff G\in\operatorname{conv}(S_k\cap B(G,1000))$，可计算形式为**最大角间隙 $\le180^\circ$**（与逐方向检查等价，数值零不一致；证明见《问题四_小证明.md》§2、§4）。
3. **采样必须"变密 + 外扩"**：内层六边形格格距 $\le1000$ m（取 $900$ m）；仅用域内采样在圆域边界留下 $46.3\%$ 盲区，**必须域外采样**；外环半径的**甜点区为 $1900\sim2000$ m**（更远反而重现盲区）。
4. **推荐构型**：内层 $s=900$ m、$R_{\rm lat}=1650$ m（$13$ 点）+ 外环 $R_{\rm out}=1930$ m、$12$ 点（相位 $15^\circ$），共 **$25$ 点**，$20$ m 与边界稳健网格均零盲区；**等点数改进（候选 A）**：内层格改 $s=1000$ m、$R_{\rm lat}=1800$ m，开链 $-111$ s/局、稳健余量 $6.14^\circ\to14.66^\circ$（§5.5(4)3、§7 的 A/B 结果）。
5. **定位与清除完全复用问题三**：示向度与朝向无关、`/clear` 与朝向无关，故两级清除链原样适用；**问题四的额外难度全部在检测与排除**。
6. **实测**：按种子单一口径，平均定位清除时间 $664.3$ s，比保底基线（批次口径 $2783.2$ s）快 $4.2$ 倍，$100$ 个种子全部 $100\%$ 清除，并消除了基线在定向源上的漏检。
7. **问题级下界**（算法无关，详见《问题四_证明_下界.md》）：最坏 $n=10$ 时 $\ge317.0$ s/源的保证性下界（2026-09-12 第 4 版：对抗构型 $L^*$ 改为直接最大化）；其中"定向源抬高下界"的可证部分几乎全部来自**判空成本**（每空频道 $35\to50$ s，$35$ s 为文献档），外环的严格绕行下界 $1207.6$ s 被源间行程支配。**口径**：实测均值与"最坏 $n=10$ 档"不可比（同 $n$ 口径 $\approx2.26$、同实例口径 $\approx3.54$），且下界**不含**"巡检 $+$ 逐点判空"的行程，故比值不表示可压缩空间的真实大小。

---

## 9 理论依据与文献支撑

**（1）分离超平面定理.** 定理 3 的核心是"点不属于闭凸集 $\iff$ 存在分离超平面"，这是凸分析的经典结论（文献 [1] 第 11 章）。它把"对所有朝向都存在探测点"这一**连续量词**的判定，化归为一个**凸包/最大角间隙**的有限计算。

**（2）定向视场下的覆盖与包围.** "单点覆盖不足以判定、需要多方向覆盖"在传感器网络与计算几何中有对应命题：定向传感器（摄像机/定向天线）的覆盖判定属"带视场的覆盖/守卫"问题，其保证性判据必须考虑**视场方向**；本章定理 3 给出的是"闭半平面视场 + 距离截断"下的**精确**形式（可由分离超平面自证，不依赖文献）。与本问题结构最接近的是**限视距 watchman route**与"有限视场追逃"（文献 [22]、[24]）：二者都说明"视距有限 ⇒ 保证性巡视必须绕行/多次访问"，这正是本章 §5.5 的采样构型与《问题四_证明_下界.md》引理 3/4 的文献对应物；"最少观察点数"形式的组合下界见 [25]。

**（3）搜索论.** 把 `no_signal` 解释为"对（位置, 朝向）联合假设的部分削弱"，与最优搜索论中"负信息即假设剪除"的框架一致（文献 [21]）；本章取**最坏朝向**（而非边缘化），对应鲁棒/悲观推断（文献 [3]）。**注意**：通道级的"负信息"**不能**用分组测试加速——题面 API 一次只能测一个频道（无合并检测），故 [37] 的思路不适用。

**（4）问题级下界的文献支撑与后续改进方向.** 下界四部分的文献对应物：行程项 = 最短开路精确 DP（[26]）与近似侧 [27]；判空项 = "负信息必须覆盖整个候选域"（[21]）与"外部证据点必须绕边界一圈"（[22]、[25]）；保证性巡视的结构成本 = 限视距巡视/追逃（[22]–[24]）。**后续改进方向（本文未实现）**：① 把巡检采样点与源清除点放进**同一条开链**统一规划（TSPN，[29]），可望把巡检行程进一步并入清源路线；② 纯方位定位的最优观测几何（D-最优，交会角 $\approx90^\circ$）用于选第二/第三视线（[30]）；③ "何时中断巡视去清源"的在线决策与竞争比（[31]）；④ 覆盖巡视路线的分解/生成树构造（[32]–[34]）与带界信息路径规划（[35]）。

---

## 参考文献

沿用问题一至问题三的编号 [1]–[37]（完整条目见《问题三.md》§参考文献与 `参考论文/README.md`）。本章直接用到 [1]（分离超平面）、[3]（有界误差/鲁棒推断）、[21]（最优搜索：负信息剪除）、[22]（限视距 watchman route）、[24]（有限视场追逃）、[25]（捕获界）、[26,27]（$L^*$ 的精确下界与近似上界）、[29]（TSPN：巡检与清源统一的开链规划）、[30]（纯方位定位的最优观测几何）、[31]（何时中断巡视的在线决策）、[32]–[35]（覆盖巡视与信息路径规划）、[37]（分组测试**不适用**的说明）。

> **编号更正（2026-09-12）**：本版之前的 §9 曾把 [20]、[21] 称作"定向传感器网络"类文献，实为 **[20] Lin–Kernighan（TSP 启发式）**与 **[21] Stone（最优搜索论）**；现已改正为上面的对应关系。**注**：[16]–[37] 的书目信息本机无法逐条联网核实（Semantic Scholar 429、DBLP 拦截、arXiv 断连，仅 OpenAlex/Crossref 可用），已在 `参考论文/README.md` 统一标注【待核对】；本章全部结论均可由附录 B 代码与 `.review/q4_exclusion.py`、`.review/q4_gap_grid_robust.py` 复现，**不依赖**文献。

---

## 附录 A　核心算法伪代码

```
函数 EXCLUDABLE(S_k, G):                     # 最大角间隙判据
    Q ← { p ∈ S_k : ‖p−G‖ ≤ 1000 }
    if |Q| < 2: return False
    A ← sort({ atan2(p−G) : p ∈ Q })
    gap ← max( A[i+1]−A[i] )，含首尾环绕
    return gap ≤ 180°

函数 Q4_CONFIG():                            # 表 3 的构型
    内层 ← 六边形格(s=900, 保留 ‖p‖ ≤ 1650)
    外环 ← { 1900·(cos(2πi/12), sin(2πi/12)) : i = 0..11 }
    return 内层 ∪ 外环                       # 共 25 点，零盲区

函数 Q4_RUN():
    tour ← NN(Q4_CONFIG(), 原点) + 2-opt
    for p in tour:
        for k in 未定论频道:
            res ← /measure(p,k)
            if res=near: /clear(p,k)
            elif res=direction: dets[k].add((p,svd))
            else: noSig[k].add(p)
    对每个频道 k:
        if dets[k] ≠ ∅: CLEAR_CHAIN(k)                    # 问题三
        elif 对所有网格检验点 G, EXCLUDABLE(noSig[k], G): k 判空
        else: 加密采样后重判
```

## 附录 B　Python 参考实现

> 完整可运行程序见 `本地测试器/simulator/dog_plan.py`（`q4_points()` 与 `Planner`）。核心判据与构型：

```python
# -*- coding: utf-8 -*-
"""问题四策略核心（摘自 dog_plan.py）。无 numpy 依赖。"""
import math

ARENA, D2R, DELTA, DETECT_R = 1800.0, math.pi / 180.0, 1.0, 1000.0


def hex_lattice(s, Rmax):
    """六边形（三角）格，格距 s，保留 ‖p‖ <= Rmax 的点。"""
    pts = []
    ny = int(Rmax / (s * math.sqrt(3) / 2)) + 2
    nx = int(Rmax / s) + 2
    for j in range(-ny, ny + 1):
        y = j * s * math.sqrt(3) / 2
        for i in range(-nx, nx + 1):
            x = i * s + (j % 2) * s / 2
            if math.hypot(x, y) <= Rmax:
                pts.append((x, y))
    return pts


def q4_points(s=900.0, Rlat=1650.0, Rout=1900.0, nout=12):
    """内层六边形格 + 域外包围环；20 m 网格复核零盲区（表 3）。"""
    pts = hex_lattice(s, Rlat)
    for i in range(nout):
        a = 2 * math.pi * i / nout
        pts.append((Rout * math.cos(a), Rout * math.sin(a)))
    return pts


def excludable(no_sig_pts, G):
    """定理 3 的可计算形式：G ∈ conv(no_sig_pts ∩ B(G,1000)) <=> 最大角间隙 <= 180°。"""
    Q = [(p[0] - G[0], p[1] - G[1]) for p in no_sig_pts
         if math.hypot(p[0] - G[0], p[1] - G[1]) <= DETECT_R + 1e-9]
    if len(Q) < 2:
        return False
    ang = sorted(math.atan2(q[1], q[0]) for q in Q)
    gaps = [ang[i + 1] - ang[i] for i in range(len(ang) - 1)]
    gaps.append(ang[0] + 2 * math.pi - ang[-1])
    return max(gaps) <= math.pi + 1e-12
```

**调用示例（复现盲区与外环参数）：**

```python
# 需 numpy；见 .review/q4_exclusion.py
#   A 节：判据等价性（300 组随机构型，零不一致）
#   C 节：仅域内采样(s=950, R_lat=1740, 13 点) -> 6361 个检验点中 2944 个不可排除（46.3%）
#   D 节：外环扫描 -> R_out=1900/2000 且 n_out>=12 时盲区 0；R_out>=2100 重现盲区
#   20 m 网格复核：s=900, R_lat=1650, R_out=1900, n_out=12 -> 共 25 点，零盲区
```
